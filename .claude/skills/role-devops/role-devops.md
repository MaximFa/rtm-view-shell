---
role: devops
project: RTM View Shell
version: 0.2
last_verified: 2026-08-29T14:10:00Z
owner: devops
reviewer: curator
---
# role-devops — RTM DevOps role-skill (Specialist Protocol)
> COLD-STARTED FROM ARTIFACTS (deploy/, tools/, Installations/, db/tools/, git log --oneline deploy/),
> NOT session narrative. Standard: .coord/protocols/role-skill-standard.md.

## §A CORE  (invariant · HARD CAP ~40 lines · read EVERY init)
### ⛔ ЧП / EMERGENCY MODE — ACTIVE (declared 2026-06-25 coordinator-0624; REMOVE on operator lift)
Release is bug-ridden (dashboards) + the reports version blocking its fixes is in catastrophic state → emergency until the operator lifts ЧП.
1. NO corner-cutting; ANY detail (ESPECIALLY visual) = critically RED — every defect is a blocker, no "minor".
2. NO decision around the coordinator; every fork → coordinator → operator (ONE at a time, by importance, plain language).
3. NO unsanctioned runs: do NOT hand the operator a chat CC run-prompt code-box UNTIL the coordinator's §4 bless.
4. Coordinator PERSONALLY visual-verifies EVERY closed gap (not object-store/report alone).
5. Verify on REAL prod-mirror data (234 backup, RTSData_*); our env = a FROZEN data-mirror of prod; our migration package = our migrated DB. One-time seed from the backup.
6. Protocol shorthand: `.` = `коорд: входящие`; `..` = "check the result" (specs know it).
7. Coordinator + operator steer the recovery out of the dive.

Role: Deployment scripts, packaging, ops tooling. Owns: deploy/, tools/Build-*.ps1, Installations/, db/tools/.
Does NOT write business logic — code changes flow via CC prompts.

**Reality wins — update me.** If §C VERIFY finds §A disagrees with the code/artifacts, the CODE is right; mark the line superseded.

Cardinal truths (source-pinned):

1. **MANDATORY Compare-ToBaseline before ANY deploy. Never rely on memory.**
   Never apply migrations/binaries without FIRST running Compare-ToBaseline against THAT server.
   Never reuse another server's -MigrationList or trust recollection — each server's applied-set differs.
   The Compare IS the gate: yields the server-specific -MigrationList AND catches runtime-critical drift.
   · SOURCE: 234+45 deploys 2026-06-19; journal 2026-06-19; 234 baseline_delta 111313 -> FULL-10 vs 45's different list

2. **§35: PS1 files MUST be UTF-8 BOM + CRLF for Windows PowerShell 5.1.**
   Box-draw/Cyrillic without BOM -> "Unexpected token" parse failure.
   · SOURCE: CLAUDE.md §35, 024feef BOM fix

3. **Preserve operator config across binary updates (appsettings.json + *.Production.json).**
   · SOURCE: 234 28P01 incident; e46e849

4. **EVERY run-box handed to the operator opens with WHERE IT RUNS.** First line, before the code:
   the target machine (DEV / 234 / 45 / 140), the database it touches, and whether it writes or only reads.
   The operator drives several boxes on several machines in one session; a box that does not say where it runs
   makes HIM carry that state — and a converge box pasted into the wrong console is not recoverable.
   · SOURCE: operator directive 2026-08-29 ("ГДЕ БЕЖИМ" — before every run-box)
5. **EVERY password prompt in a run-box is `Read-Host -AsSecureString`. NEVER a bare `Read-Host`.**
   A bare prompt echoes the secret to the screen, into the console history file, and into whatever the operator
   pastes back. It already happened once: a plain `Read-Host "postgres password"` put the PRODUCTION superuser
   password of server 234 into the chat in clear text (2026-08-30). Masked input is not a preference — it is the
   only form allowed, on every machine, including throwaway diagnostics.
   · SOURCE: operator directive 2026-08-30 ("я ввожу, видны ****")
6. **ONE TASK PER MESSAGE. Not one topic — one ACTION.** A message that says "copy these files, then run this box"
   is two tasks: the operator does the first, the box runs against the not-yet-copied state, and the output is
   garbage that costs another round-trip to interpret. Exactly that happened on 2026-08-30 (three migration files
   "No such file" because the run followed the copy instruction in the same message). Hand over one action, wait
   for its result, then hand over the next.
   · SOURCE: operator directive 2026-08-30 ("одна задача - один ответ")


## §B LESSONS  (append-only · dated · source-pinned · status)
- 2026-06-19 · StrictMode scalar.Count: Get-ChildItem/Sort-Object/.Split() return SCALAR on single item -> .Count THROWS under Set-StrictMode -Version Latest. ALWAYS wrap collection results in @(...). · SOURCE: de5e835 (@() on $allBackups L137 + $migrations L204) · status: active
- 2026-06-19 · §35 BOM re-encode via Python: a TEXT write drops the UTF-8 BOM -> PS 5.1 fails. Re-encode in BINARY: b"\xef\xbb\xbf" + text_CRLF.encode("utf-8") + fsync; verify head -c3 == ef bb bf. · SOURCE: d62e704 dropped BOM -> 024feef fix · status: active
- 2026-06-19 · Deploy must PRESERVE operator config: Update-RTMView Shell-preserve missed appsettings.json -> overwrote 234's live conn-string -> 28P01 password auth failed. Preserve every operator config file, not just *.Production.json. · SOURCE: 234 Shell crash; e46e849 fix · status: active
- 2026-06-19 · pg_dump completeness: pre-apply pg_dump must run as OBJECT OWNER (postgres), else backup is INCOMPLETE. Run the whole apply path as postgres. · SOURCE: 234 STEP-4 pg_dump-completeness flag · status: active
- 2026-06-19 · pkg-copy PD-007: staged package file gets truncated by mount write-back AFTER native commit; re-materialize from HEAD + byte/hash-verify the swap source before every swap. · SOURCE: BOM + StrictMode fixes both needed pkg re-materialize · status: active
- 2026-06-25 · ref: visual-check prep runbook = **docs/Visual-Test-Preflight.md** (profiles A=rebuild / B=running; shared gate Chrome→Soma /health:5199→Shell /ops/health.up→restart×3→start). Use when running or awaiting a visual check. · SOURCE: docs/Visual-Test-Preflight.md · status: active

- 2026-07-03 · Local self-signed HTTPS (Kestrel Store cert, AllowInvalid=true) does NOT complete the TLS handshake on the local box (cert/client/protocol-agnostic; TLS1.2/1.3=OS default; Schannel silent) -> local prod-parity HTTPS validation blocked; validate on the REAL server (234, live insightense cert) instead. Candidate fix: PFX Path like RTM. · SOURCE: local prodparity redeploy 2026-07-03 · status: superseded-by 2026-08-30 (fixed: see below)
- 2026-07-03 · Install-RTMView -Mode Full writes LITERAL REPLACE_FQDN/REPLACE_HTTPS_PORT/REPLACE_CERT_SUBJECT placeholders into the deployed appsettings.json when per-server params (-Fqdn/-ShellPort/-CertSubject) are omitted -> broken Kestrel. RULE: fail-fast on missing per-server params, OR redeploy code-only changes via Update-RTMView (preserves the server's real appsettings/cert/data.sys) — NEVER Install -Mode Full on an existing server. · SOURCE: 234 deploy decision 2026-07-03 (role-coordinator §B) · status: active

- 2026-07-03 · Build-ProdRelease packs Install/Update-RTMView.ps1 at the ZIP ROOT but does NOT pack db\tools\ (only DB\ dump via $PublishDB). Update-RTMView resolves Compare-ToBaseline at $RepoRoot\db\tools\ (parent of script dir) -> in a standalone package deploy the path misses -> [E1] drift gate SILENTLY SKIPS (WARN, does not stop) = §DEPLOY-16 mandatory gate bypassed. FIX (follow-up): Build-ProdRelease must pack db\tools\ next to Update-RTMView so the drift gate runs on the target server. For a code-only/zero-migration deploy the skip is acceptable WITH a compensating EF-history-count check (unchanged) + no 'Applying migration' in startup log. · SOURCE: 234 b03b870 deploy 2026-07-03 (coordinator RULING) · status: active

- 2026-07-11 · ONE task/command to the operator at a time — hand a SINGLE run-command/step, WAIT for the operator's result, THEN issue the next. Do NOT batch multiple dotnet/deploy commands in one hand-off (operator has to execute + report each serially). Applies to build+test gates, multi-step runbooks, sequential deploys. · SOURCE: operator directive 2026-07-11 (batched build+test to operator) · status: active

- 2026-07-13 · PS1 saved WITHOUT UTF-8 BOM parses fine in pwsh7 (UTF-8 default) but FAILS in Windows PowerShell 5.1 (no BOM -> Win-1252 codepage decodes UTF-8 multibyte em-dashes as garbage -> broken string literals -> ParserError). §35 PS1-parse DoD MUST be verified with **powershell.exe (5.1)** + first-bytes EF BB BF — NEVER pwsh7 alone (it hides the defect). My earlier R2 parse-check (ParseFile) used the generic parser -> did NOT catch the BOM/decode issue. · SOURCE: commits 0966263/88f4888 stripped BOM on 3 PS1 -> Build-ProdRelease ParserError on 234 box, 2026-07-13 · status: active

- 2026-07-13 · DOUBLE-BOM trap: §0.7 re-sync `git show HEAD:<f> > <f>` of a BOM'd file via **PowerShell `>`** (Out-File UTF8BOM) PREPENDS a 2nd BOM to git-show's already-BOM'd bytes -> EF BB BF EF BB BF. Windows PS 5.1 reads BOM#1 as file-BOM and BOM#2 as a literal ZWNBSP (\uFEFF) before #Requires -> `#Requires` unrecognized -> 'Unexpected attribute CmdletBinding' parse cascade. The COMMIT is fine (single BOM); only the WORKING TREE is corrupt. FIX: re-sync BOM'd files BYTE-EXACT (bash `>` / Python binary write / git checkout-index) — NEVER PowerShell `>`. When a PS 5.1 parse fails on a BOM'd file, check the WORKING-TREE bytes (head -c6) not just the commit blob. · SOURCE: cb6f069 working-tree double-BOM broke Build-ProdRelease parse, 2026-07-13 · status: active


- 2026-07-13 · 140 fresh-install shakedown: a FAILED install left C:\RTMView\Shell+RTM deployed AND the migrate exe (CcDashboard.Web.exe, PID 8276) STILL RUNNING (Defect-B old build's migrate never exited). A re-provision STEP1 re-check MUST wipe dirs + orphan-kill even when services are absent. · rule: never assume 'services gone' = 'clean' — verify dirs + running exes. · SOURCE:140 run, PID8276 killed · status: active
- 2026-07-13 · PowerShell STRIPS inline double-quotes when calling native psql `-c 'SELECT ... "PascalTable"'` -> psql gets unquoted -> folds lowercase -> false `relation does not exist`. Use `psql -f <file>` (here-string @'...'@ -> Set-Content) so quoted PascalCase survives. SAME root as Provision-FreshDb [VERIFY] false-FAILs (Defect C). · SOURCE:140 sanity, "RTSGrid_Metric"=202 only via -f · status: active
- 2026-07-13 · Windows Service registered directly (UseWindowsService, sc binPath=exe) runs CWD=C:\Windows\System32, so a RELATIVE Serilog path (logs/log-.txt) writes the SERVICE log to C:\Windows\System32\logs\, NOT the app dir (app-dir logs/ only holds the migrate run, whose CWD Provision sets). Fix: absolute Serilog path or service WorkingDirectory. · SOURCE:140 svc log-20260713.txt · status: active
- 2026-07-13 · app user ccdashboard_user lacks CREATEDB; if RTMViewShell (StartType=Automatic) starts BEFORE the DB is provisioned, DatabaseInitializer Migrate -> NpgsqlDatabaseCreator.CreateAsync -> 42501 permission denied -> [FTL] Application terminated. Canonical order (Provision creates DB as postgres + -NoStartServices) avoids it; harden: app should ASSUME DB exists. · SOURCE:140 svc log 00:42 FTL vs 03:39 clean · status: active


- 2026-07-13 · Fresh-install SHELL crashes on RESTART if the platform tenant slug was changed from 'platform': SeedPlatformTenantAsync hardcodes slug=='platform' (DatabaseInitializer.cs:119/126, no Seed:PlatformTenantSlug), so a renamed platform tenant (e.g. ->nayax for the FQDN) isn't found -> seeder creates a DUP 'platform' tenant -> SeedSuperadminAsync re-creates 'admin' -> GLOBAL UserNameIndex 23505 -> Program.Main crash. Also users unique index is global (not per-tenant per §6.2). Any -SkipShell RTM update that bounces the Shell surfaces this. Fix = configurable seed slug + per-tenant username index. · SOURCE:140 svc log 23505 + tenants(nayax/platform dup) 2026-07-13 · status: active


- 2026-07-13 · Update-RTMView -SkipRTM/-SkipShell does NOT leave the other service untouched: it STOPS+STARTS (bounces) BOTH (stop@106-115/start@392-401 ignore the -Skip flag; only the binary redeploy is skipped). The bounce is USEFUL — an RTMService bounce re-triggers the adapter snapshot on the pipe -> populates NGC_Queues (seal). Also -DBPassword is MANDATORY (internal pg_dump overwrites PGPASSWORD with $DBPassword default '' -> aborts AFTER both services stopped -> both DOWN). And -SkipCacheMigration to not touch a live Garnet. · SOURCE:140 Shell redeploy 2026-07-13 · status: active
- 2026-07-13 · Verify an EF migration applied by checking the ACTUAL object, not __EFMigrationsHistory (App-context history table name/schema varies — ILIKE '%migrationshistory%' found 0). For PerTenantUserNameIndex: `SELECT indexdef FROM pg_indexes WHERE schemaname='identity' AND tablename='users'` -> UNIQUE IX_users_NormalizedUserName_TenantId WHERE IsActive proves H; old UserNameIndex becomes non-unique. · SOURCE:140 2026-07-13 · status: active
- 2026-07-13 · Update-RTMView drift-gate path bug: it looks for Compare-ToBaseline.ps1 at C:\Temp\db\tools\ (hardcoded C:\Temp) not the extracted package dir C:\Temp\<pkg>\db\tools\ -> WARN-skips every time even when the Full pkg ships it. Gate never runs. · SOURCE:140 Update output 2026-07-13 · status: active


- 2026-07-14 · Adapter (RTM.Twilio) warm binary-swap on a SHARED pipe can leave BOTH feeds down: adapter=pipe CLIENT reconnects, but the RTM pipe-SERVERS may not re-accept a reconnecting client without a SERVER restart -> 'Pipe hasn't been connected yet' persists on both rtmpipe(legacy)+rtmpipe_v3(ours). Fix path that WORKED: rollback old binary + restart legacy RTM. Lesson for adapter-swap runbooks: PRE-PLAN restarting the RTM pipe-servers (our RTMService + legacy RTM) inside the window, and run the isolating probe (restart our RTMService -> does the new adapter connect rtmpipe_v3?) BEFORE concluding a binary regression. A green WIRE-14/14 (format) does NOT cover live warm-server connect behavior. · SOURCE:140 6ebd39f rollback 2026-07-14 · status: active (CORRECTED 2026-07-14: operator restarted ALL services in recovery -> cannot attribute to a 6ebd39f binary regression; leading cause = pipe-SERVERS need restart to re-accept; adapter-swap runbook MUST restart RTM pipe-servers then RE-TEST 6ebd39f before concluding a binary bug) [OPERATOR DIRECTIVE 2026-07-14: legacy RTM must NEVER be restarted as part of our procedure — live prod, not ours. Acceptance = adapter self-reconnects to a STILL-RUNNING legacy pipe-server (only OUR RTMService may bounce). The recovery that restarted legacy is NOT a valid standing procedure.]

- 2026-08-29 · `git check-ignore -v` prints the LAST MATCHING RULE, **negation included**: a line carrying `!<pattern>` means the file is NOT ignored. I claimed "-v prints a line per ignored path" — wrong in exactly the case that matters. In the 2026-08-17 review four printed lines carried `!backlog.md` and meant the rule WORKED; they were read as a violation. RULE: never infer a command's meaning from the presence of output — read what the output SAYS, and settle "is the work saved" by tree membership (`git ls-tree` / `git rev-parse <ref>:<path>`), never by ignore-machinery text. · SOURCE: curator-0611 entrance acceptance 2026-08-29 (inbox/devops.md, Q3 defect); `git check-ignore -v .coord/backlog.md` = empty/exit 1 while `git ls-tree -r --name-only v3 .coord/backlog.md` = 1 · status: active
- 2026-08-29 · NEGATIVE CONTROL, stated to norm strength: it is NOT enough to also check "what must not be there". The SAME predicate must be run against a deliberately absent marker and MUST return 0. A check that cannot return 0 is not a check — it is a formality that will pass on a broken artifact. My weaker phrasing ("the negative half — what should not be there") let a check that never fails read as rigorous. · SOURCE: curator-0611 entrance acceptance 2026-08-29 (inbox/devops.md) · status: active
- 2026-08-29 · §C VERIFY was written against the WORKING TREE (`Select-String`) = NORM-CUR-13 violation on all four items, and item 4 goes GREEN precisely when the `Compare-ToBaseline` gate is WARN-skipped (Update-RTMView.ps1:167) — twice already in the field (2026-07-03 package shipped without `db\tools\`; 2026-07-13 hardcoded `C:\Temp\db\tools\`). A check confirming a §A truth that is NOT in force at that moment is worse than no check. FIXED in §C below: object-store form + explicit expected counts + disk-vs-store hash + gate-reachability. · SOURCE: curator-0611 entrance acceptance 2026-08-29; role-devops §B 2026-07-03 / 2026-07-13 · status: active

- 2026-08-29 · Operator run-boxes are executed by PASTING into an interactive PS console, where `return` at top level does NOT abort the remaining pasted lines. My R1 box used `if (-not $dump) { ...; return }` as a stop — the "STOP" printed and execution carried on, creating an empty `rtmviewdb_reh` and calling `pg_restore` with an empty path. RULE: a run-box guard must be a BLOCK (`if (ok) { ...work... } else { "STOP: reason" }`) or a real script file — never `return`/`exit` as the stop, and never a stop that only prints. · SOURCE: DEV R1 box 2026-08-29 (dump absent on DEV) · status: active
- 2026-08-29 · `Set-Content -Encoding UTF8` in Windows PowerShell 5.1 writes UTF-8 **WITH BOM**, and `psql -f` then fails with `syntax error at or near "<U+FEFF>SELECT"`. Mirror image of §35 (where PS1 files REQUIRE the BOM): for files consumed by psql/pg tools write BOM-LESS — `[IO.File]::WriteAllText($f,$sql,(New-Object System.Text.UTF8Encoding($false)))` — and prove it in the same box with `Format-Hex -Path $f | Select -First 1` (must start `53 45 4C`, not `EF BB BF`). Same file name reused across attempts hides the fix — use a NEW name. · SOURCE: DEV feed-probe box 2026-08-29, three identical BOM failures · status: active

- 2026-08-29 · A migration's NAME is not the name of the object it creates. I probed `to_regclass('public."WfmTenantSettings"')` as the signature object for migration `20260721110721_WfmTenantSettings` — it returns NULL on a CORRECTLY migrated DB, because that migration adds six `Wfm*` COLUMNS to `tenant_settings` (`:13-49`, every `AddColumn` carries `table: "tenant_settings"`), and creates no table at all. Shipped to Phase P unfixed, it would have been a false STOP on a live server. RULE: derive every signature probe from the migration's CODE (`CreateTable` vs `AddColumn` vs `Sql`), never from its name — the same class the coordinator hit on 46c6d4b hours earlier, which means reading the commit/migration title instead of its body is a standing trap, not a one-off slip. · SOURCE: rehearsal R4 2026-08-29, migration source 20260721110721_WfmTenantSettings.cs · status: active

- 2026-08-29 · Do not build an EXPECTED NUMBER from a truncated view. I declared "expect 6 Wfm columns" from `grep ... | head -12`; the migration adds EIGHT, so a fully-correct result read as a mismatch. Same shape as the name-vs-object trap the same hour: in both cases the CHECK was fine and the EXPECTATION was manufactured from a partial source. RULE: an expected count is derived from the COMPLETE authoritative source (`grep -c` / full `grep -o | sort -u`), never from a head/tail-limited excerpt — and if the excerpt was limited, say so instead of quoting a number. · SOURCE: rehearsal R4/R4b 2026-08-29, 20260721110721_WfmTenantSettings.cs · status: active

- 2026-08-29 · `$ErrorActionPreference = "Stop"` + a native tool that writes to stderr = self-inflicted abort. `psql` emits `NOTICE: ... does not exist, skipping` on stderr for every idempotent `DROP ... IF EXISTS`; under Stop, PowerShell converts that into a terminating `NativeCommandError` and kills the run mid-layer, leaving a partially applied state that LOOKS like a SQL failure. RULE for run-boxes driving psql/pg_dump/pg_restore: `$ErrorActionPreference = "Continue"`, judge success by `$LASTEXITCODE` (with `-v ON_ERROR_STOP=1` inside psql), and grep the captured output for ERROR/FATAL — never treat "stderr is non-empty" as failure. · SOURCE: rehearsal R5a 2026-08-29, aborted on the first NOTICE of 01_ngc_functions.sql · status: active

- 2026-08-29 · `pg_restore --data-only` is ALL-OR-NOTHING PER TABLE: if the dump's column set differs from the target's by even one column, the whole table loads ZERO rows (`ERROR: column "X" of relation "T" does not exist`). Converge-style transfers between two schema generations MUST pre-compare structure (restore the same dump into a staging DB, diff `information_schema.columns` for every transferred table) BEFORE the run, and use an explicit column-list copy for the mismatched ones. Found on the 234 dump: NGC_Queues + NGC_AgentGroups carry June's `CreatedDatetime`, absent in d1982de's schema.sql; 15/17 tables matched. · SOURCE: rehearsal R6/R6b 2026-08-29 · status: active
- 2026-08-29 · **db/tools sequence resync is a NO-OP for identity columns.** `Provision-FreshDb.ps1` step 7 (block reused from `Restore-All.ps1`) selects sequences via `pg_depend.deptype='a'`, but `GENERATED ALWAYS AS IDENTITY` sequences are `deptype='i'`. Measured on the rehearsal DB: all 15 public sequences are 'i', none 'a' -> the standard resync silently resyncs NOTHING, and `pg_sequence_last_value` stays NULL, so the first app insert after any restore-based provisioning hits `23505 duplicate key`. Very likely the root cause of the server-45 incident (§B 2026-06-07), which was hunted in the procedure while the TOOL was broken. Any resync must select `deptype IN ('a','i')`. · SOURCE: rehearsal R6b 2026-08-29, pg_depend deptype census · status: active

- 2026-08-29 · Operator directive: **every run-box states WHERE IT RUNS before the code** — machine, database, read-or-write. Given while the same session was alternating between DEV (rehearsal, rtmviewdb_reh) and a pending 234 run with a nearly identical-looking box: the risk is not the operator's memory, it is that two boxes can look the same and land on different servers. A box without a destination header is not ready to hand over. · SOURCE: operator 2026-08-29 · status: active

- 2026-08-29 · A structural diff that compares COLUMN NAMES ONLY is not a structural diff. Names matched perfectly for `RTSData_UserStatusLog` while the TYPE differed — dump `integer`, target `bigint` — and that drift loads SILENTLY (widening cast), 202917 rows, green count, nobody the wiser. Compare name + data type (+ ordinal), both directions, for every transferred table. Widening (int->bigint, varchar(n)->wider, ->text) is safe and gets RECORDED; NARROWING is always a STOP because the loss happens during the copy, not at a gate. · SOURCE: rehearsal Part B struct-diff 2026-08-29; target bigint since BE migration 20260526203609:77 · status: active
- 2026-08-29 · Gate thresholds must name the KNOWN SET, not count it. My struct-diff gate said "more than two drifted tables = STOP"; the third finding tripped it correctly but the message was arithmetic ("3 > 2") instead of naming what was new. Compare against an explicit approved list so the gate reports "NEW drift: <table>" — a count says something changed, a list says WHAT. · SOURCE: rehearsal Part B 2026-08-29 · status: active

- 2026-08-29 · **`setval` needs `quote_ident` — and the shipped resync block lacks it.** `Provision-FreshDb.ps1` step 7 builds `format('SELECT setval(%L, ...)', r.sch||'.'||r.seq, ...)`: the sequence name goes in unquoted, so a PascalCase sequence is folded to lower case and the call dies with `relation "identity.role_claims_id_seq" does not exist`. This is a SECOND defect in the same block, independent of the deptype one — even with `deptype IN ('a','i')` fixed, every PascalCase sequence still fails. Correct form: `quote_ident(r.sch)||'.'||quote_ident(r.seq)`. · SOURCE: rehearsal Part B2 2026-08-29 · status: active
- 2026-08-29 · Piping one native tool into another via `cmd /c "a | b"` from PowerShell is not a reliable transport: the `\copy ... TO STDOUT` -> `\copy ... FROM STDIN` pipe failed with `could not write COPY data: Broken pipe` / `Invalid argument` (the receiving process never came up, quoting of the two quoted exe paths inside one cmd string). For column-list transfers use an intermediate TEXT FILE (`\copy ... TO '<file>'` then `\copy ... FROM '<file>'`) — a temp text file is transport, not a substitute source, so it does not violate the single-source rule. · SOURCE: rehearsal Part B2 2026-08-29 · status: active
- 2026-08-29 · `psql` on a Windows console runs with the console codepage (WIN1252 here): any UTF-8 data outside it dies with `character with byte sequence 0x.. has no equivalent in encoding "WIN1252"` — and the failing query is the VERIFY, so a correct database looks broken. This project's data contains Hebrew (business-unit names). Every verify script must set `client_encoding` to UTF8 (`PGCLIENTENCODING=UTF8` or `\encoding UTF8`) before selecting user-visible text. · SOURCE: rehearsal Part B2 2026-08-29, NGC_BusinessUnit names · status: active

- 2026-08-29 · I re-ran the RELOAD phase against a database that already held the previous run's data, instead of rebuilding from restore — every table then failed with `duplicate key`, and the final counts, though correct, were the SUM of two runs and worthless as evidence. A reload phase is only meaningful on a freshly rebuilt target; "the numbers came out right" is not the same as "this run produced them". RULE: any re-run of a load phase starts from the restore step, and the run-box itself must rebuild the target rather than trusting the operator (or me) to remember. · SOURCE: rehearsal Part B3 2026-08-29 · status: active
- 2026-08-29 · Integrity must be measured ON THE SOURCE BEFORE the transfer, not only on the target after it. Doing so proved that 21 dangling RTSGrid_Cell rows and 81 dangling NGC_UserAgentgroup rows ARRIVED that way from 234 and were not created by our reload — without that baseline the same numbers on the target are indistinguishable from damage we caused, and the honest conclusion ("pre-existing data drift, report it, do not silently repair") would have been unavailable. Add a source-side integrity pass to every data-migration procedure. · SOURCE: rehearsal Part B3 2026-08-29 · status: active

- 2026-08-30 · **An integrity gate is measured against the state BEFORE the change, not against an ideal.** My VERIFY expected 0 dangling references; the 234 source already carries 21 + 81 of them, so on the live server that gate would have fired AFTER the drop and the reload — at the most expensive point — on drift that predates us, and would have read as "the transfer broke everything". Correct form: measure the baseline in the source before the change, compare the target to THAT, and treat BOTH directions as failures — more means we created breakage, LESS means we silently repaired customer data and every later number becomes ambiguous. Zero is the expectation only where the source measured zero. · SOURCE: coordinator ruling 2026-08-30 on the rehearsal artifact · status: active

- 2026-08-30 · **You cannot grep .NET assemblies for a source string with `Select-String`.** Managed string literals are stored UTF-16LE, so a plain text search returns 0 on a binary that genuinely contains the marker. My probe-(a) check reported `markers: 0` for BOTH the deployed tree and the package while the source carries exactly 11 (`ScreenEditorPage.razor`, identical in v3 and d1982de) — a false negative that would have read as "the probe is not in the build". Correct forms: search the UTF-16LE byte pattern, or count in the OBJECT STORE and separately prove the deployed tree was built from that commit. · SOURCE: rehearsal R7a 2026-08-30 · status: active

- 2026-08-30 · Two run-box traps in one step: (1) `Invoke-WebRequest -SkipCertificateCheck` is PS7-ONLY — on Windows PowerShell 5.1 a self-signed Kestrel cert fails with "Could not establish trust relationship", which reads as an application failure when it is a client limitation; use an `ICertificatePolicy` trust-all callback + explicit Tls12. (2) A per-byte PowerShell loop over hundreds of MB of DLLs effectively hangs — read the file once with `Encoding.Unicode` and use `String.IndexOf`. Also: the compiler INTERNS identical literals, so a marker appearing 11 times in source appears fewer times in the assembly — the honest predicate is PRESENCE in the expected assembly, never equality with the source count. · SOURCE: rehearsal R7b/R7c 2026-08-30 · status: active

- 2026-08-30 · **The parked "local HTTPS is impossible" lesson (2026-07-03) is SUPERSEDED — it was the certificate, not the platform.** The pre-existing `CN=localhost` cert in `LocalMachine\My` (thumbprint 81530E79…, valid to 2028) produced `ERR_CERT_INVALID` in Chrome — a structurally rejected cert, so Chrome offers no "Proceed" at all — and blocked the UI entirely, while HTTP was force-redirected to HTTPS by the app. Creating a fresh self-signed cert WITH an explicit Server-Authentication EKU (`-TextExtension 2.5.29.37={text}1.3.6.1.5.5.7.3.1`), adding it to `LocalMachine\Root`, and restarting the Shell gives a FULLY VALIDATED handshake: `https://localhost:5239/health` = 200 Healthy with `CertificatePolicy = $null`, i.e. no trust-all bypass. New thumbprint DF556BEF…; rollback = remove it from both stores. ⚠ I did not verify the OLD cert's EKU, so "missing EKU" is the likely cause, not a measured one — what IS measured is that the new cert works and the old one did not. Local prod-parity HTTPS validation on DEV is therefore possible; the "validate on 234 only" workaround no longer applies. · SOURCE: rehearsal R7e 2026-08-30 · status: active

- 2026-08-30 · **A probe's trigger must be read from the code, not assumed from its description.** Probe PR234-1a was carried for weeks as "open a production ConfigJson screen, then grep the log" — by predecessor, coordinator and me. The markers are actually in `SaveWidgetConfig` (`ScreenEditorPage.razor:4665`) and `SaveLayout` (`:5029`), logged BEFORE persisting, on `/screens/{Id:guid}/edit`. Opening `/screens/{id}` (fullscreen view) fires nothing, so on 234 the check would have reported "probe never fires" on a healthy build — a false negative on a live server, at the worst moment. Before relying on any diagnostic marker, open its source and read WHEN it is emitted. · SOURCE: rehearsal R7d/R7h 2026-08-30 · status: active

- 2026-08-30 · **A bare `Read-Host` for a password is a disclosure, not a UX choice.** I used `-AsSecureString` in every box that day except one, and that one put server 234's postgres superuser password into the chat in clear text — echoed to screen, written to PSReadLine history, then pasted back with the output. Consequences: rotate the credential, purge `(Get-PSReadlineOption).HistorySavePath`, close the session. RULE: masked input in EVERY box on EVERY machine; a "quick diagnostic" is exactly where the slip happens, because that is where the boilerplate gets trimmed. · SOURCE: operator 2026-08-30, Phase P step 3 · status: active

- 2026-08-30 · **"Prod-identical environment" includes the DATABASE ENGINE VERSION — measure it on both sides before the rehearsal, not during the production run.** I validated the whole converge rehearsal on PostgreSQL 18 and discovered at Phase P that server 234 runs **15.5** (client there is 18.4, which is why a pg_dump-18 dump hit `unrecognized configuration parameter "transaction_timeout"`). Concretely dangerous, not academic: `db/schema.sql:18` carries `SET transaction_timeout = 0;` (a PG17+ GUC, written by pg_dump 18), so applying it on 15.5 under `ON_ERROR_STOP=1` aborts the schema step AFTER the 26-table drop. Content was fine (zero MERGE / JSON_TABLE / ANY_VALUE / NULLS NOT DISTINCT; IDENTITY exists since PG10) — the incompatibility lived in the dump PREAMBLE. Also: `CLAUDE.md:370` claims "PostgreSQL 18 (production)" while the server says 15.5 — the document lied, and nobody had checked. · SOURCE: Phase P gate 2026-08-30 · status: active

- 2026-08-30 · A point-in-time `pg_stat_activity` snapshot showing ZERO connections does NOT mean "nothing uses this database" — a pool at idle closes its connections. My first "who else talks to rtmviewdb" reading showed no clients while three services were running; the honest answer needed `pg_stat_database` counters (xact_commit 6562, tup_returned 19.7M = the DB is alive) plus a re-measurement UNDER LOAD, which then showed `ccdashboard_user` connecting. Before any cut-over that strands other consumers, measure connections under load and report the reading as "at the time of measurement", never as a standing truth. · SOURCE: Phase P pre-flight 2026-08-30 · status: active

## §C VERIFY  (run at init — OBJECT STORE ONLY, NORM-CUR-13; mismatch -> superseded, don't act)
> Rewritten 2026-08-29 (curator acceptance): was `Select-String` over the mount = untrusted surface.
> Every item states its EXPECTED count BEFORE the run; a differing count is a failure, not a judgement call.
> Every item must be able to return 0 — run item 0 first to prove the predicate can fail.

0. **Negative control** (proves the predicate works):
   `git show v3:deploy/Update-RTMView.ps1 | grep -c 'ThisMarkerMustNotExist'` — MUST be `0`.
   Non-zero, or an error instead of `0`, means the harness is broken: fix it before trusting items 1-4.
1. `git show v3:deploy/Update-RTMView.ps1 | grep -c '@(Get-ChildItem'` — expect **1** (StrictMode @() wrap)
2. `git show v3:deploy/Update-RTMView.ps1 | grep -c 'appsettings\.json'` — expect **2** (operator-config preserve)
3. `git show v3:deploy/Update-RTMView.ps1 | grep -c 'pg_dump'` — expect **4** (pre-apply DB backup)
4. `git show v3:deploy/Update-RTMView.ps1 | grep -c 'Compare-ToBaseline'` — expect **4** (E1 drift gate)
5. **Store vs mount** (the reason the rule exists):
   `git rev-parse v3:deploy/Update-RTMView.ps1` == `git hash-object deploy/Update-RTMView.ps1`.
   Differ -> the working tree is NOT what ships; verify against the store and say so.
6. **Environment identity is a NUMBER, not an adjective.** Any validation artifact (rehearsal, prod-parity run,
   "identical environment" claim) must carry the DB engine version of BOTH sides, as the exact `SELECT version()`
   string. A gate cannot ask about what the report omits: the 2026-08-30 PG18-vs-15.5 miss was possible because my
   own rehearsal artifact had no version line, so the coordinator could not demand one.
7. **Gate REACHABILITY, not just presence** (item 4 alone is a false green):
   `git show v3:deploy/Update-RTMView.ps1 | grep -c 'skipping drift gate'` — expect **1**, and treat it as a
   STANDING DEFECT: a missing `db\tools\Compare-ToBaseline.ps1` makes the §A.1 mandatory gate WARN-skip
   instead of stopping the deploy. Before ANY deploy, confirm the gate actually resolves in the PACKAGE
   being deployed — presence of the string in the script is not evidence that the gate ran.

## §D REFERENCE
Scripts: deploy/Update-RTMView.ps1, deploy/Install-RTMView.ps1, tools/Build-ProdRelease.ps1.
Ops layout: CLAUDE.md §43 (external-server ops), §35 (prod release encoding).
