body.push(H1("1. What an Info Slot is"));
body.push(P("An Info Slot is a named channel for on-screen messages. Management staff write short messages into it, and those messages appear live — scrolling or one at a time — inside an Info Slot widget on contact-centre dashboards. Unlike the data widgets, an Info Slot carries text written by people; the messages are stored in RTM View Shell’s own database and pushed to dashboards in real time."));
body.push(P("As an administrator you create and configure the Info Slots and decide which permission groups may write to each one. Writing the messages is covered in the Info Slot Message Management Guide (B-03)."));
body.push(P([{t:"Where: ",b:true},"Info Slot Management is at /admin/info-slots, available to Administrator and Superadmin."]));
body.push(H1("2. The Info Slot list"));
body.push(P("The page lists your organisation’s Info Slots, showing each one’s name, display mode, active-message count, how many permission groups can write to it, and whether it is active. Use “+ New Info Slot” to create one, or Edit / Delete on a row. A Superadmin additionally sees a tenant selector — choosing “All Tenants” shows Info Slots across every tenant and records a cross-tenant access audit event."));
body.push(H1("3. Creating or editing an Info Slot"));
body.push(H2("General"));
body.push(...bullets([[{t:"Name",b:true}," (required, unique) and an optional Description."],[{t:"Display Mode",b:true}," — Ticker (scroll continuously) or Sequential (one at a time, auto-advancing)."],[{t:"Seconds Per Message",b:true}," — how long each message shows in Sequential mode (min 3, default 10); appears only when Sequential."],[{t:"Is Active",b:true}," — a toggle to enable or disable the slot."]]));
body.push(H2("Access — permission-group assignment"));
body.push(P("A dual-pane selector moves permission groups between Available and Assigned. The assigned groups are exactly the groups whose members may write messages. A slot with no assigned groups can be written to only by Administrators and the Superadmin."));
body.push(H1("4. Deleting an Info Slot"));
body.push(P("Deleting permanently removes the slot with its message history and group assignments. Because that would break any dashboard showing it, the system checks first: if the slot is placed on one or more dashboards, the delete is blocked and lists those dashboards so you can remove the widget there first. Confirming removes it; the action is recorded as InfoSlot.Deleted."));
body.push(callout("note","Same rule applies to deactivation",["Turning an active Info Slot off is blocked while it is placed on a dashboard — the warning lists the dashboards. Remove the placements first."]));
body.push(H1("5. Who may write — the Permission Groups Info Slots tab"));
body.push(P("Write access is controlled in two equivalent places: the Access tab above, and a dedicated Info Slots tab in the Permission Groups editor (A-03). Either way, the result is the same assignment. Administrators and the Superadmin can always write, regardless of group."));
body.push(H1("6. How it works behind the scenes"));
body.push(P("Each Info Slot has its own real-time channel (a dedicated SignalR hub) separate from the dashboard data feeds, so messages appear and disappear on screens the instant they are added, edited or expire. A background service removes messages automatically when their expiry passes."));
body.push(H1("7. Worked example"));
body.push(...nums(["Open /admin/info-slots → “+ New Info Slot”.","General tab: Name “Shift Announcements”, Display Mode Ticker, Is Active on.","Access tab: assign the “Supervisors” permission group.","Save. Editors place the Info Slot widget on the relevant dashboards.","Supervisors post messages from /info-slots (B-03); they appear scrolling in real time."]));
body.push(new Paragraph({ children:[new PageBreak()] }));
body.push(H1("Appendix A — Info Slot fields"));
body.push(tbl(["Field","Notes"],[["Name","Required; unique per tenant"],["Description","Optional"],["Display Mode","Ticker (scrolling) or Sequential (one at a time)"],["Seconds Per Message","Sequential only; min 3, default 10"],["Is Active","Enable/disable; can’t disable while placed on a dashboard"],["Assigned permission groups","Groups whose members may write messages"]],[3100,6260],{zebra:true}));
body.push(H1("Appendix B — Access & audit summary"));
body.push(P("Manage Info Slots: Administrator + Superadmin (/admin/info-slots). Write messages: members of assigned permission groups + Administrator/Superadmin (/info-slots, B-03). Superadmin “All Tenants” view writes Tenant.CrossTenantAccess. Deletion writes InfoSlot.Deleted. Deletion/deactivation is blocked while the slot is placed on a dashboard."));
