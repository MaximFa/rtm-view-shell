# Brand assets — logos for documents

Drop logo files here. Used by the BI/Reporting deliverables (docx + HTML/PDF design version)
for cover pages and page headers.

## What to put here (ideal set)
- **Primary logo** — full lockup (mark + wordmark), transparent background.
- **Mark / icon** — square symbol only (for small header corners / favicons).
- **Mono / reversed** — a white (or light) version for use on the dark navy cover & headers.

## Formats & specs
- **PNG** with transparent background — REQUIRED (works in both Word and HTML/PDF).
  - High-res for print: primary logo ≥ 1200 px wide; mark ≥ 400×400 px.
- **SVG** also welcome (sharpest in HTML/PDF; Word also accepts SVG).
- Avoid JPG for logos (no transparency).

## Suggested filenames (so I can wire them in automatically)
- `logo-primary.png`      (or .svg)   — color, on light background
- `logo-reversed.png`     (or .svg)   — white/light, for dark backgrounds
- `logo-mark.png`         (or .svg)   — symbol only
- (optional) `logo-primary-mono.png`  — single-colour version

Once files are here, tell me and I'll place them on the cover + page headers of both the
docx and the HTML/PDF editions. If you also have brand colours/fonts, drop them and I'll align.
