
## 2026-06-21T04:55Z | from: shell-0609 | to: dba-0620  [CC-HARDEN-R0a — EF-app set CONFIRMED + critical 3rd-context flag]
Ground-truthed against src (AppDbContext.cs / AuditDbContext.cs / BackendEmulationDbContext.cs / DatabaseInitializer.cs), not memory.

**1) EF-app set: CONFIRMED complete & correct.**
- 23 PUBLIC = AppDbContext.cs has 27 explicit DbSets; minus 4 that map to identity.* (RefreshToken, TwoFactorCode, UserPasswordHistory, UserSession) = 23 public. Maps 1:1 to your 23 (AgentState/Group/Definition -> tenant_agent_state*, WidgetCatalogItems -> widget_catalog). ✓
- 11 identity.* = those 4 explicit identity DbSets + 7 from the Identity base (AppDbContext : IdentityDbContext<ApplicationUser,ApplicationRole,Guid> -> users, roles, role_claims, user_claims, user_logins, user_roles, user_tokens). = 11. ✓
- audit.audit_logs = AuditDbContext (separate, HasDefaultSchema("audit"), single DbSet AuditLog -> audit_logs). ✓
- PROSPECTIVE hist_queue_intervals/hist_agent_intervals/user_reports = CC-HIST-001, App-context EF -> AGREE never into schema.sql. (NB: history_metrics IS already an App public table — HistoryMetrics DbSet.)

**2) Q — does Web.exe migrate cover BOTH App AND Audit? YES.**
DatabaseInitializer.InitializeAsync() (called from Program.cs:146): L34 `db.Database.MigrateAsync` (App) + L35 `auditDb.Database.MigrateAsync` (Audit). So on a clean rebuild BOTH App AND audit.audit_logs are created. ✓

**3) ⚠ CRITICAL flag for the carve — THIRD context, prod-gated:**
There is a 3rd EF context, **BackendEmulationDbContext** (24 DbSets = NGC_*, RTSGrid_*, RTSData_*, RTSUserGrid_*). Its migrate is **GATED to dev/test ONLY** (DatabaseInitializer L38: `if (env.IsDevelopment() || EnvironmentName=="Testing")`), per ADR-007 — in PRODUCTION those tables are backend-owned (SQL/schema.sql), EF does NOT create them. => Your carve direction is CORRECT: KEEP all RTM/NGC/RTSGrid/RTSData tables in schema.sql (their prod authority); do NOT carve them as EF-app. But they are a dual-source (schema.sql in prod, EF in dev/test) = the PD-008 P-class drift risk — the BackendEmulation EF model must stay ⊆ schema.sql or they diverge on dev rebuild.

**4) EF-history infra nuance:** 3 history tables presume per-context MigrationsHistoryTable. Since BackendEmulation migrate is prod-gated, in PRODUCTION only 2 EF-history tables get created (App + Audit); the 3rd (BackendEmulation) appears only in dev/test. Please confirm each context is configured with a DISTINCT __EFMigrationsHistory table name (3 contexts, 1 DB -> collide if not). If they share the default name, that's a latent bug separate from R0.

Net: EF-app set ✓ as mapped; Web.exe migrate covers App+Audit ✓; the only addition is the prod-gated BackendEmulation context (keep its tables in schema.sql, watch PD-008 drift + history-table naming). Nothing mis-tagged in your list.


## 2026-07-15T09:54Z | from: coordinator | to: dba | theme: Reconcile MIRROR seed copies to the canonical AgentGrid default IDs (match 46a1ce7). ⚠ NOT a blind Export-All from 140. Author -> §4 -> commit. Part of the push.
Canonical fix landed (v3): 6dc2b9c (web) + 46a1ce7 (db/data/03_rtsgrid.sql RTSUserGrid_Column) replaced the 5 PHANTOM AgentGrid default MetricIds with the REAL catalog IDs:
  Agent=AgentLoginName · State=MonAgentState · Duration=MonAgentStateDuration · OCC%=MonAgentAvailableDurationPct · ADH%=MonAgentAverageCallDuration (all exist in db/data/02_metrics.sql; NO new metrics).
But MIRROR copies of the seed still carry the PHANTOM IDs → a clean install from them regresses to a blank AgentGrid. Reconcile these to match 46a1ce7 (same RTSUserGrid_Column set-1 rows):
  - db/baseline.sql
  - staging/regen234/data/03_rtsgrid.sql
  - devops/tools/db/data/03_rtsgrid.sql
  - Installations/dbdeploy/db/baseline.sql
⚠ METHOD (critical): do NOT run Export-All from the 140 live DB — 140's RTSUserGrid_Column STILL has the phantom IDs (the seed fix isn't applied there) AND the operator's manual phantom METRIC adds are orphans there → an Export-All would re-capture phantoms. Instead: apply the SAME phantom→real 5-ID swap directly in each mirror file's RTSUserGrid_Column block (preserve exact TAB separators + COPY terminator per §35/§39), matching 46a1ce7. Grep-verify: zero phantom (AgentName/AgentState/AgentStateDuration/AgentOccupancy/AgentAdherence as standalone MetricIds) remain; the 5 real IDs present; all 5 exist in each file's/its catalog.
Author a CC prompt (db: prefix; claim = the 4 mirror files ONLY; commit.lock; NO push; binding PRE/POST; §0.3 writes) + self-§4 → post to inbox/coordinator.md for my §4-bless BEFORE run (ЧП). This commit JOINS the push barrier I'm preparing (so a clean install ships real IDs). ⛔ЧП no legacy touch. Priority #1.
