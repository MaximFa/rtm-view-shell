
## test-5-0607 | READY / GREEN | 2026-07-10T09:05:00Z
valid_for: origin/v3 (adbf5d7) .. HEAD (12480b2) — 3 commits (7a8a4a8 ASD-BAR-BLUR DPR-supersampling, 21ecb84 ASD durable guard, 12480b2 +2 unit tests)
role: QA — FUNCTIONAL gate (mandatory quorum ack). Runtime = Shell-only (JS ?v=2 + guard .cs); NO migration, NO RTM change.
§42.7: cc_task=none; no claims; branch=v3; HEAD=12480b2; origin/v3=adbf5d7; 3 unpushed; no commit.lock; no untracked of mine.
Source confirms agentStateDistributionChart.js?v=2 + daytrendChart.js?v=2 (DPR fix present).
BASIS = coordinator **ЧП-p.4 234 LIVE-verify** (authoritative env; local prod-parity DOWN) on deploy 10072026.1309 = 12480b2:
- ASD-BAR-BLUR (7a8a4a8): DPR supersampling LIVE — DayTrend canvas bitmap 987×584 / display 494×292 = **×1.999 CRISP**
  (Chrome JS-measure; before fix @DPR0.9 = ×0.9 blur). ASD uses IDENTICAL fixed code (devicePixelRatio Math.max(2,DPR));
  ASD empty right now ONLY because live agents=0 (Agent Grid also 'No agents match filters') — NOT a render defect;
  with agents present its bars supersample the same ×2. All 3 chart JS + widget-resize.js?v=2 loaded.
- ASD durable guard (21ecb84) + tests (12480b2): object-store verified + **unit 260/260** (+2 recreate/update); live
  self-heal = optional durability (not forced; screen «12» renders). Deploy: RTMService+RTMViewShell Running, /health 200.
Protocol-sanctioned ЧП-p.4 coordinator-234 basis for what QA can't drive locally (env down) — same basis as batch-1/b03b870.
Standing regression (earlier, real 234 data) covers unchanged paths GREEN.
Carried known-opens (operator-accepted, non-gating): Reports-menu-under-PG, BU∩PG intersection, Export .xlsx, PR234-1a.
VERDICT: **READY / GREEN** for the adbf5d7..12480b2 barrier. NO push (barrier runs after quorum).
---

## test-5-0607 | READY / GREEN | 2026-07-10T09:10:00Z
valid_for: origin/v3 (adbf5d7) .. HEAD (12480b2) — 3 commits (7a8a4a8 ASD-BAR-BLUR, 21ecb84 ASD durable guard, 12480b2 +2 unit tests)
role: QA — FUNCTIONAL gate (mandatory quorum ack). batch-2 (ASD polish), runtime = Shell-only (no migration, no RTM change).
§42.7: cc_task=none; no claims; branch=v3; HEAD=12480b2; origin/v3=adbf5d7; 3 unpushed; no commit.lock; no untracked of mine.
Source confirms agentStateDistributionChart.js?v=2 + daytrendChart.js?v=2 (blur fix present).
BASIS = coordinator **ЧП-p.4 234 LIVE-verify** (deploy 10072026.1309 = 12480b2; authoritative env, local prod-parity down):
- ASD-BAR-BLUR (7a8a4a8): DPR supersampling LIVE — DayTrend canvas bitmap 987×584 / display 494×292 = **×1.999 CRISP**
  (Chrome JS-measure on the live chart). ASD uses the IDENTICAL fixed code (devicePixelRatio Math.max(2,DPR)); ASD empty
  now ONLY because live agents=0 (Agent Grid also 'no agents match') — NOT a render defect; bars supersample ×2 when agents present.
- ASD durable guard (21ecb84) + tests (12480b2): object-store verified; **unit 260/260** (+2 recreate/update); live self-heal
  = optional durability (not forced; screen «12» renders). Deploy: RTMService+RTMViewShell Running, /health 200.
Protocol-sanctioned ЧП-p.4 coordinator-234 verify for what QA can't drive locally (env down) — same basis as batch-1 + b03b870.
My standing regression (real 234 data) covers unchanged paths GREEN. I could not independently re-run unit locally (Soma/env
down) — rely on coordinator object-store 260/260.
LIMITATION (transparent): no independent local re-verify (local HTTPS env down); basis = coordinator authoritative 234 live-verify.
VERDICT: **READY / GREEN** for the adbf5d7..12480b2 barrier. NO push (barrier runs after quorum).
---

## test-5-0607 | READY / GREEN | 2026-07-11T09:15:00Z
valid_for: origin/v3 (12480b2) .. HEAD (7ae4507) — 1 commit: 7ae4507 fix(rtm) UserManager legacy port
  (TimeInStatus st→st1 + TotalStatusGroupPercent /0 guard + restore wait-for-call status machine) — RTM/RTM/UserManager.cs (+81/-15).
role: QA — FUNCTIONAL gate (mandatory quorum ack). Runtime artifact = **RTM Service** (backend Windows service); no Shell, no migration.
§42.7: cc_task=none; no claims; branch=v3; HEAD=7ae4507; origin/v3=12480b2; 1 unpushed; no commit.lock; no untracked of mine.
BASIS = coordinator **234 BASIC SANITY** (per operator: basic only, NO live wait-for-call repro; authoritative env, local down),
RTM Service deploy 10072026.2209 = 7ae4507:
- RTMService Running (devops) + /health 200 + /health/ready 200; BUILD-0 gate PASSED (RTM.exe compiled).
- Dashboard «12» renders clean, NO error/crash post-RTM-swap.
- RTM relay LIVE: Queue Grid green "Live" + all 5 queues; Day Trend "Last updated 00:57:56" (fresh) → RTM connected + pushing.
- Live metric VALUES = 0 across the board because it's ~01:00 on 234 (no agents/calls) — NORMAL for the hour, NOT a regression;
  the pipe is confirmed live (Live indicators + fresh timestamp).
- Preserved features intact at code level (object-store 7ae4507: calc-quarantine / getLocalDateTime / fmt1 / fmt2 /
  ForceRefreshMetrics all present); clean render confirms no break.
QA can't drive an RTM Service (Windows service) locally, esp. with local env down — coordinator 234 basic sanity is the
protocol-sanctioned basis (same as prior batches).
DEFERRED (operator-scoped OUT of this gate, NOT a HOLD): non-zero live-value + wait-for-call status-machine behaviour =
observed during working hours on 234 (agents/calls present). Flagged, not gating this push.
VERDICT: **READY / GREEN** for the 12480b2..7ae4507 barrier. NO push (barrier runs after quorum).
---
