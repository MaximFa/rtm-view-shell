
## test-5-0607 | READY / GREEN | 2026-07-11T10:00:00Z
valid_for: origin/v3 (7ae4507) .. HEAD (116416c) — 1 commit: 116416c fix(rtm) configurable NamedPipe name
  (AppConfig.PipeName, default rtmpipe) — RTM/RTM.Configuration/AppConfig.cs + RTMAdapter.cs + appsettings.json (+10/-4).
role: QA — FUNCTIONAL gate (mandatory quorum ack). Runtime = RTM Service only; ZERO SQL/DB/migration; backward-compat.
§42.7: cc_task=none; no claims; branch=v3; HEAD=116416c; origin/v3=7ae4507; 1 unpushed; no commit.lock; no untracked of mine.
BASIS (two-channel):
- OBJECT-STORE backward-compat CONFIRMED: AppConfig.cs:63 — PipeName = IsNullOrWhiteSpace(config["RTM:PipeName"]) ? "rtmpipe"
  : config[...]; i.e. when RTM:PipeName is unset the pipe defaults to the LEGACY "rtmpipe" → existing deploys unchanged.
- Coordinator/devops 234 BASIC SANITY: deployed 234 (11072026.1028, Update-RTMView -SkipShell -SkipDrift), RTMService Running
  + /health 200; build-0 gate PASSED.
QA can't drive an RTM Service (Windows service) locally (local env down) — coordinator 234 basic sanity + object-store
backward-compat is the protocol-sanctioned basis. Low blast radius (config-name plumbing, default preserves legacy).
VERDICT: **READY / GREEN** for the 7ae4507..116416c (Part B) barrier. NO push (barrier runs after 4/4 quorum).
---

## test-5-0607 | HOLD: load-bearing regression NOT runnable — local env DOWN | 2026-07-15T10:40:00Z
valid_for: origin/v3 (116416c) .. HEAD (46a1ce7) — 23 commits (AgentGrid Defect-A + Shell-UI pagination/search/filters/headers + install stack)
role: QA — LOAD-BEARING FUNCTIONAL gate (mandatory quorum ack).
§42.7: cc_task=none; no claims; branch=v3; HEAD=46a1ce7; origin/v3=116416c; 23 unpushed; no commit.lock; no untracked of mine. Git-side clean.

WHY HOLD (not GREEN): the coordinator's requirement for THIS barrier is an EXPLICIT load-bearing FUNCTIONAL regression
(⛔ЧП, NOT smoke, NOT a 234-basis ack): build v3 HEAD 46a1ce7 + run testing/regression_checklist.md over UI+DB, MUST verify —
- AgentGrid renders agents WITH data via the Restore-All/db/data seed path (Defect-A fix in db/data/03_rtsgrid.sql);
- Shell-UI cluster: pagination / search / filters;
- fresh-DB migrate clean (0364a71 migration).
These are the exact NEW paths (23 commits) — they cannot be signed off from object-store or a prior GREEN. QA must RUN it.

BLOCKER: the LOCAL prod-parity env is DOWN — Soma :5199 = connection refused (confirmed just now, error page), so Shell/DB/
Soma /ops/build + /db + UI are all unreachable. Per the no-fix-infra directive I did not bring it up. I cannot build 46a1ce7,
reseed the DB (Restore-All db/data), run migrate, or drive the UI. => the load-bearing regression is not runnable right now.

TO FLIP HOLD -> GREEN, I need the local env up so I can run it:
1. Soma up (:5199) + Shell built at 46a1ce7 + running (or I build via Soma /ops/build once Soma is up);
2. DB reseeded via Restore-All(db/data) (the AgentGrid Defect-A data path) + a fresh-DB migrate run (0364a71);
3. operator login → then I run: AgentGrid renders agents WITH data · pagination/search/filters · fresh-DB migrate clean
   · standing regression no-regression (dashboards/reports/bg). -> consolidated GREEN.
Alternatively, if the operator scopes this to a coordinator ЧП-p.4 234 LIVE-verify basis (as with the recent RTM commits),
say so and I'll ack on that basis; but as written this barrier asks QA to RUN the regression, which needs the env.
KNOWN pre-existing gap noted (NOT a HOLD reason, backlogged): db/baseline.sql lacks RTSUserGrid_Column defaults →
Create-FreshDb = blank AgentGrid; validate via Restore-All(db/data) instead. Understood — will not HOLD on it.
Not fixing infra. NO push.
---
## test-5-0607 | HOLD (update): env UP but DB not reseeded — regression still blocked | 2026-07-15T12:20:00Z
Soma came up (operator). BUILD=0 (46a1ce7) + unit 263/263 GREEN. BUT the DB is not in the required state:
RTSGrid_Metric MISSING (42P01, /db/query) → Archiver+HistoricalAggregation crash the Shell host (StopHost);
migration 0364a71 NOT applied (history latest = AddReportEntities). Load-bearing UI regression NOT runnable until the DB is
reseeded via Restore-All(db/data) + migrated (0364a71) — operator/devops action; I do not run Restore-All. RTSData_UserStatus
has 138 agents (data is present) → once the metric/grid layer is restored, AgentGrid should render. HOLD. No push.
---

## test-5-0607 | READY / GREEN (flips prior HOLD — Operator scope C) | 2026-07-16T03:56:00Z
valid_for: origin/v3 (116416c) .. HEAD (46a1ce7) — 23 commits (AgentGrid Defect-A + Shell-UI pagination/search/filters/headers + install stack)
role: QA — LOAD-BEARING FUNCTIONAL gate (mandatory quorum ack).
§42.7: cc_task=none; no claims; branch=v3; HEAD=46a1ce7; origin/v3=116416c; 23 unpushed; no commit.lock; no untracked of mine. Git-clean.
Operator ruled option C (do NOT force local-env standup — blocked on BACKLOGGED install-path defects that are NOT the
23-commit code). My functional basis:
1. **BUILD = 0** — Soma /ops/build @ v3 HEAD 46a1ce7, exit 0 (only benign EF1002 warns in a test proj). [verified by me]
2. **Unit = 263/263, failed 0** — Soma /ops/test?suite=unit, exit 0. [verified by me]
3. **140 PROD-IDENTICAL live-verify** (coordinator + operator authed-visual, this session, prod-identical env — the
   sanctioned ЧП-p.4 basis when local env can't run): AgentGrid "DE All" + "US All" RENDER agents WITH data (Defect-A fixed);
   Shell-UI cluster (pagination / search / filters) operator-verified on the running Shell. Genuine functional coverage, NOT smoke.
TRANSPARENCY: I could NOT run the local UI regression myself — the local DB was mis-provisioned (RTSGrid_Metric MISSING /
42P01 → Archiver+Aggregation crash the host; migration 0364a71 unapplied). Operator confirmed these = the BACKLOGGED
install-changeset defects (cert placeholder, BackendEmulation=schema.sql, Restore-All bugs) — NOT the 23-commit code, and
separately tracked. So the functional coverage for these 23 commits comes from the 140 prod-identical live-verify above.
KNOWN-OPEN carried (operator-accepted, non-gating): install-path DB provisioning (RTSGrid_Metric/0364a71/Restore-All) —
backlogged; Reports-menu-under-PG, BU∩PG intersection, Export .xlsx, PR234-1a — prior deferred.
VERDICT: **READY / GREEN** for the 116416c..46a1ce7 barrier, on the Operator-scoped-C basis. NO push (barrier after quorum).
---

## test-5-0607 | READY / GREEN | 2026-07-21T06:35:00Z
valid_for: origin/v3 (7c8b9d0) .. HEAD (cd0e39a) — 10 commits (Max Wait C1 89feb34/16c6011 · TZ chain 0b07651/0329bf0/e58cac8/01dbc2c · on-demand grid 961a979 · danger-zone-UI 2293f6b · diag+revert ad73870/cd0e39a net-clean)
role: QA — LOAD-BEARING FUNCTIONAL gate (mandatory quorum ack).
§42.7: cc_task=none; no claims; branch=v3; HEAD=cd0e39a; origin/v3=7c8b9d0; 10 unpushed; no commit.lock; no untracked of mine. Git-clean.
FUNCTIONAL BASIS (consolidated regression — NOT a diff-read):
1. **No regression in Dashboards/Reports — QA-verified INDEPENDENTLY on nayax** (https://nayax.insightense.com:8444,
   app.css?v=32, this session): Search=GREEN (Enter-filter), Status-filter=GREEN; DE General Manager Dashboard — all 4
   widgets render clean, **0 console errors** (Queue Grid "Live"+5 queues incl the **Max Wait** column; Agent Grid columns
   render; Agent State Distribution renders; Day Trend); Reports List + View chrome OK. Off-hours → live VALUES 0 (0 agents/
   calls) = normal, not a defect.
2. **Max Wait C1 holds** (89feb34 F5-proof anchor=now-enqueue + 16c6011 server-local→UTC): deployed on 140 and HOLDS —
   coordinator + operator authed-visual (prod-identical env; local env parked per scope-C). I cite this as the sanctioned
   ЧП-p.4 140 basis for the timing behaviour (needs live calls; nayax off-hours shows 00:00, column structurally present).
3. **db TZ chain** (0b07651/0329bf0/e58cac8/01dbc2c — interaction load filter by server-local UpdateTime=today, legacy-
   equivalent) + **961a979 on-demand grid** (runtime QueueGrid renders w/o RTM restart): backend/db — 140-verified
   (US load == legacy on 140); dba/backend own the code review.
4. **2293f6b danger-zone** (Tenant Danger Zone renders only on the General tab): Shell UI-only, shell-owned; low blast
   radius. (Can spot-verify read-only on nayax on request; not independently re-checked here per operator "enough".)
TRANSPARENCY carried: during the nayax session the "US General Manager" dashboard UpdatedBy moved 15→16 Jul after my tab
briefly entered its editor (no intentional save/content change) — flagged.
VERDICT: **READY / GREEN** for the 7c8b9d0..cd0e39a barrier. NO push (barrier runs after full quorum).
---

> barrier CLOSED 2026-07-21T08:50Z (PUSHED cd0e39a) — consumed

## test-5-0607 | READY / GREEN | 2026-07-22T13:55:00Z
valid_for: origin/v3 (cd0e39a) .. HEAD (d1982de) — 9 commits (WFM Phase 1: Erlang 4e21796 · indexes 83ce56b · B0 07b48a1 ·
  B1 515c355 · 3c-config e84e654 · 3c-widget 30225d6 · N-fix 6e3c922 · BU-agg fb9a6a4 · BU-key d1982de)
role: QA — MANDATORY FUNCTIONAL gate.
§42.7: cc_task=none; no claims; branch=v3; HEAD=d1982de; origin/v3=cd0e39a; 9 unpushed; no commit.lock; no untracked of mine. Git-clean.
FUNCTIONAL BASIS (NOT a diff-read):
1. ⭐ **C2 LIVE GATE PASSED on 140 with LIVE NON-ZERO data** (DE General Manager) — the load-bearing evidence:
   State OK · λ 40.0/hr · AHT 4:57 · N 14 · Traffic 3.30 Erl · P(wait) 0% · SL 100% · ASA 0s · Required 6 ·
   Occupancy 23.6% · Understaff 0% · Variance +8 · AsOfUtc ticks 30s (hosted loop alive).
   **Arithmetic HAND-VERIFIED by the coordinator**: A = 40×297/3600 = 3.30 == displayed; Occupancy = 3.30/14 = 23.6% ==
   displayed; percents NOT double-scaled. For a NEW MATH subsystem this is exactly the right gate — value-level
   verification on live data, not a render smoke.
2. **The gate proved load-bearing**: 5 defects found+fixed BEFORE this push (N-42703, BU-aggregate, BU-key mismatch,
   +2 diagnostics) — none of them catchable by unit tests. That is the strongest possible argument the functional gate worked.
3. **build0 + Tests.Unit Failed 0 / Passed 283** (devops @30225d6); later WFM fixes CC-gate green. Erlang core (4e21796)
   additionally covered by unit tests **against a verified anchor** — dual coverage (unit math + live arithmetic).
4. **My regression view**: I independently verified the pre-existing paths healthy on nayax this cycle (Dashboards/Reports,
   4 widgets render clean 0 console-errors, search/filters GREEN). WFM is additive (new widget + hosted loop + 3 covering
   indexes); the C2 run on the DE dashboard showed the existing widgets + WFM co-existing with State OK.
ACK WITH KNOWLEDGE (non-blocking, carried):
- 2 residual `WFM key collision` WARNs (detection still name-based though the store key is now the id) — cosmetic log noise, queued.
- Sub-BU "No Data" = customer CONFIG gap (BUs without SuperGroups); the widget correctly showed the nodata sentinel — NOT a code defect.
- I did not independently drive the WFM widget myself (not deployed on nayax; local env parked per scope-C) — basis is the
  coordinator's hand-verified C2 live gate on 140 (sanctioned ЧП-p.4 authoritative env).
VERDICT: **READY / GREEN** for the cd0e39a..d1982de (WFM Phase 1) barrier. NO push (barrier runs after full quorum).
---

> barrier CLOSED 2026-07-22T18:05Z (PUSHED d1982de) — consumed
