---
session: RTM DBA
slug: dba-0620
started: 2026-06-20T09:55:04Z
heartbeat: 2026-06-25T19:38:40Z
status: handoff
role: dba
cowork: A
modules: []               # review-gate role — NO standing module claim
files: []                 # claim at task-time (the specific db/ file)
cc_task: none
---
DBA standing specialist (role #5, Cowork-A). REVIEW-GATE model (like Security): no exclusive standing claims while idle.
Fresh incarnation, supersedes stale dba-0610 (heartbeat 06-19 >3h, HANDOFF'd). Operator-directed, pre-launch.

OWNS/AUTHORS at task-time (claim the specific file then): db/schema.sql, db/tools/**, db/data/** (baseline) + db_patch_history/§38a ledger.
REVIEWS (no claim): all db/migrations/** + db/functions/** for §33.8 routine-kind, §38a self-record, baseline-drift, indexes/grants/GQF (§5/§7).

BOOT done 2026-06-20: §0.2 integrity OK (.git/config null-bytes = mount false-alarm §0.5, git ok on retry; M-files all non-db territory). §40 skills + role-dba §A/§C VERIFY green (Compare-ToBaseline ✓, 64 CREATE TABLE, db/migrations ✓, db_patch_history×3). §A core consistent — no supersession.

PENDING TASK (standby): review bi's CC-HIST-001 output (tools/cc_prompt_hist_001.md) — EF+raw-SQL migration creating hist_queue_intervals / hist_agent_intervals (monthly RANGE partitions, B1=A) + user_reports (non-partitioned) + fn_hist_ensure_partitions / fn_hist_drop_aged + companion db/migrations/20260620_0NN_hist_reports.sql + §38a self-record. Migration NOT YET produced (verified: no hist_* in db/migrations, no hist_queue_intervals refs in src/db).

PRE-NOTED REVIEW CHECKLIST (verified-ready against prompt rev2):
- Partition correctness: PK incl IntervalStart (both hist_*); partition key in EVERY unique key (UNIQUE has IntervalStart) — prompt spec OK, verify in delivered DDL.
- Coverage gap risk: migration creates current+next2 months only; startup lookback 24h can cross a month boundary -> insert into prior-month interval with NO partition -> error. CHECK: ensure_partitions covers lookback window OR a DEFAULT partition exists OR create 1 month back too.
- fn_hist_ensure_partitions idempotency (to_regclass / IF NOT EXISTS per partition); partition naming convention.
- fn_hist_drop_aged: never drops current/future; default keep 84mo (7yr) safe.
- §33.8 routine-kind: fn_hist_* are Shell-called via SELECT (NOT RTM CALL) -> FUNCTION is CORRECT (not PROCEDURE). Verify call site uses SELECT/ExecuteSql, not CALL.
- §38a: companion migration ends with INSERT INTO db_patch_history.
- EF-only discipline: EF migration (migrationBuilder.Sql) DDL == companion db/migrations DDL (consistent).
- No contour DDL: ZERO DDL/DML against RTSData_*/NGC_*/identity in diff (read-only contour).
- Index plan: (TenantId, IntervalStart) + the partition-key unique = adequate for time-bounded reports.
- audit.audit_logs CONFIRMED plain table (PK on Id only, no partition) -> hist partitioning is genuinely net-new, no existing machinery to mirror.

Verdict destination: inbox/coordinator.md + .coord/cc/dba.md.

R0a (2026-06-20): CC-HARDEN-R0 OBJECT->AUTHORITY MAP produced (.coord/r0_object_authority_map.md), 38 carve/26 keep=64, ground-truthed via App snapshot. Routed to shell/backend/coordinator §4. R0b carve HELD (push-freeze + confirm + §4). No db/schema.sql claim until R0b dispatch.

Inbox 2026-06-21T05:47:19Z: 6 blocks handled. R0b carve owner-exec review GREEN — FK-safety verified (35 FK: 5 RTM-internal kept, 30 app carved, 0 cross-authority); audit-context Q resolved (Web.exe migrate covers App+Audit). R0b READY -> native CC (db/schema.sql write = not Cowork per §0.7). CC-HIST-001 migration NOT yet landed -> hist-review standby.

Inbox 2026-06-21T06:08:14Z: R0b EXECUTE relayed to operator (native CC). Awaiting CC RESULT -> consume binding + rebuild proof (Compare B:0). hist-review standby.

Results 2026-06-21T07:11:19Z: R0b (84f087c) STATIC object-store verify GREEN (26 keep, 38 carved, FK-safe, fn-fix, whitelist, runbook); functional rebuild = operator-Windows + now blocked by hist defect. CC-HIST-001 (b1bdd54) review = CHANGES REQUESTED: 1 BLOCKER (EF mig INSERT db_patch_history -> 42P01 on fresh rebuild, step2 migrate before step3 schema.sql creates it; recommend REMOVE INSERT) + 2 minor; partition design GREEN (coverage flag addressed). Routed to bi+coordinator; R0b binding consumed.

2026-06-21T08:02:15Z: 11622f0 re-verified GREEN. Hold-filter not-a-defect (no StatusName col; StatusId correct; literal=234 smoke). Rebuild-tooling GAP (baseline.sql absent->Create-FreshDb broken; Restore-All no migrate). Exact combined proof command (Restore-All + Web.exe migrate + Compare B:0) handed to operator+coord. Proof gates barrier #3; runs on Windows.

2026-06-21T08:09:44Z: REBUILD PROOF FAILED (dev). R0b INCOMPLETE -> NOT Delivered, barrier #3 HELD. Causes: (1)db/data seeds carved app tables [mine], (2)CREATE SCHEMA public no IF NOT EXISTS [mine], (3)RTSData CustomCallData19/20 missing in schema.sql [co-backend], (4)Restore-All BOM + psql WIN1252 + no-migrate [tooling]. Proposed R0c follow-up CC. Static verify still valid; functional rebuild is the gate.

2026-06-21T09:25:00Z: R0c authored (tools/cc_prompt_r0c_rebuild_complete.md). backend canon-confirm (c)=20 text nullable consumed + pinned; HELD removed -> R0c fully unblocked. Submitted to coordinator for §4. On §4 PASS -> native-CC exec -> re-run proof B:0 -> Delivered -> barrier #3 opens. Barrier HELD.

2026-06-21T09:40:00Z: R0c §4-PASS, EXECUTE relayed to operator (native CC). Awaiting CC RESULT -> consume binding + re-run Rebuild-Proof.ps1 to B:0 -> R0b+CC-HIST-001 Delivered -> barrier #3 opens. Barrier HELD.

2026-06-21T09:55:00Z: R0c (b8631bd) STATIC object-store verify = GREEN (all 4 parts). Rebuild-Proof.ps1 reviewed sound. Combined proof command handed to operator (dev, Windows). On Compare B:0 -> R0b+CC-HIST-001+R0c Delivered -> barrier #3 opens. Barrier HELD; R0c not Delivered until B:0.

2026-06-21T10:10:00Z: R0c proof BLOCKED — Rebuild-Proof.ps1 committed WITHOUT UTF-8 BOM (only tool lacking it) + 13 non-ASCII lines -> PS 5.1 WIN1252 misread -> parse desync (§35 rule). Static R0c parts still GREEN. Authored tools/cc_prompt_r0c_fix_bom.md (re-encode UTF-8 BOM). Gave operator immediate local BOM one-liner to unblock proof. NOT Delivered until B:0; barrier HELD. LESSON: new PS1 with non-ASCII MUST be UTF-8 BOM; my grep-structure review of the tool missed the encoding — verify BOM on new PS1.

2026-06-21T10:30:00Z: Rebuild-Proof.ps1 2nd PS5.1 bug after BOM = 3-arg Join-Path (49/50). Expanded fix prompt (BOM+Join-Path). Gave operator local patch to unblock. LESSON reinforced: review new PS1 ON PS5.1 (BOM + 2-arg Join-Path + no ternary/??), structure-grep insufficient. Barrier HELD until B:0.

2026-06-21T10:45:00Z: Rebuild-Proof.ps1 (after BOM+Join-Path local patch) runs clean through steps 1-2; step 3 fail = framework-dependent publish (my omission, not tool) -> operator re-publishing self-contained (§27 -r win-x64 --self-contained) + re-run. Barrier HELD until B:0.

2026-06-21T11:05:00Z: REBUILD CLEAN steps 1-6 (self-contained exe): migrate no-42P01, functions no-42703 CustomCallData19, data no-encoding/no-missing-table -> R0b+R0c fixes FUNCTIONALLY VALIDATED. Compare[7] failed = missing grant-step (pg_dump app-user perm denied on postgres-owned RTM tables), NOT a rebuild defect; folded grant fix into r0c_fix prompt (3rd item). Operator re-running Compare as postgres on live rtmviewdb_proof for B=. Barrier HELD until B:0.

2026-06-21T11:20:00Z: Rebuild CLEAN (zero errors), Compare B/C/F=0. A=12 schema drift = Compare-ToBaseline NOT carve-aware (line 195 dumps public+identity+audit vs RTM-only schema.sql -> app tables show as extra by design). D=fresh-ledger artifact. NOT Delivered. Awaiting delta report to confirm 12=app + no real RTM drift; then Compare RTM-whitelist fix (item 4) -> re-run -> A:0. Barrier HELD. 5th tooling gap; carve must mirror Compare too.

2026-06-21T11:40:00Z: A=12 root-caused = schema.sql(38 fns) DUPLICATES+DRIFTS db/functions(43); 12 flagged in functions not schema.sql; +2 fn_daytrend* only-in-schema. Per §39.1 functions belong ONLY in db/functions. Rebuild RUNTIME-CLEAN (0 table/index/constraint drift, B/C/F=0); D=ledger artifact. Proposed R0d (functions exclusively db/functions, schema.sql tables-only, Export-All tables-only) OR accept-with-debt. Recommend R0d. Barrier HELD pending coordinator decision.

2026-06-21T15:10:00Z: coord §4-APPROVED OPT-1. Authored tools/cc_prompt_r0d_functions_singlesource.md. CORRECTION: fn_daytrendagentstatus in BOTH (dup, strip); fn_daytrendinteractions only-in-schema (move). (iv) Compare-fix MANDATORY (baseline=schema+db/functions else A worse). Part (i) gated on backend canon (routed). Submitted §4. Barrier HELD till A:0.

2026-06-21T15:25:00Z: R0d gates MET (§4-PASS + STEP 0.5 NUL-restore + backend canon). PINNED: agentstatus token-identical x3 (md5 644ce053) -> keep 02 no-prod-verify; interactions HEAD:schema:1011 post-_002 -> move. Folded into prompt, 🔒 released. EXECUTE relayed to operator (native CC). Barrier HELD till A:0.

2026-06-21T15:55:00Z: R0d (d50f1f1) STATIC object-store GREEN (4 parts; interactions body token-intact 6aa33778; schema 0-fn/26-tbl/0-NUL; Export-All tables-only; Compare carve-aware). Final re-proof needs r0c_fix_bom (Rebuild-Proof.ps1 BOM+Join-Path+grant) committed -> requested §4. On commit -> one Rebuild-Proof run -> A:0 -> all Delivered -> barrier #3. HELD till A:0.

2026-06-21T16:05:00Z: Rebuild-Proof.ps1 local patch reverted again (committed HEAD still broken; re-sync/cache clobbers). r0c_fix_bom §4 pending. A:0 result not wrapper-dependent (R0c/R0d committed) -> operator running local-patch + Compare-as-postgres (carve-aware) for A:0. Barrier HELD.

2026-06-21T16:25:00Z: Re-proof A=106 = MY bad R0d(iv): concat raw db/functions vs pg_dump line-diff double-counts every routine. Tables/idx/constraint/seq all 0 drift; B/C/F=0; rebuild zero-error -> SUBSTANTIVE GOAL PROVEN. On fresh rebuild fn-drift impossible by construction -> routine line-diff meaningless. Proposed R0e (Compare [A]=tables-only -t whitelist + routine name/sig presence, revert (iv)) OR accept-proven+debt. Own the (iv) mistake. Barrier HELD, decision to coordinator.

2026-06-21T18:10:00Z: coord §4-APPROVED R0e (OPT-1, tool-only). Authored tools/cc_prompt_r0e_compare_redesign.md (revert (iv); [A] tables-only -t whitelist; routines name/sig presence; FOLD Rebuild-Proof BOM+Join-Path+grant; supersedes r0c_fix_bom). Submitted §4. Last R0 brick. Barrier HELD till A:0.

2026-06-21T18:20:00Z: R0e §4-PASS + EXECUTE relayed to operator (native CC). On RESULT -> consume binding + one Rebuild-Proof run -> A:0/B:0/C:0/F:0 -> all Delivered -> barrier #3. Last R0 brick. Barrier HELD till A:0.

2026-06-21T18:45:00Z: R0e (39270ad) verify: Compare redesign GREEN (concat removed, tables-only @rtmTables, routine presence); Rebuild-Proof BOM+grant ✓ but (4)(b) 3-arg Join-Path L49/50 MISSED -> runtime-fail PS5.1. Authored tools/cc_prompt_r0e_fix_joinpath.md (2-line). NOT Delivered. Barrier HELD till A:0.

2026-06-21T18:55:00Z: coord OK dispatch R0e(4)(b) Join-Path fix (relayed EXECUTE); v3-planning greenlight RETRACTED -> HOLD, not starting. On fix commit -> one Rebuild-Proof run -> A:0 -> all Delivered -> barrier #3. Barrier HELD.

2026-06-21T19:10:00Z: R0e-fix (8bbee78) GREEN — Rebuild-Proof.ps1 Join-Path nested, BOM ok, 0 NUL. Committed db/tools fully correct (Compare carve-aware + Rebuild-Proof BOM+JoinPath+grant). Relayed final one-command proof to operator. On A:0 -> all Delivered -> barrier #3. HELD.

2026-06-21T21:55:00Z: R0e+fix RAN fully (tool executes; rebuild 0 errors; B/C/F=0). Compare A/A-R still nonzero = 2 R0e impl bugs (delta 213648): [A] -t pg_dump empty (PS splat) -> all 24 tables 'missing'; [A-R] sig-format mismatch source-parse vs identity-args -> 46/46. NOT real drift. Recommend OPT-A: re-frame gate to rebuild-zero-error+B/C/F=0 (proven), accept R0b-R0e Delivered, Compare-normalization = separate deployed-server task. STOP Compare loop. Barrier HELD pending coordinator.

2026-06-21T22:15:00Z: Coordinator adopted OPT-A + opened push-barrier #3. Preflight db/ PASS (restored 7 PD-007-drifted from HEAD -> hash-clean; 6 db: commits in HEAD; no in-flight/untracked). Gate OPT-A met. Wrote READY ack (.coord/push/acks/dba-0620.md) with Compare-tool-bug disclosure (non-gating). status: pushing.

2026-06-21T22:35:00Z: R0 EPIC CLOSED (barrier #3 pushed; R0b-R0e+CC-HIST-001 code-Delivered; OPT-A gate; FREEZE lifted). NEW TASK: LEAD v3 data-architecture for v1 report set — docs/planning on branch v3, NO impl until §4, build on Track A, MINIMAL v1, SF-BI-001 hard-gate, RTM-contour operator-gated. Acked coordinator; handshook bi on report-query data contract. Next: bi's v1 report scope -> draft v3 data-arch doc -> §4.

2026-06-21T22:55:00Z: bi v1-scope (Reports_v1_Scope.md, R1-R4 on landed hist_*, no new tables) consumed. v3 data-arch CONVERGED with bi: +2 composite indexes (queue Workgroup, agent AgentExternalId), TenantSettings.SlThresholdSeconds(default 20) decision, confirm Track A retention/rollup, Hold=234 smoke. Next: short v3 data-arch design doc on branch v3 -> §4. No impl until §4.

2026-06-21T23:20:00Z: Three-tier expansion consumed (raw archive IN v1). DRAFTED docs/bi/Reports_v1_DataArch.md (impl-ready): tier-3 aggregates (+2 idx, SlThresholdSeconds, Track A retention) + tier-2 raw archive (4 EF-app arch_rtsdata_* mirrors, monthly RANGE 84mo, idempotent watermark archiver, archive-first->purge-second, sizing, re-agg indexes; contour read-only). Requested §4. No impl until PASS.

2026-06-21T23:45:00Z: v3 data-arch §4-PASS (20:42). Authored tools/cc_prompt_v3_dataarch_step0.md (T1 aggregates: +2 idx+SlThresholdSeconds; T2 raw archive: 4 arch_rtsdata_* partitioned mirrors+PartTime/PK+ON CONFLICT+watermark+ensure/drop@84mo+ArchiverService archive-first; T3 >=6 tests). STEP-0 backend co-sign gate folded (archiver cadence/batch vs RTM write path). Submitted §4; routed co-sign to backend. Branch v3, NO push.

2026-06-22T00:10:00Z: backend co-sign (21:05, conditional) FOLDED into step-0 prompt: per-table ON CONFLICT (Interaction/UserStatus UPSERT not DO NOTHING; UserStatusLog DO NOTHING), STABLE PartTime (no mutable cols), batch<=10k+yield+watermark-lag. OPERATOR DECISION escalated: UpdateTime contour index (operator-gated DDL) vs seq-scan+cap -> gates ArchiverService; T1+arch-tables proceed. Reviewed bi UserReport migration = APPROVE + IsDeleted NOT NULL default nit.


## 2026-06-25T19:38:40Z | сессия: handoff (dba-0620 -> successor) | EMERGENCY
NON-BUS-DERIVABLE STATE (everything else = read the bus):
- COMMITS (object-store): deliverables 0bd52c0 (v3 DATA-ARCH step-0), 6ea3e6c (carve ChatMessage SF-ARC-001/002/003), 351ec3d (F-QA-6 BackendEmulation CustomCallData1..20) — ALL PUSHED (ancestors of origin/v3, shipped in db9d18e / RTM-REL-2026.06). NO unpushed dba commits.
- CLAIMS: RELEASE all (review-gate model, no standing claims while idle). Successor CLAIMS prod-mirror staging only when the 234 backup lands.
- IN-FLIGHT CC PROMPTS: none open. step0 + fqa6 prompts DONE/pushed; R0* DONE. No section-4 awaiting.
- CURRENT IN-FLIGHT TASK (op-7, EMERGENCY): PROD-DATA-MIRROR one-time seed. Spec = docs/Prod-Mirror-Seed-Plan.md (in v3). Operator brings a 234 DB backup (REAL prod data: RTSData_* + client shape BU/Agents/AgentGroups/PermissionGroups) -> Installations/prod-mirror/. Env becomes a FROZEN data-only mirror; schema stays OURS (our migrated DB). DBA owns DATA-LOAD (which tables+order, identity/scope, PG18); backend owns seeder gate; devops/QA verify. #1 INVARIANT: rebuilds must NEVER re-seed test data -> 3-way gate (config Seed:SampleData=false + provenance sentinel + idempotency-by-real-rows NOT SITE001). NO ACTION until the backup lands. All via coordinator section-4 (no-run-without-bless).
- OPEN LOOSE ENDS:
  1. Ф8 IsSystem (DECIDE before Ф8): report_screens has NO IsSystem column (object-store 473363c). (A) add IsSystem bool NOT NULL DEFAULT false migration + re-seed key (TenantId,IsSystem,Name) OR (B) plain screens (TenantId,Name). bi/operator pick -> DBA reviews the migration DDL.
  2. SF-SEC-001 DB-password rotation — co-own w/ operator; DBA runbook to AUTHOR on operator GO: ALTER ROLE ccdashboard_user PASSWORD <new> on 234/45/5239 (PG15 on 45 per section-43, else PG18) + update conn-strings (appsettings.Production + RTM appsettings + Grant scripts) + re-verify grants + purge plaintext (env/Credential Manager, CODE-05/06). Operator-gated.
  3. Reports-Backend-v1-Spec READY ack given (report_categories SEPARATE, schedules complete, ConfigJson jsonb no-GIN-v1, widget-type=enum no-catalog). Future Ф3..Ф8 generated migrations -> DBA reviews each DDL pre-commit. Ф1 (473363c) + Ф2 (2a8cffe) already ACKed clean.
- ENV PINS: branch v3; origin/v3 shipped db9d18e; PG18 (server-45=PG15); OnDate dd/MM/yyyy; Workgroup=NGC_Queues.ExternalId (section-36.4); agents=external id (IDENT-01/B5); section-0.3 Python+fsync writes (Edit BANNED); section-0.4 phantom .git/index.lock recurs (temp-index path); backticks in python3 -c get shell-eaten -> bus writes ONLY via /tmp heredoc .py files.
RESUME POINT: idle, awaiting (a) the 234 prod backup for the mirror seed, (b) Ф8 IsSystem decision, (c) SF-SEC-001 operator GO, (d) Ф3+ migration DDL reviews.
