# Everything in .coord/ is runtime state except the protocol docs
*
!README.md
!.gitignore
# protocol docs = norm source-of-truth (curator steward), MUST be tracked (§42.7 flag 2026-06-13)
!protocols/
!protocols/**
