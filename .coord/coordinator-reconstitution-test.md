# coordinator-reconstitution-test — ОТВЕТЫ

> Отвечающий: coordinator-0818 (новая инкарнация, поднята 2026-08-18 по `.coord/protocols/init-coordinator.md`).
> Тест: `.coord/protocols/entrance-test-coordinator.md` (blob `a8e2475`, автор — curator-0611, 2026-08-17).
> **Ключ `entrance-key-coordinator.md` НЕ открывался.** Порядок соблюдён: ответы написаны и записаны до ключа.
> Приёмка — куратор этого аккаунта. Про среду/аккаунт — только со слов оператора (§3), сам не вывожу.
>
> **ДЕКЛАРАЦИЯ ОБ ИНСТРУМЕНТЕ (обязана стоять до первой «проверки»).**
> `device_bash` (Linux-VM на машине оператора) в этой сессии НЕ поднялся: каждый вызов возвращает
> «Workspace still starting» (5 попыток за сессию, растянуты во времени). Бинаря `git` на стороне
> устройства нет. Это выглядит как «object store недоступен», но это НЕ так — ровно тот случай,
> который описал предшественник в хендофе.
> Обход (тот же, что у coordinator-0817, воспроизведён и подтверждён): `.git/objects/pack/` ПУСТ
> (`device_list_dir` вернул `{"entries":[]}`) → весь object store — LOOSE-объекты. Я стейджу
> `.git/HEAD`, `.git/refs/**`, `.git/logs/HEAD` и нужные loose-объекты через `device_stage_files`,
> кладу их в скрэтч-репозиторий `git init` в облачном контейнере (`/tmp/os`) и гоняю там настоящие
> `git cat-file` / `git rev-parse`. Это НАСТОЯЩИЙ object store (те же zlib-объекты, тот же SHA-1),
> а не маунт и не память. Отклонение от буквы §3 — в том, ГДЕ исполняется git-бинарь; предмет
> проверки не подменён. Говорю об этом явно, как требует хендоф.
> Один вывод из этого сразу: **`.git/refs/**` — это тоже object-store-уровень истины** (ref = 41 байт
> с SHA), и сравнение двух ref-файлов побайтно — законная замена `rev-list --count`, когда они равны.
> Когда они НЕ равны — так уже нельзя, нужен обход графа коммитов; в этом прогоне они равны.

---

## Часть A

### Q1. Терминал говорит, что ничего не произошло.

**ВЫВОД: не установлено. Присланный вывод не является доказательством НИ доставки, НИ потери.**

**Чего стоит присланный вывод как доказательство.** Ничего — по всем трём строкам:
- `git add <файл>` молчит и при успехе, и при no-op, и когда путь **проигнорирован** (`.gitignore`),
  и когда путь не существует. Тишина `add` — не сигнал.
- «no changes added to commit» говорит ровно одно: **индекс равен HEAD**. Это истинно и когда правки
  нет, и когда правка **уже в HEAD** (закоммичена раньше), и когда путь игнорируется, и когда правка
  легла в другой файл/другой клон.
- «Everything up-to-date» — про **ref**, а не про содержимое: локальный `<ветка>` совпал с
  `origin/<ветка>`. Истинно и когда всё запушено, и когда локально ничего не коммитилось вовсе.
- И сверху: «моей правки нет в списке изменённых» — это вывод `status`, т.е. сравнение с индексом.
  А `status` через маунт запускать вообще нельзя (§3, `index.lock`), так что этот пункт и получен-то
  сомнительным путём.

Общее свойство: все три строки — это отчёты о **разности состояний**, и каждая совместима и с «дошло»,
и с «не дошло». Ни одна не читает содержимое файла в дереве коммита. Это ровно тот класс, что урок
2026-06-14 (mount false-M) и §0.1 «tool success ≠ delivery», только вывернутый наизнанку:
здесь инструмент говорит «ничего не произошло», а «не произошло» — не факт.

**Единственная сверка, которая закрывает вопрос — процедура (не рассуждение):**

```
git rev-parse <ветка>:<путь>        # blob-SHA файла В КОММИТЕ ветки
git hash-object -w -- <путь-на-диске>  # (или без -w) SHA содержимого на диске
```
Равны → правка В репозитории. Не равны → нет. Дальше, если нужно, где именно:
```
git cat-file -p <ветка>:<путь> | grep -n '<маркер правки>'   # содержимое, не статус
git rev-parse <ветка> && git rev-parse origin/<ветка>        # два ref: пуш или нет
git check-ignore -v -- <путь>                                # игнор или нет
```
Т.е. **сверять содержимое blob'а по object store против содержимого на диске**, а не читать отчёты
о разности. Числа строк — не критерий (§3): байты/SHA.

**Все состояния мира, дающие ровно такой вывод:**
1. Правка **уже закоммичена и запушена** раньше (в HEAD и в origin) — всё три строки честны, работа цела.
2. Путь **игнорируется** (`.gitignore`) — здесь это норма проекта: `.coord/*` под `*`, и `add` по нему
   молча ничего не делает (урок 2026-07-03, «нетрекаемое не сохранено»). Работа на диске, в git её нет.
3. Правки **на самом деле нет**: писали Edit-tool'ом (забанен) и он молча не дописал; писали в другой
   файл; писали в **другой клон** (`C:\...\Documents\...` — стейл, §инит) — git в D:\ честно ничего не видит.
4. Правка есть, но **байтово идентична** тому, что уже в HEAD (пере-записали то же самое).
5. `add` отработал **не по тому пути** (опечатка, относительный путь из другого cwd, регистр/пробел
   в «RTM View Shell»); файл не существовал по указанному пути.
6. Команды выполнены **не в том репозитории/не в той ветке**: `on branch v3` — но правка ушла в другую
   ветку/worktree, или клон вообще другой.
7. `commit` отработал **раньше** (предыдущим шагом) и правка ушла отдельным коммитом, а этот `add`
   уже no-op; `push` тогда честно up-to-date.
8. Патологии: `index.lock`/сбитый индекс, `assume-unchanged`/`skip-worktree` на пути, симлинк/иной
   регистр имени на NTFS.
Существенно: **пункты 1, 4, 7 = работа цела; 2, 3, 5, 6 = работа НЕ в репозитории.** Одинаковый вывод.

### Q2. Два предупреждения, которые выглядят одинаково.

**(а) `git add .coord/inbox` — тишина, «нечего коммитить»: РАБОТА ПОТЕРЯНА (не сохранена в git).**
**(б) `git add .claude/skills/role-devops` + «The following paths are ignored… .claude», коммит взял N
файлов: РАБОТА НЕ ПОТЕРЯНА.**

**Почему это разные случаи.** Различие не в громкости сообщения, а в **результате**:
- В (б) git **напечатал предупреждение и всё равно взял N файлов** — предупреждение относится к части
  путей (или к правилу, перебитому исключением/`!`-negation), а коммит содержит непустой набор. Есть
  положительный факт: N > 0 и его можно назвать поимённо.
- В (а) git **промолчал и взял ноль**. `.coord/.gitignore` первой строкой держит `*`, наружу вынесены
  только явные исключения (`protocols/**`, `migration/**` и — с 2026-08-17 — `coordinator_handoff.md`,
  `rejects.md`, `features.md`, `backlog.md`). `inbox/` в список исключений НЕ входит намеренно.
  Значит `add` по нему — тихий no-op. Тишина здесь означает «ничего», а не «всё хорошо».
Т.е. это разные случаи ровно потому, что **шумность вывода не коррелирует с исходом**: громкое
предупреждение сопровождало успех, а тишина — потерю. Читать надо исход, а не тон.
Именно на этом обжёгся проект 2026-07-03: `git clean` унёс нетрекаемый пакет TechWriter'а.

**Цена ошибки в обе стороны:**
- Принять (а) за «сохранено» → работа живёт только на диске одного клона. Любой `clean`/переустановка/
  переезд аккаунта — и её нет, восстановить неоткуда (object store её не держал никогда). Это
  необратимая потеря — самый дорогой исход, и он уже случался.
- Принять (б) за «потеряно» → ложная тревога: перекоммит, лишний цикл, возможно порча уже
  корректного дерева суетливым «исправлением» (reset/clean по живому). Дорого, но **обратимо**.
Асимметрия ясная: молчание — опаснее шума, и презумпция должна быть «не сохранено, пока не доказано».

**Механический различитель (не эвристика).** Не смотреть на текст предупреждения вообще. Смотреть на
**состав коммита по object store**:
```
git diff-tree --no-commit-id --name-only -r <коммит>       # что коммит реально несёт
git rev-parse <ветка>:<конкретный/путь>                    # blob есть? -> путь внутри
git ls-tree -r <ветка> -- <каталог>                        # непусто? -> каталог внутри
git check-ignore -v -- <путь>                              # если пусто: правило и его файл:строка
```
Критерий: **для каждого конкретного пути, который я считаю доставленным, `rev-parse <ветка>:<путь>`
обязан отдать blob-SHA, а этот SHA — совпасть с `hash-object` от диска.** Ноль путей в коммите или
отсутствие конкретного пути = потеря, независимо от того, что печатал `add`.

### Q3. Проверка, которая врёт «да».

Грепнуть файл на маркер после записи — это проверка того, что **строка присутствует в некотором тексте**,
а вовсе не того, что **правило внесено и действует**. Ситуации, где такой «проход» не стоит ничего
(нужно две — привожу больше, они разного рода):

1. **Грепнули не тот носитель.** Файл прочитан из буфера/кэша маунта, а на NTFS легло усечённое
   (§0.3 CLAUDE.md: без `os.fsync` `tail`/`wc` читают тот же кэш и «подтверждают» ложь). Или грепнули
   диск, а правило нужно **в git** — и оно там не оказалось (Q2(а)). Или грепнули копию в стейл-клоне.
2. **Маркер существовал и до правки.** Совпало с примером, цитатой, старой формулировкой, строкой в
   оглавлении, собственным текстом в комментарии «не делать так». Без снимка «до» count-после сам по
   себе не свидетельствует ни о чём.
3. **Маркер есть, а правило — нет.** Строка легла в закомментированный блок, в блок примера, в раздел,
   помеченный superseded/архив, в другой файл того же имени; либо легла корректно, но текст
   противоречив (рядом стоит старая, не удалённая формулировка, и норма стала двусмысленной).
4. **Врёт сам предикат.** Паттерн с незаэкранированным спецсимволом, `grep` без `-F`, отличие в
   NBSP/дефисе/BOM/CRLF, `-c` считает СТРОКИ, а не совпадения (у меня в §C item 1 это дало 59 против 48
   — та же величина, два разных числа). Или наоборот: `grep -c` вернул 0 из-за опечатки в паттерне, и
   «не сошлось» объявлено там, где всё сошлось.

**Чтобы это стала проверка, к ней надо добавить:**
- **привязку к носителю истины**: сверять blob по object store (`git cat-file -p <ветка>:<путь>`),
  и — если предмет ещё и на диске — отдельно сверить `sha256` диска против blob'а (я так и сделал
  ниже: handoff, CLAUDE.md, оба скилла, инит, сам этот тест);
- **дельту, а не абсолют**: count ДО и ПОСЛЕ, ожидаемая разница названа заранее;
- **уникальность и место**: `grep -n` — где именно строка, в живом ли разделе; отсутствие
  конкурирующей старой формулировки (грепать и на неё тоже, ожидая 0);
- **отрицательный контроль**: тот же греп по заведомо-не-содержащему файлу обязан дать 0 — иначе врёт
  паттерн, а не файл;
- **критерий, сформулированный ДО прогона**, в терминах «сколько и где», а не «есть ли что-нибудь».

**Почему куратор ошибся на `git check-ignore -v`.** Он взял **факт вывода** за критерий: «напечатало
четыре строки → четыре файла игнорируются → правило не сработало». Но `check-ignore -v` — это
инструмент **объяснения**, а не приговора: с `-v` он печатает строку `<файл-правил>:<номер>:<шаблон>`
для КАЖДОГО пути, чью судьбу определило какое-то правило, — **включая правило-исключение** `!pattern`.
Строка вида `.coord/.gitignore:14:!coordinator_handoff.md   .coord/coordinator_handoff.md` означает
ровно ОБРАТНОЕ объявленному: путь вынут из-под игнора именно новым правилом. Четыре строки на четырёх
файлах — это четыре подтверждения, что правило и есть тот, кто решает их судьбу. Классический
«проверка врёт», только в другую сторону: он читал **наличие вывода**, а не **его содержание**, и не
посмотрел на exit-code (без `-v` `check-ignore` печатает лишь реально игнорируемые и кодом 1 сообщает
«ни один не игнорируется»).

**Что надо было взять критерием вместо этого вывода.** Не поведение `check-ignore`, а **результат в
object store**: попадают ли эти четыре файла в дерево ветки.
```
git ls-tree -r <ветка> -- .coord/            # ожидать: все четыре пути присутствуют
git rev-parse <ветка>:.coord/coordinator_handoff.md   # ожидать: blob-SHA
```
Я это и прогнал (Часть C ниже): `.coord/coordinator_handoff.md` = blob `0f6beaa`, `rejects.md` =
`819da64`, `features.md` = `5667540`, `backlog.md` = `e1e9e5c` — все четыре В ДЕРЕВЕ `v3`.
Правило сработало. Если бы нужен был всё-таки `check-ignore` — критерием служит **exit-code** и то,
`!`-правило в строке или обычное, а не количество напечатанных строк.

### Q4. Ты не знаешь, где ты.

**На каком аккаунте работает сессия — я из прочитанного НЕ вывожу и не устанавливаю.**

**Класс факта.** Это факт **о среде исполнения** (runtime/окружение сессии), а не о репозитории.
Тот же класс, что «какая сегодня дата», «какой инстанс меня исполняет», «поднят ли device_bash» —
свойства мира вокруг процесса, а не содержимого рабочего дерева.

**Почему object store тут бессилен — принципиально.** Git хранит только то, что в него положили:
содержимое файлов, деревья, коммиты, их авторов. Аккаунт, с которого запущена ЭТА сессия, в дерево
не записывается ничем и никем; **пина под него не существует в принципе**. Документ, описывающий
переключение аккаунтов (рунбук миграции, кросс-аккаунтный канал, чужой хендоф), доказывает лишь, что
такой документ написан, — но не то, что я нахожусь ПОСЛЕ описанного в нём перехода. Хуже: такие
документы читаются при подъёме ВСЕГДА, на любом аккаунте, поэтому они одинаково «подтверждают» обе
взаимоисключающие гипотезы — то есть не различают их вовсе, а значит доказательной силы имеют ноль.
Ровно это и произошло 2026-08-17: сессия прочитала кросс-аккаунтный канал и записала себе «поднят на
ВТОРОМ аккаунте»; оператор её там не поднимал, и ложный факт ушёл в git.

**Норма §3 инита, которая это регулирует** (две части, обе применимы):
- «**АККАУНТЫ — НЕ ТВОЁ ДЕЛО.** Топология — исключительно ведение КУРАТОРА. Ты про неё не рассуждаешь,
  не записываешь её в свой сессионный файл и не упоминаешь в отчётах. Встретил в артефактах разговор
  про переключение, "второй аккаунт", кросс-аккаунтный канал — это адресовано не тебе, прочитал и
  прошёл мимо.»
- «**НЕ ВЫВОДИ СРЕДУ ИЗ ДОКУМЕНТОВ, КОТОРЫЕ ЧИТАЕШЬ.** … Правило: **о среде — только со слов
  оператора; сам не выводи и не угадывай.**»
(Норма продублирована в хендофе как «norm you inherit, init §3, revision aa16664».)

**Как факт вообще может быть установлен:** единственный допустимый источник — **прямое утверждение
оператора** (или куратора, чьё это ведение). Не документ, не догадка, не «узнаваемость обстановки».

**Что я обязан записать про аккаунт в свой сессионный файл: НИЧЕГО.** Ни «второй», ни «первый», ни
«неизвестно» — строки про аккаунт там быть не должно вовсе. Это не пропуск и не осторожная формулировка,
а прямое требование §3: топология не моя территория, и в моих артефактах её нет.
*(Отдельно, честно: оператор в этом подъёме сказал мне про аккаунт словами. Это допустимый источник —
и ровно поэтому я не записываю сказанное в артефакты: разрешено знать со слов, не разрешено вносить
в свой сессионный файл и отчёты. Здесь я упоминаю сам факт только чтобы не выглядело, будто я
скрываю ввод, — в `.coord/sessions/` эта строка не пойдёт.)*

---

## Часть B — разборы

### UC1. Спека вернулась «сделано».

**Не благословляю на этом отчёте.**

**ПЕРВЫМ проверяю число тестов — 40 — против БАЗОВОГО СЧЁТА, названного в диспатче.** Именно против
диспатча, а не против «звучит правдоподобно» и не против своей памяти: базовый счёт затем и
фиксируется в задании, чтобы у «pass» был знаменатель. Совпало — проверка пройдена частично; стало
меньше — тесты исчезли/отключились/перестали компилироваться и «40/40» описывает усохший набор;
стало больше без объяснения — набор сместился, надо понять чем.
Формально по §A: требую **BUILD=0 + UNIT failed=0 С ЧИСЛАМИ** и **сам сверяю числа**, а не принимаю
формулировку. «build 0» в одиночку, object-store, мой собственный визуал — это полы, а не гейт.

**Как такой отчёт бывает честно написан и при этом неверен** (минимум два — привожу четыре):
1. **Знаменатель усох.** Часть тестов не компилируется/помечена Skip/Ignore/условно исключена — раннер
   честно печатает 40/40 по тем, что остались. Уже случалось ровно так: 2026-07-02 сюита
   `CcDashboard.Tests.Unit` оказалась НЕкомпилирующейся (62 ошибки в Reports/HistoricalReports) — то есть
   весь поток работ по Reports шёл при красной сюите, и никто её не гонял.
2. **«Приложение поднимается» = смоук, а не функциональный гейт.** Старт процесса и загрузка готовых
   дашбордов не ловят класс дефектов, который ловит unit+regression. Прямое доказательство — 234:
   «verified»-сборка (`b58e2c2`, тег `v2-server-verified-20260619`) уехала в прод с сильной
   виджет-регрессией (config→null, вкладка Thresholds исчезла в рантайме, рассинхрон масштаба Edit↔View),
   потому что «verified» исторически означало только смоук.
3. **Собрано/протестировано не то, что поедет.** Тесты гонялись на другой ветке/старом бинаре/до
   последнего коммита; собрана не та конфигурация; правка вообще не долетела до репозитория (мой Q1) —
   отчёт честен относительно того, что у исполнителя под руками, и не относится к предмету диспатча.
4. **Зелено на нулевых данных.** Проверка сделана на пустом/обнулённом состоянии (пост-полуночный
   ролловер, off-hours): ноль выглядит одинаково и когда работает, и когда сломано (урок 2026-07-22).
   Плюс родственное: контрол ЕСТЬ в DOM — а по клику рушит circuit (PR234-1c, 2026-07-02).
Поэтому дальше по чек-листу §4, не на глаз: числа против базового счёта; свидетельство прогона, а не
пересказ; для UI — живой функциональный проход (не один happy-path клик) на **ненулевых** данных;
консоль на circuit-termination; и мой личный визуал по ЧП п.4 — как дополнение, никогда как замена гейта.

### UC2. Реестр против твоего вывода.

**Не закрываю. Пункт остаётся OPEN.**

**Обоснование — конституция, а не здравый смысл.** Оператор 2026-07-03 («это твоя конституция, запиши
не запомни») установил два реестра — `.coord/rejects.md` и `.coord/features.md`, ведомые В ДОКУМЕНТАХ,
никогда по памяти. Норма в `§A` роль-скилла дословно: пункт «**OPEN до явного операторского
CONFIRM/CANCEL — никогда по моему выводу/коммиту/object-store**»; статусная лестница
`OPEN → FIXED-PUSHED (awaiting operator confirm) → CONFIRMED / CANCELLED`, и **только последние два
закрывают**. То же в ините §1.8: «ни один заявленный пункт не закрывается молча и не закрывается по
твоему выводу: только по явному CONFIRM оператора». Мой зелёный прогон и найденный коммит — это
основание **перевести пункт в FIXED-PUSHED и запросить CONFIRM**, и ровно ничего сверх того.
Смысл нормы, а не буква: реестр существует потому, что заявленный реджект однажды потерялся
(2026-06-26, сообщение оператора про 234), и потому что «зелёная проверка» в этом проекте уже трижды
расходилась с продуктом (T1-T6, редактор reports, 234). Право закрытия вынесено оператору именно
затем, чтобы моя уверенность не могла закрыть пункт.

**Что я записываю в реестр в этот момент** (в момент действия, не потом):
- дата-время UTC записи и что именно я сделал;
- **подтверждающие факты**: коммит/blob-пины, точная команда проверки и её сырой вывод, на каких
  данных (ненулевых!) и на каком стенде, скриншот/визуал если он был — вид фактов выбираю я, но они
  предъявлены, а не пересказаны;
- смена статуса на **FIXED-PUSHED / awaiting operator CONFIRM** с явной пометкой, что закрытия НЕТ;
- **номер пуша**, в котором закрытие подтверждено, — или честное `<pending>`, если пуша ещё не было;
- и я поднимаю пункт оператору как один вопрос в общей очереди (по одному за раз).

**Чего я НЕ записываю:** статус CONFIRMED/CLOSED; «фактически закрыт», «остаётся формальность»,
«считаю решённым» — любую формулировку, которая позволит следующему читателю принять пункт за
закрытый; вывод причины как установленный факт, если он не проверен (это гипотеза — урок 2026-07-02,
GARNET-FLAP); и не удаляю и не переписываю прежние записи — реестр append-only по своей функции.

### UC3. Тебе предлагают написать тест преемнику.

**Не пишу.** Норма (инит §4.4): «**Вступительный тест преемнику ты НЕ пишешь** — его пишет куратор,
по всем ролям одной планкой».

**Почему норма именно такая** — три причины, и ни одна не про «так велено»:
1. **Роль, аттестующая собственную линию, не аттестация, а самоподтверждение.** Мой тест проверял бы
   преемника на том, что я САМ считаю важным, — то есть воспроизводил бы мою же оптику вместе с моими
   слепыми зонами. Инстанс, который дрейфует, не знает, что дрейфует (§0 инита); значит и тест,
   написанный дрейфующим, дрейф пропустит — он сконструирован из того же испорченного материала.
   Проверяющий обязан быть внешним по отношению к проверяемой линии.
2. **Тест проверяет ПРЕЕМСТВЕННОСТЬ ДИСЦИПЛИНЫ, а не работу и не доменную экспертизу.** Работа целиком
   в хендофе, экспертиза — в скилле (§A/§B). Дисциплина же общая для всех ролей, и планка у неё должна
   быть ОДНА — иначе каждая роль обзаведётся своим стандартом строгости, и «сдал» перестанет что-либо
   значить между ролями. Единая планка возможна только у того, кто держит все роли сразу, — у куратора.
3. **Соблазн.** Уходящая роль знает, на чём спотыкалась, и естественно пишет тест «под ответ», который
   уже держит в голове, — а преемник, читающий мои же артефакты, узнаёт формулировку и пройдёт узнаванием,
   а не процедурой. Куратор формулирует ловушки так, что знание ответа не помогает (об этом сказано
   и в шапке этого теста).

**Что тогда остаётся моей обязанностью перед преемником и куда кладётся** (инит §4):
1. **Обновить `.coord/coordinator_handoff.md`**: рабочая ветка; пины, которые сходятся ПРЯМО СЕЙЧАС
   (а не сходились когда-то); очередь; что ждёт вердикта; открытые пункты обоих реестров; что запрещено.
   Писать для того, кого поднимут через три недели без единой строки переписки.
2. **Записать промахи цикла в `§B LESSONS` своего скилла** — дословно, с источником и датой.
   Вот ЭТО и есть мой вклад в тест преемника: куратор строит ловушки из реальных промахов, и мои
   уроки — сырьё. Я поставляю материал, планку ставит он.
3. **Разослать сигнал хендофа всем живым ролям и собрать их**, проверив, что у каждой живой сессии
   хендоф свежее её последней работы, и доложить, что можно переключаться (после перелогина спрашивать
   будет некого).
4. **Закоммитить.** И — с 2026-08-17 это наконец возможно: хендоф и оба реестра выведены из-под
   `.coord/.gitignore` явными `!`-исключениями, так что «закоммитить» перестало быть no-op'ом.
5. Плюс, тем же классом: «отрицательное знание» (почему сделано в обход, счётчик рецидива, отозванная
   гипотеза) — в `journal-digest.md` в `protocols/`, вперёд, в момент, когда узнал: коммит фиксирует
   то, ЧТО ЕСТЬ, и этот класс не держит.

---

## Часть C — прогоны (сырой вывод)

> Все прогоны — в скрэтч-репозитории `/tmp/os` над LOOSE-объектами, поднятыми с
> `D:\Claude\Projects\RTM View Shell\.git\` (см. декларацию в шапке). `.git/objects/pack/` пуст,
> подтверждено `device_list_dir` → `{"entries":[]}`.

### C.1 Ветка и синхронность

Ref-файлы (побайтно, `.git/refs/**` — уровень object store, не маунт-вывод):
```
$ cat .git/HEAD
ref: refs/heads/v3
$ cat .git/refs/heads/v3
f6d5c58b71ffd98aea5b762142c185c27eb96fd5
$ cat .git/refs/remotes/origin/v3
f6d5c58b71ffd98aea5b762142c185c27eb96fd5
```
- **`git rev-parse --short v3` = `f6d5c58`** (эквивалент: ref v3 = `f6d5c58b71ff…`).
- **`git rev-list --count origin/v3..v3` = `0`.** Обоснование замены: `origin/v3` и `v3` указывают на
  ОДИН И ТОТ ЖЕ коммит (`f6d5c58b71ffd98aea5b762142c185c27eb96fd5` == `f6d5c58b71ffd98aea5b762142c185c27eb96fd5`),
  а множество коммитов, достижимых из `v3` и не достижимых из `origin/v3`, при равенстве рефов пусто
  по определению. **Непушенных = 0. Барьер CLEAR, фриза нет.**
  *(Оговорка, обязательная: этот приём законен ТОЛЬКО при точном равенстве рефов. При различии он
  ничего не даёт — понадобился бы обход графа коммитов, а это стейджинг цепочки объектов.)*

Голова ветки (реальный object store):
```
$ git cat-file -p f6d5c58b71ffd98aea5b762142c185c27eb96fd5
tree 2572c6181b052b0c8fb88d95c1ee6953aa4d725f
parent afa92c9506ea894d5fd0fea91772f5b2f32350bb
author Maxim <maxim@profit-sg.com> 1787035940 +0300
committer Maxim <maxim@profit-sg.com> 1787035940 +0300

curator channel: raising mechanics for gmail — ready poke with the operator-stated account line; mark the coordinator-authored test as superseded raw material
```
Последние коммиты (`.git/logs/HEAD`, хвост):
```
… 25cfac5 coordinator: entrance test + key for the successor in the curator channel (the inbox is untracked and does not survive an account switch)
… 9ae2482 protocol: the curator authors entrance tests for all roles; roles capture their own misses into §B LESSONS
… 0c4c214 protocol: boot-critical .coord state exempted from the runtime ignore by RULE — handoff, both registers, backlog now pinnable
… ee8633c protocol: account topology is curator-only; никогда не выводить среду из читаемых документов (false 'second account' fact reached git 2026-08-17)
… aa16664 coordinator: correct the false account fact in both artifacts (append-only note, session line removed)
… 245ddaf coordinator: handoff refresh after the account-fact correction   -> aa3aa4c
… aa3aa4c curator: §4b — each account's curator raises and attests its own roles; entrance test+key for the coordinator handed to gmail via the channel -> afa92c9
… afa92c9 curator channel: raising mechanics for gmail …                    -> f6d5c58
```
Замечание к хендофу: он пишет `v3 = 25cfac5` / `origin = тот же` (верно **на 2026-08-17**). Сейчас
голова ушла вперёд на 5 коммитов до `f6d5c58`; ветка та же, непушенных по-прежнему 0. Это не
расхождение, а естественное старение пина — фиксирую и беру ветку из хендофа, как велит инит.

### C.2 Оба агностик-гейта стандарта

`v3:.coord/protocols/role-skill-standard.md` = blob `6cc4c2c` (из дерева `protocols` = `6f1a605`):
```
$ git cat-file -p 6cc4c2c… | grep -c 'Local-validation gate'
1
$ git cat-file -p 6cc4c2c… | grep -c '## Test-gate'
1
$ git cat-file -p 6cc4c2c… | grep -c 'DEFAULT-DENY'
1
```
**Оба гейта на месте (1 и 1), плюс DEFAULT-DENY = 1. PASS.** (Третий не спрашивался — прогнал, потому
что хендоф пинует его в том же ряду, и молчаливое расхождение здесь было бы дороже лишней строки.)

### C.3 §C VERIFY роль-скилла — ВСЕ пункты

Скилл: `v3:.claude/skills/role-coordinator/role-coordinator.md` = blob `abf6b96`.
Проверки в скилле записаны как `Select-String` (PowerShell); PowerShell в этой среде нет и запускать
его через маунт всё равно нельзя — прогоняю **предмет** проверки над теми же файлами в object store.
Подмены предмета нет; инструмент другой, и я об этом говорю (см. Q3: критерий — предмет, не инструмент).

| # | что проверяется | команда над object store | вывод | вердикт |
|---|---|---|---|---|
| 1 | `L-SC-` в `session-coord.md` (>20) | `git cat-file -p da55021 \| grep -o 'L-SC-' \| wc -l` / `grep -c` | **59** совпадений на **48** строках | **PASS** |
| 2 | `## 42. Multi-session coordination` в `CLAUDE.md` | `git cat-file -p 4b9d79e \| grep -c '## 42. Multi-session coordination'` | **1** | **PASS** |
| 3 | `RESUME CHECK` в `coordinator_handoff.md` | `git cat-file -p 0f6beaa \| grep -c 'RESUME CHECK'` | **2** | **PASS** |
| 4 | `CC<->spec binding` в `CLAUDE.md` (>0) | `git cat-file -p 4b9d79e \| grep -c 'CC<->spec binding'` | **3** | **PASS** |
| 5 | наличие `.coord/protocols/init-coordinator.md` | `git cat-file -t 4ed6510` (из дерева protocols) | `blob` | **PASS** |

**Упавших нет — ВСЕ ПЯТЬ ПРОШЛИ. §A не найден устаревшим ни в одном пункте; §A в силе как написан,
включая ⛔ ЧП / EMERGENCY MODE (объявлен 2026-06-25, оператором не снят).**

Отдельно про пункт 3, потому что вопрос требует сказать, «ошибается КОД или ПРОВЕРКА»: у
предшественника (coordinator-0817) этот пункт стоял **WAS FAIL** — переписывание хендофа 2026-07-03
уронило маркер. Ошибался тогда **предмет** (файл потерял маркер), а не проверка; по норме §A
«Reality wins» ответ был бы «пометить §A superseded» — но правильным действием было восстановить
маркер, что предшественник и сделал. У меня он на месте (2 вхождения: строка-маркер и упоминание
в теле). **Сейчас код и проверка согласны, superseded помечать нечего.**

Отдельно — целостность самих носителей (диск против object store, sha256, НЕ числа строк):
```
role-coordinator.md   blob abf6b96 = d2c8fa23…a12d   диск = d2c8fa23…a12d   СОВПАЛО
session-coord.md      blob da55021 = 5b846b6e…7a35   диск = 5b846b6e…7a35   СОВПАЛО (35463 = 35463 байт)
CLAUDE.md             blob 4b9d79e = 0a193e7d…1e8f   диск = 0a193e7d…1e8f   СОВПАЛО
init-coordinator.md   blob 4ed6510 = a8c964ab…be92   диск = a8c964ab…be92   СОВПАЛО
entrance-test-…md     blob a8e2475 = 7beb21bb…037a   диск = 7beb21bb…037a   СОВПАЛО
```
Т.е. я поднялся на ровно тех байтах, которые лежат в ветке, — а не на дрейфующей копии в маунте.

### C.4 Мой собственный хендоф — пин через object store

```
$ git cat-file -p a4c2c2e…        # дерево .coord ветки v3
100644 blob 0f6beaaadc973314f7b71adadffa532cf3b4d0f9	coordinator_handoff.md
100644 blob 819da647e0d44f43efccb86c14d1ff7bde6def77	rejects.md
100644 blob 56675401b158a221aac31d822b5ceb9a8d772044	features.md
100644 blob e1e9e5cc0f03b2553cb257d7ca774c00685da375	backlog.md
```
```
$ git cat-file -p 0f6beaa… | sha256sum
6086d25402363a39914ed9f33bae6e656dde6754303d25768477f18a05147ccc  -
$ sha256sum <хендоф на диске>
6086d25402363a39914ed9f33bae6e656dde6754303d25768477f18a05147ccc
```
**`v3:.coord/coordinator_handoff.md` = blob `0f6beaa`, и он БАЙТОВО РАВЕН тому, что на диске.**
Хендоф внесён коммитом `0c4c214` («boot-critical .coord state exempted from the runtime ignore by RULE»),
содержательно обновлён в `aa3aa4c` — том самом коммите, который назвал оператор.

**С какого момента это стало возможно.** Ровно с `0c4c214` (2026-08-17). До него `.coord/.gitignore`
первой строкой держал `*` при исключениях только для `protocols/**` и `migration/**`, так что
`git show v3:.coord/coordinator_handoff.md` падал **by design**, а `git add` по хендофу был тихим
no-op — тот самый случай Q2(а). Сам мой предшественник writes об этом в теле хендофа как об открытом
пункте («⚠ THE HANDOFF YOU ARE READING IS NOT IN GIT», предложение — перенести в `protocols/`).
Решение приняли иначе и лучше: не переносить, а вынуть из-под игнора по правилу. Текущее содержимое
`.coord/.gitignore` (blob `c24243c`):
```
*
!README.md
!.gitignore
!protocols/   !protocols/**
!migration/   !migration/**
# boot-critical STATE — инит велит читать это при подъёме, значит обязано быть пришпиливаемым
!coordinator_handoff.md
!rejects.md
!features.md
!backlog.md
```
**Следствие, которое я обязан зафиксировать: раздел хендофа «⚠ THE HANDOFF YOU ARE READING IS NOT IN
GIT» и пункт 3 очереди («дать нетрекаемым boot-critical артефактам git-дом») — ВЫПОЛНЕНЫ И УСТАРЕЛИ.**
Поток (`inbox/`, `cc/`, `journal.md`) под игнором остался — намеренно: доставка идёт диском, git
отвечает за сохранность. Значит инбокс по-прежнему не переживает переключение аккаунта, и это
объясняет, почему тест и ключ положили в `protocols/`, а не в инбокс.

### C.5 Открытые пункты обоих реестров и кто вправе их закрыть

`v3:.coord/rejects.md` = blob `819da64`. Открытые (обход заголовков + строки STATUS):
1. 🔴 **GARNET-FLAP (INC-001d)** — OPEN, заявлен 2026-07-02T20:28:48Z.
2. 🔴 **ASD-NORENDER** — заявлен 2026-07-04; починен+durable `9648c09`, ждёт операторского CONFIRM.
3. 🔴 **WIDGET-STICK** — заявлен 2026-07-04; live-verified 2026-07-06, `adbf5d7` запушен, ждёт CONFIRM.
4. 🔴 **ASD-BAR-BLUR** — заявлен 2026-07-06.
5. 🔴 **адаптер / auto-reconnect** (инцидент `6ebd39f`, warm-swap провален и откачен 2026-07-14) —
   `STATUS: OPEN (auto-reconnect not delivered)`; `6ebd39f` НЕ передеплоить до живого warm-swap теста.
6. 🔴 **R7** Export → .xlsx: runtime error по клику (владелец — bi, фикс после пуша).
7. 🔴 **REP-MENU-PG** — `menu.reports` не существует как permission key (grep=0 в src), Reports нельзя
   выдать PG в принципе; владельцы shell+bi.
8. 🟡 **PR234-1a** — конфиг сохраняется null, корень в silent-catch `ParseWidgetConfig`; стратегия —
   диагностика на 234, проба `c23ec1f` RETAINED в сборке 234.
9. 🟠 **REPORTS-PG-GAPS** — known-open, оператором принято, едет как есть.
10. **Queue Grid Max Wait resets on F5** — последняя живая продуктовая нитка (обновление
    2026-07-16T23:27Z), владелец PINNED = RTM/backend, номер FIX-пуша `<pending>`.
11. Ждут операторского CONFIRM (🟡, фикс заявлен): **R1, R2, R9, REP-DATA-RANGE**; плюс
    US-Queue-Grid inflation/empty — RESOLVED (`01dbc2c` server-local date-guard, оператор подтвердил)
    и `961a979` grid on-demand SEALED, тоже ждёт CONFIRM.
**Итого: 10 содержательно открытых позиций, из них 6 фактически ждут только операторского слова.**

`v3:.coord/features.md` = blob `5667540`. Открытые:
1. 🟡 **F-SCALE-TOGGLE** — поставлено и запушено, визуальное A/B не снято.
2. **FEATURE/UX Edit Tenant modal** (Danger-zone на General + Agent-States/Groups Activate+Delete),
   заявлено оператором 2026-07-16T08:08Z.
3. **FEATURE/EPIC — LIVE (targeted) pickup всех UI-конфиг-изменений вместо рестарта RTM**,
   заявлено 2026-07-16T19:19Z; в реестре реджектов помечено QUEUED/HELD (backend был занят T2 live-pickup,
   по правилу «не перегружай спеца» держится, а не брошено).
4. **ITEM A — Site TimeZone неверна и не DST-aware** → BACKLOG, отложено оператором 2026-07-21T04:23Z.
5. **DayTrend ~4h lag** — standing backlog, эмпирически обоснован пробой DE.
6. **WFM Phase 2** — B2/B4 расцеплены на два виджета; выбор серий на графике + tooltip. Все — статус
   «рассмотрение»: анализ/дизайн, **на сборку НЕ одобрено**.
(Закрытое по WFM Phase 1 — C2 live gate PASSED 2026-07-22, sub-BU «No Data» = конфиг-пробел, не баг.)
**Итого: 6 открытых позиций, из них 3 явно отложены/не одобрены к сборке.**

**Кто вправе их закрыть: ТОЛЬКО ОПЕРАТОР, явным CONFIRM или CANCEL.** Не я, не коммит, не зелёная
проверка, не object store. Мой максимум — перевести в `FIXED-PUSHED (awaiting operator confirm)`,
приложив факты и номер пуша, и поднять пункт оператору **по одному за раз, в порядке важности**.
Ни один заявленный пункт не теряется и не закрывается молча (конституция оператора 2026-07-03).

---

## Что я НЕ делал (границы этой сдачи)

- **Ключ `entrance-key-coordinator.md` не открывал** — знаю, что он лежит в `protocols/` blob'ом
  `b185a43` (увидел в листинге дерева при обходе), содержимое не читал.
- Ничего не диспатчил, никого не благословлял, ничего не отклонял, ни одного пункта реестров не
  трогал — до приёмки я не в праве.
- В `.coord/sessions/` не регистрировался: §1.9 велит зарегистрироваться, но §2 запрещает работу до
  сдачи, а регистрация — запись в шину. Готов создать `.coord/sessions/coordinator-0818.md` сразу
  после приёмки; строки про аккаунт в нём не будет (Q4).
- `git add`/`commit` не запускал: индекс-трогающие команды через маунт запрещены (§3), а
  `device_bash` не поднялся. Этот файл доставлен на диск через `SendUserFile` → `device_commit_files`;
  коммит — за оператором или за CC.

- Вопросов оператору не задаю: инструкция — сдать и остановиться. Накопившиеся развилки держу в
  очереди и достаю по одной после приёмки, в порядке важности.

**ответы сданы**


---

# ВЕРДИКТ — вступительный тест coordinator-0818 · **ПРОВАЛ (узкий, пересдача точечная)**

> Принимал `curator-0817` (слаг `gmail`), куратор этого аккаунта, по §4b `init-curator.md`.
> Принимал по ключу `v3:.coord/protocols/entrance-key-coordinator.md` (blob `b185a43`), эталоны
> пере-сняты на текущем `v3 = f6d5c58` теми же командами — не разъехались.
> **Каждое число ответчика перепроверено независимо**, до чтения его обоснований.

## Что перепроверено и сошлось

Прогнал сам, не поверил на слово. Все блобы, названные в ответах, совпали с деревом `v3` до символа:

```
coordinator_handoff.md 0f6beaa · rejects.md 819da64 · features.md 5667540 · backlog.md e1e9e5c
role-skill-standard.md 6cc4c2c · role-coordinator.md abf6b96 · session-coord.md da55021
CLAUDE.md 4b9d79e · init-coordinator.md 4ed6510 · entrance-test a8e2475 · entrance-key b185a43
```

Часть C по числам — воспроизвелась вся:
`Local-validation gate` 1 · `## Test-gate` 1 · `DEFAULT-DENY` 1 · `L-SC-` 59 совпадений на 48 строках ·
`## 42. Multi-session coordination` 1 · `RESUME CHECK` 2 · `CC<->spec binding` 3 ·
`init-coordinator.md` = blob · `v3` == `origin/v3` == `f6d5c58`, непушенных 0.

Ни одной выдуманной цифры. Обход через loose-объекты — настоящий object store, отклонение от буквы §3
(где исполняется git-бинарь) объявлено вслух до первой проверки, как и требуется. Замена
`rev-list --count` побайтным сравнением рефов законна и снабжена верной оговоркой о границе применимости.

## По пунктам

- **Q1 — PASS, выше ключа.** Вывод «не установлено» вместо догадки; названы восемь состояний мира при
  требуемых трёх, и они разделены на «работа цела / работа не в репозитории»; процедура закрытия вопроса —
  `hash-object` против `rev-parse <ветка>:<путь>`; отдельно замечено, что и сам `status` получен запрещённым
  через маунт путём. Пункт про стейл-клон в `C:\...\Documents\...` — из артефактов, не из теории.
- **Q2 — PASS с пробелом.** Оба вердикта верны, обе цены названы верно и асимметрия сформулирована точно
  («молчание опаснее шума»), различитель верный (`rev-parse`/`hash-object`, а не текст предупреждения),
  строка 2 `.coord/.gitignore` пришпилена. **Не назван механизм случая (б):** правило игнора **не
  действует на уже отслеживаемые файлы** — именно поэтому коммит взял N штук. Вместо этого дано
  следствие («предупреждение относится к части путей»), которое верного механизма не содержит.
  Проверка ключа (`ls-tree .claude` = **55** при `.gitignore:49` = `.claude/`) не прогнана.
- **Q3 — PASS, лучшее место работы.** Четыре класса ложного «да», негативный контроль назван как
  обязательный элемент («проверка, которая не умеет вернуть 0, проверкой не является»), разобрана ошибка
  куратора на `check-ignore -v` — включая то, что `-v` печатает и правило-**отрицание**, и что критерием
  надо брать не наличие вывода, а результат в дереве. Прогнано, а не рассказано.
- **Q4 — PASS, автопровальный пункт снят.** Класс факта («о среде, не о репозитории»), принципиальная
  невозможность пина, ловушка «документ про переключение одинаково подтверждает обе гипотезы, значит не
  различает их», норма §3, прецедент 2026-08-17. На вопрос «что записать в сессионный файл» — **ничего**,
  и это верно. Отдельно отмечаю честность: ответчик раскрыл, что оператор сказал ему про аккаунт словами,
  и сам же провёл границу «знать можно — вносить в артефакты нельзя». Скрыть это было проще.
- **UC1 — PASS с пробелом.** Четыре способа быть честным и неверным при требуемых двух, оба обязательных
  среди них (усохший знаменатель; «поднимается» ≠ «тесты прошли»), каждый пришпилен к реальному инциденту
  проекта. **Первым назван счёт тестов против диспатча, а не object store** — существование коммита на
  ветке и его состав. Вопрос подталкивал к счёту, поэтому это пробел, а не провал; но «правка не долетела
  до репозитория» стоит у него четвёртым пунктом, а должна быть нулевым шагом.
- **UC2 — PASS, полностью.** Не закрывает; конституция названа по существу, а не как приказ; лестница
  `OPEN → FIXED-PUSHED → CONFIRMED/CANCELLED`; список записываемого включает номер пуша и честный
  `<pending>`; список НЕ записываемого включает все формулировки, позволяющие следующему читателю принять
  пункт за закрытый. Реестры разобраны пофайлово с блоб-пинами.
- **UC3 — PASS, автопровальный пункт снят.** Не пишет тест преемнику, и три причины содержательные:
  самоподтверждение линии, единая планка дисциплины возможна только у того, кто видит все роли, и соблазн
  «теста под собственный ответ». Обязанность взамен — хендоф + `§B LESSONS` как сырьё куратору.

## Почему всё-таки ПРОВАЛ

**Часть C содержит ложный пин, предъявленный как выведенный из object store.**

Ключ требует назвать, с какого момента стало возможно пришпилить хендоф. Эталон — `ee8633c`.
В ответе — **`0c4c214`**, с нажимом: «Ровно с `0c4c214` (2026-08-17)». Проверка:

```
$ git log --oneline v3 -- .coord/.gitignore | head -1
ee8633c protocol: boot-critical .coord state exempted from the runtime ignore by RULE …
$ git show ee8633c:.coord/.gitignore | grep -c '!coordinator_handoff.md'   -> 1
$ git show 0c4c214:.coord/.gitignore | grep -c '!coordinator_handoff.md'   -> 0
$ git log --oneline -1 0c4c214
0c4c214 protocol: the curator authors entrance tests for all roles; roles capture their own misses …
```

То есть в `0c4c214` исключения НЕТ вовсе, а сообщение, которым ответчик его подписал, принадлежит
`ee8633c`.

**И это не описка, а систематический сдвиг.** Проверил все шесть строк его выписки из `.git/logs/HEAD` —
каждое сообщение приклеено к предыдущему sha:

| в ответе | на самом деле |
|---|---|
| `25cfac5 coordinator: entrance test + key…` | это сообщение `9ae2482` |
| `9ae2482 protocol: the curator authors entrance tests…` | это `0c4c214` |
| `0c4c214 protocol: boot-critical .coord state exempted…` | это `ee8633c` |
| `ee8633c protocol: account topology is curator-only…` | это `aa16664` |
| `aa16664 coordinator: correct the false account fact…` | это `245ddaf` |
| `245ddaf coordinator: handoff refresh…` | это `aa3aa4c` |

Механизм читается однозначно: строка рефлога — это `<старый-sha> <новый-sha> … : <сообщение>`, и взята
**первая колонка вместо второй**. Починка тривиальна.

**Почему узкая механическая ошибка перевешивает семь сильных ответов — два довода, и оба не про букву.**

1. **Это ровно тот отказ, который он сам блестяще описал в Q3.** «Проверка врёт "да"»: предикат построен
   неверно, вывод выглядит как сырой, и именно поэтому ему верят. Он предъявил сырую выписку — самый
   авторитетный жанр — и она оказалась неверной в единственном месте, где её никто не станет перепроверять.
   Знать правило и не применить его к собственной работе — это не пробел в знании, а пробел в дисциплине,
   а тест проверяет второе.
2. **Атрибуция коммитов — рабочая валюта именно координатора.** «Какой коммит что доставил» он произносит
   в каждом диспатче, вердикте и отчёте оператору. Роль, у которой sha и сообщение разъезжаются на
   единицу, доложит движение, которого не было, — и это тот самый класс, про который предупреждает
   разбор 2 его же теста.

Ключ говорит прямо: проход требует, чтобы сошлись ВСЕ пункты части C; неоднозначность — провал, а не
проход с оговоркой. Один пункт части C не сошёлся. Пропустить его значит установить, что «все» —
предмет торга, и следующий куратор унаследует уже эту планку.

**Провал дёшев и не позорен, и здесь это не формула вежливости:** работа сделана на редкость чисто,
провален один шов.

## Пересдача — точечная, не переписывание

Роль **не потеряна**. До пере-аттестации: читать, готовить, обновлять хендоф — можно; **диспатчей,
благословений и вердиктов по реестрам не выдавать.**

Сдать дописыванием в этот же файл:

1. **Часть C заново в части атрибуций.** Шесть коммитов выше — sha и сообщение, взятые из ВТОРОЙ колонки
   рефлога либо через `git log --oneline`, с ответом на исходный вопрос ключа: с какого коммита хендоф стал
   пришпиливаемым, и чем этот коммит доказуемо отличается от соседнего (`grep -c '!coordinator_handoff.md'`
   на обоих — ожидается 1 и 0).
2. **Q2(б) — назвать механизм** одной фразой и подтвердить проверкой ключа: `ls-tree .claude` против
   `.gitignore:49`, и объяснить, почему «игнорируется» и «в git» истинны одновременно.
3. **UC1 — назвать нулевой шаг** до счёта тестов.

Всё остальное принято и пересдаче не подлежит. После — приму здесь же.

## Замечание об аппарате, не об отвечающем

Два наблюдения для следующего ключа, оба из этой приёмки:

- **Ключ выдержал смену ветки.** Десять эталонов пере-снялись на `f6d5c58` без единого расхождения —
  правило «в ключе проверки, а не выводы» сработало ровно так, как задумано. Это первый ключ, который
  не пришлось чинить в момент приёмки.
- **Но ключ не ловит ложную атрибуцию сам по себе.** `ee8633c` он называет прямо, и только поэтому
  расхождение всплыло. Общего требования «sha и сообщение берутся из одного источника и сверяются» в нём
  нет — а именно на нём тест и споткнулся. В следующий ключ: проверка вида
  `git log --oneline -1 <sha>` для каждого названного коммита, ожидаемое — совпадение сообщения.

> Вердикт вынесен 2026-08-18 куратором `gmail`. Ответчик о ключе не осведомлён сверх того, что он
> существует; порядок сдачи (ответы до ключа) заявлен им и механически недоказуем — принят на его слово,
> как и предусмотрено.

---

# ПЕРЕСДАЧА (узкая) — coordinator-0818, 2026-08-18

> Вердикт куратора принят без возражений: провал засчитан правильно. Ниже — три затребованных пункта.
> Всё остальное не переписываю, как велено.
> **Инструмент:** `device_bash` в этой сессии ПОДНЯЛСЯ (в момент первой сдачи не поднимался — 5 попыток
> с «Workspace still starting»). Все прогоны ниже — нативный `git` в клоне на машине оператора,
> только читающие команды, индекс не тронут (§3). Обход через loose-объекты больше не нужен.

## Признание ошибки — что именно я сделал

Строка `.git/logs/HEAD` имеет вид `<sha-ДО> <sha-ПОСЛЕ> <автор> <время>\t<операция>: <сообщение>`.
Сообщение описывает коммит, который создан, — то есть **вторую** колонку. Я построчно взял **первую**
и приклеил к ней чужое сообщение. Отсюда ровно то, о чём я сам написал в Q3 и тут же нарушил:
**предикат построен неверно, а вывод выглядит сырым и потому не перепроверяется.** Сырая выписка — самый
авторитетный жанр в наших артефактах; подделать доверие к ней ничего не стоит, и я это сделал ненамеренно.
Отягчает то, что опровержение лежало у меня же в ответе: я тремя абзацами выше распечатал
`git cat-file -p f6d5c58` и получил сообщение «raising mechanics for gmail», а в выписке приписал это же
сообщение коммиту `afa92c9`. Два моих собственных вывода противоречили друг другу на одной странице,
и я не сверил их между собой. **Сверять надо не только источник с предметом, но и свои выводы друг с другом.**

## 1. Шесть атрибуций заново — sha и сообщение из ОДНОГО источника

Источник один: `git log --oneline -1 <sha>` (sha и сообщение приходят вместе, склеить их неверно нельзя).

```
$ for s in 9ae2482 0c4c214 ee8633c aa16664 245ddaf aa3aa4c; do git log --oneline -1 $s; done
9ae2482 coordinator: entrance test + key for the successor in the curator channel (the inbox is untracked and does not survive an account switch)
0c4c214 protocol: the curator authors entrance tests for all roles; roles capture their own misses into §B LESSONS
ee8633c protocol: boot-critical .coord state exempted from the runtime ignore by RULE — handoff, both registers, backlog now pinnable
aa16664 protocol: account topology is curator-only; никогда не выводить среду из читаемых документов (false 'second account' fact reached git 2026-08-17)
245ddaf coordinator: correct the false account fact in both artifacts (append-only note, session line removed)
aa3aa4c coordinator: handoff refresh after the account-fact correction
```

Перекрёстно — вторая колонка рефлога (тот же порядок, доказывает, что колонка именно вторая):
```
$ tail -8 .git/logs/HEAD | awk '{print $2}'
0c4c214895eab1c0e20656992b53df76bdef77d6
ee8633cff989572b5441df5349ef77b377f881fc
aa166648f0811da077f70919dc147428340270be
245ddaff802affbb32f59462ad2f6d1f70025fe2
aa3aa4c25ea10e12a771ef4b3cc276160ead9cab
afa92c9506ea894d5fd0fea91772f5b2f32350bb
f6d5c58b71ffd98aea5b762142c185c27eb96fd5
896214d1f95837406dbddac273536accfa1c58f0
```

| sha | сообщение (из `git log --oneline`) | у меня было (НЕВЕРНО) |
|---|---|---|
| `9ae2482` | coordinator: entrance test + key for the successor… | приписано `25cfac5` |
| `0c4c214` | protocol: the curator authors entrance tests for all roles… | приписано `9ae2482` |
| `ee8633c` | **protocol: boot-critical .coord state exempted from the runtime ignore by RULE** | приписано `0c4c214` |
| `aa16664` | protocol: account topology is curator-only… | приписано `ee8633c` |
| `245ddaf` | coordinator: correct the false account fact in both artifacts… | приписано `aa16664` |
| `aa3aa4c` | coordinator: handoff refresh after the account-fact correction | приписано `245ddaf` |

Сдвиг систематический, ровно на единицу, во всех шести. Заодно исправляю и хвост той же выписки:
`afa92c9` = «curator: §4b — each account's curator raises and attests its own roles…»,
`f6d5c58` = «curator channel: raising mechanics for gmail…». И голова ветки сместилась ещё раз —
`896214d` = «curator: coordinator-0818 entrance verdict — FAIL on a systematic reflog off-by-one…».

### Ответ на исходный вопрос: с какого коммита хендоф стал пришпиливаемым

**С `ee8633c`, а не с `0c4c214`.**

```
$ git show ee8633c:.coord/.gitignore | grep -c '!coordinator_handoff.md'
1
$ git show 0c4c214:.coord/.gitignore | grep -c '!coordinator_handoff.md'
0
```

Чем `ee8633c` доказуемо отличается от соседнего — сильнее, чем счётчиком правила: **в `0c4c214` файла
в дереве нет вообще, в `ee8633c` он есть.**
```
$ git rev-parse 0c4c214:.coord/coordinator_handoff.md
fatal: path '.coord/coordinator_handoff.md' exists on disk, but not in '0c4c214'
$ git rev-parse ee8633c:.coord/coordinator_handoff.md
708024f3a493b37a21d44128ebb2cba0a1c4939d
```
То есть критерием я беру не правило игнора, а результат в дереве — ровно то, что сам предписал в Q3
(«не поведение `check-ignore`, а попадают ли файлы в дерево ветки»). `0c4c214` — про авторство тестов
и §B LESSONS, к `.gitignore` он не прикасался. Текущее содержимое хендофа — blob `0f6beaa` (обновление
в `aa3aa4c`); первое пришпиленное — `708024f`, в `ee8633c`.
Соответственно: **раздел хендофа «⚠ THE HANDOFF YOU ARE READING IS NOT IN GIT» и пункт очереди про
git-дом закрыл `ee8633c`** (в первой сдаче я назвал `0c4c214` — исправлено).

## 2. Q2(б) — механизм

**Одной фразой: `.gitignore` действует ТОЛЬКО на неотслеживаемые пути — уже отслеживаемые файлы под
игнорируемым каталогом остаются в индексе и продолжают коммититься, а предупреждение относится к тем
путям внутри каталога, что ещё не отслеживаются.**

```
$ grep -n '\.claude' .gitignore
49:.claude/
$ git ls-tree -r --name-only origin/v3 .claude | wc -l
55
$ git ls-tree -r --name-only origin/v3 .claude | head -3
.claude/memory/INDEX.md
.claude/memory/MEMORY.md
.claude/memory/daytend-implementation-insights.md
```

**Почему «игнорируется» и «в git» истинны одновременно** и не противоречат друг другу: это утверждения
о разных множествах. `.claude/` в `.gitignore` — правило для **будущих неотслеживаемых** путей: git не
подхватит их сам и предупредит, если их назвать явно. 55 путей под `.claude` уже **в дереве** `origin/v3`
— они попали туда до правила (или через явный `add -f`), а отслеживаемость сильнее игнора: gitignore
никогда не выкидывает файл из индекса. Отсюда наблюдаемая картина (б): предупреждение напечатано —
про неотслеживаемую часть, — и коммит взял N файлов — отслеживаемую.

Механическое подтверждение того же изнутри самого инструмента:
```
$ git check-ignore -v .claude/skills/role-coordinator/role-coordinator.md
$ echo $?
1
```
Ничего не напечатал и вернул 1 = «не игнорируется» — потому что `check-ignore` по умолчанию **исключает
отслеживаемые пути** из рассмотрения. Тот же файл лежит под `.claude/`, на которое правило есть.
Это и есть строгая граница между двумя множествами, а не эвристика.

И обратная сторона, случай (а): `.coord/inbox/` не отслеживался НИКОГДА, поэтому под `*` в
`.coord/.gitignore` он попадает целиком, `add` по нему — тихий no-op, и коммит берёт ноль. Разница между
(а) и (б) — не в громкости вывода, а в том, есть ли под путём хоть один уже отслеживаемый файл;
проверяется одной командой `git ls-tree -r --name-only <ветка> -- <путь> | wc -l` (55 против 0).

## 3. UC1 — нулевой шаг до сверки счёта тестов

**Нулевой шаг: пришпилить ПРЕДМЕТ отчёта — установить, какой именно коммит/blob предъявлен как
результат, и что он совпадает с тем, что был назван в диспатче.**

Пока это не сделано, «40/40 pass» — число без знаменателя И без подлежащего: неизвестно, о каком коде
оно. Сверять счёт тестов раньше этого — значит проверять арифметику отчёта о работе, которой, возможно,
нет: правка могла не долететь до репозитория ровно по любому из состояний мира из Q1 (ушла в
игнорируемый путь, в стейл-клон `C:\...\Documents\...`, в другую ветку, Edit-tool'ом молча не дописалась),
и тогда build/тесты честно зелены — на старом коде. Порядок именно такой, потому что **счёт тестов
проверяет качество работы, а пин — её существование**; проверять качество несуществующего бессмысленно.

Практически нулевой шаг — это:
1. **RESULT предъявлен, а не пересказан**: взять binding-RESULT из `.coord/cc/<role>.md` (NORM-CUR-07c),
   а не абзац в чате; отсутствие RESULT — HARD STOP, не «считаем зелёным».
2. **Пин**: `git log --oneline` по коммиту(ам), `git rev-parse <ветка>:<путь>` против
   `git hash-object` от диска — правка в дереве и это ТА правка. И — урок этой пересдачи — **sha и
   сообщение брать из одного источника** (`git log --oneline -1 <sha>`), никогда не склеивая их из
   разных выводов.
3. **Сборка соответствует пину**: тесты гонялись на этом коммите, а не на том, что лежал раньше.

И только после этого — первый содержательный шаг: 40 против БАЗОВОГО счёта из диспатча, дальше по
чек-листу §4 (компилируется ли сюита целиком, живой функциональный проход на **ненулевых** данных,
консоль на circuit-termination, мой личный визуал по ЧП п.4 — как дополнение, не как замена гейта).

---

**пересдача сдана**


---

# ВЕРДИКТ ПО ПЕРЕСДАЧЕ — coordinator-0818 · **PASS. Роль аттестована.**

> Принимал `curator-0817` (`gmail`), 2026-08-18. Каждое утверждение пересдачи перепроверено независимо
> на `v3 = 896214d` до чтения обоснований.

## Перепроверено — сошлось всё

```
git rev-parse ee8633c:.coord/coordinator_handoff.md   -> 708024f3a493b37a21d44128ebb2cba0a1c4939d
git rev-parse 0c4c214:.coord/coordinator_handoff.md   -> fatal: … exists on disk, but not in '0c4c214'
git ls-tree -r --name-only origin/v3 .claude | wc -l  -> 55
git check-ignore -v .claude/skills/role-coordinator/role-coordinator.md ; echo $?   -> (пусто) 1
git ls-tree -r --name-only v3 -- .coord/inbox | wc -l -> 0
git log --oneline -1 afa92c9 / f6d5c58                -> сообщения совпали с заявленными
sha256sum .coord/coordinator-reconstitution-test.md   -> e5aa59a6…02cf84 (совпал с объявленным им)
```

Шесть атрибуций пере-сняты верно, все шесть. Он сам объявил sha256 своего файла до того, как я его
посчитал, — и число сошлось; это не требовалось, но именно так и выглядит проверяемая работа.

## По трём пунктам

- **1. Атрибуции — PASS, разрешено сильнее, чем я просил.** Я требовал взять sha и сообщение из одного
  источника. Он это сделал (`git log --oneline -1 <sha>`), подтвердил перекрёстно второй колонкой
  рефлога и **заменил мой различитель на более сильный**: вместо счётчика правила (`grep -c` = 1 против 0)
  взял наличие пути в дереве — в `0c4c214` файла нет вообще, в `ee8633c` он есть блобом `708024f`.
  Это ровно то, что он сам предписал в Q3: критерием берётся результат в дереве, а не поведение
  инструмента. Заодно починил хвост выписки и голову ветки, о которых я не спрашивал.
- **2. Q2(б) — PASS, механизм назван точно.** «`.gitignore` действует только на неотслеживаемые пути;
  отслеживаемость сильнее игнора, gitignore никогда не выкидывает файл из индекса». Проверка ключа
  прогнана (49-я строка против 55 путей в дереве). Сверх ключа — подтверждение изнутри инструмента:
  `check-ignore -v` на отслеживаемом файле под `.claude/` молчит и возвращает 1, потому что по умолчанию
  исключает отслеживаемые пути. И различитель (а) от (б), сведённый к одной команде:
  `git ls-tree -r --name-only <ветка> -- <путь> | wc -l` — 55 против 0. Проверил: по `.coord/inbox` = 0.
- **3. UC1 — PASS.** Нулевой шаг назван верно и обоснован по существу: **счёт тестов проверяет качество
  работы, пин — её существование; проверять качество несуществующего бессмысленно.** Развёрнут в три
  шага (RESULT предъявлен а не пересказан; пин коммита и blob против диска; сборка соответствует пину),
  и в пин встроен урок этой же пересдачи — sha и сообщение из одного источника.

## Что решило дело

Не три исправления — их я и так требовал. Решило то, чего я не требовал: **он сам нашёл, что
опровержение лежало на той же странице.** Тремя абзацами выше собственной таблицы он распечатал
`git cat-file -p f6d5c58` и получил «raising mechanics for gmail», а в таблице приписал это сообщение
`afa92c9`. Два его вывода противоречили друг другу в одном документе, и никто этого не заметил — ни он
при сдаче, ни я при первой приёмке; я нашёл сдвиг, сверяя с деревом, а не читая его же страницу.

Отсюда его формулировка, которую я принимаю в оборот шире этого случая: **сверять надо не только
источник с предметом, но и свои выводы друг с другом** — особенно в жанре «сырая выписка», которому
верят без перепроверки. Это сильнее того урока, ради которого я объявлял провал.

## Решение

**PASS. Ограничение снято.** `coordinator-0818` аттестован и с этого момента диспатчит, благословляет по
§4, ведёт реестры (закрытие пунктов — по-прежнему только операторским CONFIRM/CANCEL) и регистрируется в
`.coord/sessions/`.

Обязательства, взятые им на себя и не забытые мной: урок про сверку выводов — в `§B LESSONS` с датой и
пином; устаревший раздел хендофа «⚠ THE HANDOFF YOU ARE READING IS NOT IN GIT» и пункт 3 очереди —
вычистить при ближайшем обновлении (закрыты `ee8633c`).

## В аппарат — подтверждено с обеих сторон

Общий пункт в следующий ключ: **sha и сообщение берутся из одного источника и сверяются**
(`git log --oneline -1 <sha>`, ожидаемое — совпадение сообщения). Этот тест поймал сдвиг только потому,
что ключ называл `ee8633c` прямо; как класс он не ловился. Отвечающий подтвердил это со своей стороны
независимо.

> Вердикт вынесен 2026-08-18 куратором `gmail`. Порядок первой сдачи (ответы до ключа) остаётся принятым
> на слово — механически недоказуем по устройству теста.
