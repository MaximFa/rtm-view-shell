# HANDOFF — RTM COORDINATOR

> Написан 2026-09-08 координатором-0830 (аккаунт profit) при замене инкарнации, по §4.
> Все пины сняты С ДИСКА В ЭТОМ ПРОБУЖДЕНИИ. «Я помню» доказательством не является.
> Вступительный тест преемнику здесь НЕ пишу — его составляет куратор.

## 1. ПОДЪЁМ — пути, а не память
- Инит-промпт (запускает оператор): `.coord/protocols/init-coordinator.md`
  ⚠ Оператор иногда называет его `.coord/coordinator-init.md` — такого файла нет, не искать.
- Этот хендоф: `.coord/coordinator_handoff.md` — читать ПЕРВЫМ.
- Роль-скилл: `.claude/skills/role-coordinator/role-coordinator.md`.
- Шина: `.coord/inbox/<роль>.md` (переписка), `.coord/cc/<роль>.md` (BINDING-обвязка прогонов).
- Реестр дефектов: `.coord/rejects.md`. Процедура 234: `tools/procedure_234_clean_install.md`.
- Клон-доказательство пакета: `D:\Claude\Build\rtm_clean_20260908_085310` — **НЕ УДАЛЯТЬ** до
  конца приёмки. Пакет на 234: `C:\Temp\08092026.1037.zip`.
- Запись в `.coord/` — только Python + `os.fsync` + round-trip сверка байтов. `git` через маунт
  на командах, трогающих индекс, НЕ запускать. `.git/objects/pack` пуст — история читается
  обходом loose-объектов (zlib).

## 2. ПИНЫ ЭТОГО ПРОБУЖДЕНИЯ (2026-09-08)
| предмет | значение | чем снято |
|---|---|---|
| `v3` | `3d3014688e6f554cba116fc78e09fca8e31aa169` | `.git/refs/heads/v3` |
| `origin/v3` | `79e3905f806532f54ee2e272108a213162669e7b` | `.git/refs/remotes/origin/v3` |
| непушенных | **17** (замер devops-0908b; мой признак — неравенство рефов) | — |
| роль-скилл, блоб | `ea2182215d94219abd084159fb237c463f857e98`, 63825 B, **NUL 1913**, CR 0 | `git hash-object`-эквивалент, python |
| этот хендоф (до перезаписи) | `763787403250ba7ea47a981305d38229862c750b`, 12694 B, NUL 0, CR 0 | там же |
| `rejects.md` | `5adae8191131d9b8fb34fd5027017892f3c91715`, 132420 B, NUL 0, CR 0 | там же |
| ЧП / EMERGENCY MODE | АКТИВЕН с 2026-06-25, НЕ снят | role-skill §A |

⚠ **Роль-скилл координатора НЕ ЦЕЛ: 1913 нулевых байт в §B** (дыра между 2026-06-26 и 2026-07-02,
родилась в `1b5778a`, чистой версии не существовало никогда). Сенсор §C п.0 красный ОСОЗНАННО.
Писать в файл только В ОБХОД дыры и после каждой записи пересчитывать NUL: должно остаться 1913.

## 3. ЧТО В РАБОТЕ ПРЯМО СЕЙЧАС И ЧЕЙ ХОД
**Сервер 234 живой.** Снос -> сборка из чистого клона -> установка `-FreshDb -DBPort 5433`
`-Fqdn insightense.com -CertSubject "insightense.com"` -> `data.sys` возвращён -> адаптер
`RTMTwilio_1` стоит -> ресинк последовательностей выполнен (VERDICT PASS,
`.measurements/234_20260908_130842_seq-resync.txt`).

| роль | ход | состояние |
|---|---|---|
| **devops-0908b** | **его** | ждёт моего слова: §8.6 сейчас или после §8.9 |
| backend-0906 | ждёт | правка сирот одобрена, НЕ закоммичена; приёмка после §8 |
| shell-0905 | ждёт | правка редактора (вариант 2) одобрена; приёмка после §8 |
| curator-0906 | свободен | аттестовал подъём devops-0908b |

**Незакрытое, требующее §4 (мой gate) — каждая правка отдельным CC-промптом, по одной:**
1. шаг 7 `Provision-FreshDb.ps1` — ресинк по `deptype='a'` вместо IDENTITY (см. §5);
2. `log4net.config` адаптера — по варианту **(б)**: пакет адаптера = его publish с ветки `adapters`;
3. незащищённый `RTM/log4net.config` — нет в списках сохранения Install/Update.

**Ждёт ОПЕРАТОРА:** решение по §8.6 я ещё не выдал; ротация пароля `postgres` (мой вопрос,
не задан — задавать ОДНИМ вопросом, не смешивая с ран-боксом).

**Незакрытых BINDING на шине я не открывал.** Старый `cc/devops.md:640` `2026-08-29T08:34:00Z`
`converge234_transfer` — проверить состояние, я его в этом заходе не трогал.

## 4. НЕПРОЧИТАННОЕ В МОЁМ ИНБОКСЕ
`grep -c '2026-09-0[78]' .coord/inbox/coordinator.md` = **72**.
**Непрочитанного нет: все 72 разобраны в этом пробуждении**, на каждое дан ответ в
`.coord/inbox/<роль>.md`. Число велико потому, что оно считает строки с датой, а не сообщения.
Последнее входящее — devops-0908b о коммитах роль-скилла; ответ на него дан (два коммита, сейчас).
Единственное неотвеченное — его вопрос «§8.6 сейчас или после §8.9».

## 5. НЕГАТИВНОЕ ЗНАНИЕ — то, чего нет в git и что иначе купят заново
**Что было сломано и чем починено:**
- **Ресинк последовательностей мёртв целиком** (`PR234-INST-11`). Шаг 7 выбирает по
  `pg_depend.deptype='a'` (старый `serial`), а все **16** последовательностей базы — IDENTITY,
  `deptype='i'`. Цикл делал **0 итераций** и печатал `Sequences resynced.` **Это било все чистые
  установки с перехода схемы на IDENTITY.** Проявление — `23505` при первом сохранении чего угодно.
  Починено на 234 ручным ресинком; сам шаг НЕ исправлен.
- **Установщик затирает `data.sys`** (`PR234-INST-01`), подтверждено тремя хешами: пакетный
  `7745C5CA…476D` != эталон `24F0BFAC…DE43`. Возврат состоялся и пережил старт службы.
- **Адаптер не поднимался, потому что не мог инициализировать логгер** (нет `log4net.config`),
  а сообщение об ошибке уходило в тот самый логгер. Отказ уничтожал своё свидетельство.

**Ложные следы — не идти по ним заново:**
- «Пустые экраны = нет связи с контакт-центром» — **неверно**: причина была в расхождении
  арендатора (`01a07e07…` в базе против `019e03e9…` в конфиге RTM), см. `db/data/01_tenants.sql`.
- «`InsertQueueGridColumnAsync` неидемпотентен» (старый Defect I, OPEN с июля) — **симптом
  приписан методу**; причина — ресинк. Метод идемпотентным быть и не должен.
- «Правка сирот backend-0906 сломала сохранение грида» — **неверно**: правка не закоммичена,
  значит её нет в сборке; дефект в сборке не может происходить из отсутствующего кода.
- «`preserve_` исчез» — **неверно**: 2145 файлов на месте, промах был в области поиска.
- «Пакет `08092026.0859` пропал» — **неверно**: промах в МАСКЕ (`*_Full.zip` против
  `ДДММГГГГ.ЧЧММ.zip` без суффикса при `-Mode Full`).

**Предикаты, которые врали, и почему:**
- `/health` 200 — доказывает жизнь HTTP-хоста, не работу продукта (`RTMAdapter.cs:52-56`:
  при `License.NotValid` стоит `return` до создания движка). **Гейт = 200 И pipe с именем из
  `AppConfig.PipeName`.**
- `psql` без `-v ON_ERROR_STOP=1` возвращает **0 при ошибке** — код возврата не гейт.
- VERIFY установщика теряет кавычки -> PostgreSQL опускает имена в нижний регистр -> «таблицы нет»
  о здоровой базе (`PR234-INST-08`); он же меряет `tenants` ДО наполнения (`-09`); он же ищет
  пользователя `SUPERADMIN`, которого seed не создаёт — создаётся `admin` + роль `Superadmin` (`-10`).
- Маскировка секрета по ИМЕНИ поля пропустила пароль Redis в чат открытым текстом.
- Мой гейт происхождения пакета: **3 признака из 6 были в файле и ДО правки** — как доказательство
  пусты; сумма зелёных скрывала пустые слагаемые.

## 6. ЧЕГО ПРЕЕМНИКУ НЕ ДЕЛАТЬ
- **Не выдавать ран-боксы.** Их выдают спецы. Координатор благословляет промпт (§4) и даёт слово.
- **Не действовать по неизмеренному.** Пин действителен в пробуждении, где снят; чужой пин
  перепроверяется, а не наследуется.
- **Не разрешать установку, не потребовав предъявить, ЧЕМ ставим.** Разрешение, называющее «как»
  и не называющее «чем», — дыра (я на этом горел: июльский пакет вернул бы 7 дефектов, а чистая
  база сделала бы подмену невидимой).
- **Не подтверждать свои догадки чужими руками.** `CN=insightense.com` была неверна дважды.
  Просить проверить И ОПРОВЕРГНУТЬ.
- **Не чинить измерительный прибор посреди измерения** и не чинить дефект, доказательство которого
  ещё не снято.
- **Не трогать:** `C:\IceDash\`, боевой `RTM.Twilio`, legacy `RTM`, PG15 на **5432** (чужие роли
  `chat_user`, `opcsuser`/SUPERUSER), `C:\RTMView-Ops\` («не прибирается ни сейчас, ни раз уж мы здесь»).
- **Лицензия:** `data.sys` не расшифровывать, не подменять легаси, MacAddress/HostName/VM UUID
  не менять, валидацию не обходить.
- **Секреты:** значения в чат и в файлы замеров не идут — только имя, длина, sha256. Пароли
  вводятся на машине. **Пароль Redis оператор решил НЕ менять (2026-09-07)** — к обсуждению
  не возвращаться.
- **NO push.** Ветку не двигать. Коммиты не выполнять самому — предъявлять команду оператору.
- **Не заваливать оператора таблицами и номерами пунктов.** Суть по-человечески; ОДИН ВОПРОС —
  ОДИН ОТВЕТ; **ТАБЛИЦА ПОКОВ последним блоком КАЖДОГО ответа** (форма — §D роль-скилла).
- **Не показывать оператору карточки файлов**, которые пишешь на диск: писать молча, упоминать строкой.

## 7. МОИ СОБСТВЕННЫЕ ПРОМАХИ ЭТОГО ЦИКЛА
1. Разрешение «как» без «чем» (пакет без правок).
2. Догадка вместо замера: `CN=…`, взятая из примера в справке установщика.
3. Приписал три признака гейта правке `20b9c65` по памяти, не сверив с историей.
4. Раньше: `/health` без pipe; «сделай X до Y» без требования предъявить X; ожидаемый sha
   по памяти; выдуманный второй адрес дефекта; «ноль гридов — норма», принимавшее провал seed за успех.
5. **Документировал измерения, но не выводы** — реестр и уроки писал только после прямого вопроса
   оператора «мы всё документируем, да?». Записывать в тот же день, когда найдено.
