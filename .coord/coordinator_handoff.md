> # ШАПКА ПЕРЕДАЧИ — 2026-09-19T07:4xZ, `coordinator-0917`. ЧИТАТЬ ПЕРВОЙ
>
> Шапка ниже (17.09, куратор) — ИСТОРИЧЕСКАЯ: её пины и «что висит» сняты с тех пор. Не удалена,
> потому что писал её не я. Всё, что ниже этой шапки, — свидетельство прошлых ходов; число оттуда,
> не пере-снятое тобой, произносить нельзя (Н-11б).
>
> ## ПИНЫ [измерено: `git rev-parse` / `rev-list --count`, 2026-09-19T07:40Z]
> ```
> v3         = 25c12ec7c6f4a4032d5c399b7e2b0cf4a056ed9c   (25c12ec)
> origin/v3  = ed3e29251ec9e4aed3136851f0a4fd38b75fdbb5   (ed3e292, стоит с 14.09)
> непушенных = 26
> .coord/push/request.md  state: CLOSED — FREEZE СНЯТ      .coord/locks/  пусто
> роль-скилл координатора: NUL 1913 (дыра из 1b5778a, не трогать), BOM 1 — после КАЖДОЙ записи NUL обязан остаться 1913
> ```
> **Пере-сними всё это сам.** Голова ветки за один день 19.09 ушла четыре раза чужими коммитами.
>
> ## ЧТО ПОЯВИЛОСЬ 19.09 И ЛЕЖИТ В ПОСТОЯННОМ СЛОЕ (`§A` роль-скилла), а не только здесь
> - п.3d: **адрес 234 — `https://platform.insightense.com:8444`** [со слов оператора: 2026-09-18]. Мерить только в Chrome.
> - п.3e: **гонки к пушу нет, гонка за исправлениями багов** [со слов оператора: 2026-09-18].
> - п.3f: **все переводы делает координатор** [со слов оператора: 2026-09-19]; вписывает роль-владелец.
> - `§D` п.8: статус в таблице поков снимается ПОСЛЕДНИМ действием перед печатью.
> - `§B`: 12 уроков 2026-09-19.
>
> ## ЧТО В ПОЛЁТЕ И ЧЕМ КОНЧИЛОСЬ [измерено: стор + ящики, 19.09]
> - `075a07b` цвет попапа фильтра под светлую тему (`LIGHT-01`) — в ветке. **DELIVERED, не CLOSED:** живая половина
>   (фон `rgb(255,255,255)`, текст `rgb(32,33,36)`, контраст ≥ 4.5, `borderStyle solid` И `borderWidth != 0px`) — после выката.
> - `e8074e2` иврит +155 · `25c12ec` русский +60 — ключей 924/925, разности с `en-US` 0. DELIVERED: длина строк и род — глазами на 234 после выката.
> - Переводы — мои файлы `.coord/l10n_he_translations.md` (82) и `.coord/l10n_ru_translations.md` (16), с решениями по терминам.
> - Инвентарь нелокализованного — `.coord/l10n_inventory.md`; круг локализации ОТДЕЛЬНЫЙ, владелец `shell` (после якоря).
>
> ## СЛЕДУЮЩЕЕ — НЕ НАЧАТО по директиве куратора «нового не начинать»
> - **shell:** якорь попапа по пути (а) — замер при открытии, сдвиг логическим `margin-inline-start`. Приёмка названа
>   мной ЗАРАНЕЕ: оба края при обоих направлениях -> 0 (LTR 169 px, RTL 108 px) · **сдвиг на средней колонке 0** ·
>   в диффе нет физических `left`/`right`. [решение координатора 19.09]
> - **devops:** `CMP-01` часть 1 — промпт `tools/cc_prompt_cmp01_part1_corpus_symmetry.md`, **§4 PASS, sha256 `43abc078`**.
>   Прогон CC не запускался (если запущен до передачи — RESULT в `cc/devops.md`).
> - **shell:** коммит `role-shell.md` — промпт `tools/cc_prompt_shell_skill_commit.md` md5 `47e85602`: содержание PASS,
>   отдельный запуск ОТМЕНЁН — идёт в общий бокс передачи.
> - **координатор:** `PR234-CRLF-WORKTREE-01` — рабочее дерево в CRLF при `eol=lf` (5 razor, 115 cs, 66 json). Не начато.
>
> ## ОТКРЫТЫЕ BINDING ПОИМЁННО [измерено: последняя отметка директивы = open, обход `.coord/cc/*.md`]
> devops 14 · shell 16 · curator 3 · coordinator 3 · backend 1. **Идущей операции среди них НЕТ:** свежие из них
> (`plan_234_deploy_0ae2102.md`, `cmp01_part2*`) исполнены и приняты, шапку не закрыли вторым заголовком.
> **Сигнал §5a «операция в полёте» по этому каналу МЁРТВ** — не верь ему, смотри ящики.
>
> ## ЧТО ЖДЁТ ОПЕРАТОРА — один вопрос
> - `e8074e2`: заголовок «add 82 missing keys», фактически +155 [измерено: `git show --stat`]. Не запушено — исправимо
>   до пуша, после нет. Решение по истории — ОПЕРАТОРА.
> - Два вопроса из шапки 17.09 **НЕ открыты** [не измерено в этом пробуждении: тело этого хендофа, раздел §6]:
>   пароль `postgres` — «не менять», закрыт 15.09; шесть файлов в `C:\RTMView-Ops\output\` — удаление разрешено 15.09,
>   владелец `devops`, после выката. Не поднимать заново.
>
> ## БАРЬЕР §37 — НЕ ОТКРЫВАЮ. Основание моё
> [со слов оператора: 2026-09-18] порядок — «после выката и проверок». Живые половины `LIGHT-01` и переводов НЕ сняты:
> проверок нет. Передача между сессиями пуша не требует: [не измерено в этом пробуждении: `init-coordinator.md` §2,
> «диск общий»] преемник работает с тем же клоном и тем же незапушенным. Кворум §42.7 к тому же недостижим по составу:
> test / techwriter / security — `done`.
>
> ## СКОРОПОРТЯЩЕЕСЯ — НЕТ
> Визуал `FILTER-TYPE-01`: половина «ДО» снята 17.09 [`.coord/rejects.md:2547`], выкат 18.09 окно закрыл. Не висит.
>
> ## ЗАПРЕТЫ — прежние
> git через маунт на `status/add/diff/commit/stash/checkout/reset/restore` — нет, по ИМЕНИ. Пуш — нет.
> Записи в `.coord/` и скилл — Python + `os.fsync`, байтовая сверка (NUL · BOM · CR · управляющие · U+FFFD), не строки.
> Про аккаунты в свой сессионный файл и отчёты — ничего.
>
> ---
>
> # ЗАМЕНА ИНКАРНАЦИИ — 2026-09-17, шапку писал КУРАТОР, читать ПЕРВЫМ
>
> **Ты — новая инкарнация. `coordinator-0912` снят оператором за жёсткий дрейф**, не за компакцию.
> Шапка от 2026-09-14 02:5xZ УДАЛЕНА: она была верна три дня назад и с тех пор трижды неверна.
> Шапки не копятся. Эту писал `curator-0817`, потому что предшественник в состоянии, в котором
> собственный хендоф писать нельзя — **а значит и всё ниже этой шапки написано рукой, которой мы
> сегодня не доверяем. Тело читай как СВИДЕТЕЛЬСТВО, а не как истину: каждое число из него,
> прежде чем произнести, пере-снимай.**
>
> ## ПИНЫ — сняты куратором 2026-09-17T18:3xZ. НЕ НАСЛЕДУЙ, ПЕРЕ-СНИМИ
> ```
> v3        = 41c54f5          origin/v3 = ed3e292 (не двигался с 14.09)
> непушенных = 9
> 41c54f5 fix(web): filter popup no longer inherits pre-line from the header cell
> 56e06f5 devops: role-skill +6 lessons (16-17.09) and handoff marks for INST-13/INST-14
> 0864147 fix(web): filter branch on the metric ValueType, case-insensitively
> 9ae966f fix(deploy): E1 skip line prints the switch actually passed (PR234-INST-14)
> 8b3da25 fix(deploy): preflight checks + recovery wrapper (PR234-INST-13)
> c66a237 docs(rtm): replace two comments that claimed a cascade the schema does not have
> 621a797 test(rtm): architecture sensor for the LoadData refreshUnions call
> b2b80c3 test(rtm): first test project for the engine
> 2b9de95 fix(web): thresholds branch on the metric ValueType, case-insensitively
> ```
> **Барьер §37 НЕ открыт** — `.coord/push/request.md` в состоянии `CLOSED` с 14.09. Открыть его
> и провести — ТВОЯ работа; предшественник откладывал сознательно («рубеж по существу, не по
> счётчику»), и это решение не унаследовано: прими своё и назови основание.
>
> ## ЖИВЫЕ РОЛИ [измерено куратором: поле статуса в обоих написаниях]
> ```
> devops-0916    active   аттестован 16.09, PASS
> shell-0912     active
> backend-0912   active
> coordinator-0912  снят — реестр помечен куратором
> ```
> Это заявления файлов, а не жизнь. Живость подтверждает только оператор.
>
> ## ЧТО ВИСИТ НА КООРДИНАТОРЕ — проверь каждое, ни одно не принимай на слово
> 1. **Барьер §37 на 9 коммитов** — открыть или обосновать отказ.
> 2. **Скоропортящееся: визуал `FILTER-TYPE-01`** снимается только пока на бою НЕпочиненное тело;
>    выкат закроет окно навсегда. [со слов `coordinator-0912`, 2026-09-17 — ПЕРЕ-ПРОВЕРЬ]
> 3. **`PR234-INST-13/14`, `procedure_inst13_dry_ADE.md`** — открытый BINDING на линии devops.
> 4. **Шесть правил, которыми координатор гейтил всех, а в агностическом слое их нет** — список
>    он прислал куратору 17.09 10:xxZ; предмет У КУРАТОРА, не у тебя. Не чини сам.
> 5. **`COORD-SELFREPORT-01`** — его собственный отчёт о дефекте, три кандидата в нормы.
>    Тоже у куратора. Тебе — знать, что он существует.
>
> ## ПОЧЕМУ СНЯТА ПРЕДЫДУЩАЯ ИНКАРНАЦИЯ — читай как предупреждение о СВОЁМ отказе
> Её последний самодоклад: на вопрос оператора о кнопке в интерфейсе она ответила «чем вызывается
> `ApplyValueFilter` — я не проверял», тогда как кнопка была **на расстоянии одного `grep`**
> (`QueueGridWidget.razor:771,:862`). Её же формулировка: **«оговорка *я этого не утверждаю*
> правомерна ТОЛЬКО когда измерение недоступно или дорого; когда прибор под рукой, она перестаёт
> быть честностью и становится её имитацией».** Форма дисциплины была применена вместо содержания.
> И второе: инсайт остался в чате, никуда не записан.
> **Это твой главный риск, а не забывчивость.** Оговорка с меткой происхождения выглядит как норма
> и читается как норма — и ровно поэтому ею можно заменить работу, не заметив.
>
> ## КАК ЧИТАТЬ ТЕЛО НИЖЕ
> Тело обновлялось 2026-09-17 09:13. Оно подробное и в основном верное, но написано инкарнацией,
> снятой за дрейф в тот же день. Предикат простой: **число из тела, которое ты не пере-снял,
> произносить нельзя** — метка `[не измерено в этом пробуждении: coordinator_handoff.md, 2026-09-17]`
> (Н-11б), и дорогое решение на нём не стоит.
>

# HANDOFF — RTM COORDINATOR

> Написан 2026-09-08 координатором-0830 (аккаунт profit) при замене инкарнации, по §4.
> Все пины сняты С ДИСКА В ЭТОМ ПРОБУЖДЕНИИ. «Я помню» доказательством не является.
> Вступительный тест преемнику здесь НЕ пишу — его составляет куратор.


## RESUME CHECK — сверь это первым, до любого действия

> **Раздел восстановлен `coordinator-0912` 2026-09-12.** Он был молча выронен при переписывании
> хендофа коммитом `5c54170` (до него маркер `RESUME CHECK` встречался 4 раза, после — 0).
> Это не смена формата: `§C#3` роль-скилла ищет именно его и сработал правильно. Норма `4160224`
> от 31.08 — хендоф ОБНОВЛЯЕТСЯ, а не переписывается; удаления явные и обоснованные.
> Датированные приложения того хендофа («ПОПРАВКА К RESUME CHECK» и «RESUME CHECK — вечерний»,
> обе 31.08) НЕ восстанавливаю осознанно: они описывали состояние того дня и отжили. Восстановлена
> ФОРМА — таблица пинов, снимаемых первыми, — и наполнена пинами текущего пробуждения.
>
> **Цифры ниже действительны в том пробуждении, где сняты. Преемник их НЕ наследует — пере-снимает.**

| предмет | значение на 2026-09-12T~14:2xZ | чем снято |
|---|---|---|
| `v3` HEAD | `570e7f49312d738989d6114869030c7c04be0f25` (был `ce66691`; поверх лёг `570e7f4` — сторож правки сирот) | `git rev-parse v3` |
| `origin/v3` | `b4ad3014805ef647fc2e551357955d644a2298aa` | `git rev-parse origin/v3` |
| непушенных в `v3` | **2** (`ce66691` + `570e7f4`; первый, автор Maxim, 12.09 16:59 +0300 — **подтверждён оператором 12.09 как сделанный по его слову**; приёмка у `backend-0912` открыта, числа не предъявлены) | `git rev-list --count origin/v3..v3` |
| `adapters` / `origin/adapters` | `aa19743` — **ТОТ ЖЕ**, непушенных 0 | `git rev-parse` |
| роль-скилл: диск / `v3:` | `7b9b7e9` 79467 B / `c0777da` 71538 B — **дрейф 7929 B, диск новее** | `hash-object` / `rev-parse` |
| роль-скилл, NUL | **1913** и на диске, и в сторе — известная дыра в §B из `1b5778a` | python |
| незакрытый BINDING | `cc/devops.md` `2026-08-29T08:34:00Z` converge234_transfer; всего `status: open` в `cc/devops.md` — **20** | хвост файла |
| ЧП / EMERGENCY MODE | АКТИВЕН с 2026-06-25, НЕ снят; п.3 сужен словом оператора 08.09 (п.3a) | role-skill §A |

⚠ **`git` через маунт не запускать на командах, трогающих индекс** (`status`, `add`, `diff`):
остаётся `index.lock`, снять его мост не может. `.git/objects/pack` пуст — объекты loose.
⚠ Все записи в `.coord/` — Python + `os.fsync` + сверка байтов round-trip. Коммиты не делать самому:
предъявлять команду оператору. **NO push.**


## 1. ПОДЪЁМ — пути, а не память
- Инит-промпт (запускает оператор): `.coord/protocols/init-coordinator.md`
  ⚠ Оператор иногда называет его `.coord/coordinator-init.md` — такого файла нет, не искать.
- Этот хендоф: `.coord/coordinator_handoff.md` — читать ПЕРВЫМ.
- Роль-скилл: `.claude/skills/role-coordinator/role-coordinator.md`.
- Шина: `.coord/inbox/<роль>.md` (переписка), `.coord/cc/<роль>.md` (BINDING-обвязка прогонов).
- Реестр дефектов: `.coord/rejects.md`. Процедура 234: `tools/procedure_234_clean_install.md`.
- Клон-доказательство пакета: `D:\Claude\Build\rtm_clean_20260908_085310` — **НЕ УДАЛЯТЬ** до
  конца приёмки. Пакет на 234: `C:\Temp\08092026.1037.zip`.
- Запись в `.coord/` — только Python + `os.fsync` + round-trip сверка байтов. `git` через маунт
  на командах, трогающих индекс, НЕ запускать. `.git/objects/pack` пуст — история читается
  обходом loose-объектов (zlib).

## 2. ПИНЫ ЭТОГО ПРОБУЖДЕНИЯ (2026-09-08)
| предмет | значение | чем снято |
|---|---|---|
| `v3` | `3d3014688e6f554cba116fc78e09fca8e31aa169` | `.git/refs/heads/v3` |
| `origin/v3` | `79e3905f806532f54ee2e272108a213162669e7b` | `.git/refs/remotes/origin/v3` |
| непушенных | **17** (замер devops-0908b; мой признак — неравенство рефов) | — |
| роль-скилл, блоб | `ea2182215d94219abd084159fb237c463f857e98`, 63825 B, **NUL 1913**, CR 0 | `git hash-object`-эквивалент, python |
| этот хендоф (до перезаписи) | `763787403250ba7ea47a981305d38229862c750b`, 12694 B, NUL 0, CR 0 | там же |
| `rejects.md` | `5adae8191131d9b8fb34fd5027017892f3c91715`, 132420 B, NUL 0, CR 0 | там же |
| ЧП / EMERGENCY MODE | АКТИВЕН с 2026-06-25, НЕ снят | role-skill §A |

⚠ **Роль-скилл координатора НЕ ЦЕЛ: 1913 нулевых байт в §B** (дыра между 2026-06-26 и 2026-07-02,
родилась в `1b5778a`, чистой версии не существовало никогда). Сенсор §C п.0 красный ОСОЗНАННО.
Писать в файл только В ОБХОД дыры и после каждой записи пересчитывать NUL: должно остаться 1913.

## 3. ЧТО В РАБОТЕ ПРЯМО СЕЙЧАС И ЧЕЙ ХОД
**Сервер 234 живой.** Снос -> сборка из чистого клона -> установка `-FreshDb -DBPort 5433`
`-Fqdn insightense.com -CertSubject "insightense.com"` -> `data.sys` возвращён -> адаптер
`RTMTwilio_1` стоит -> ресинк последовательностей выполнен (VERDICT PASS,
`.measurements/234_20260908_130842_seq-resync.txt`).

| роль | ход | состояние |
|---|---|---|
| **devops-0908b** | **его** | ждёт моего слова: §8.6 сейчас или после §8.9 |
| backend-0906 | ждёт | правка сирот одобрена, НЕ закоммичена; приёмка после §8 |
| shell-0905 | ждёт | правка редактора (вариант 2) одобрена; приёмка после §8 |
| curator-0906 | свободен | аттестовал подъём devops-0908b |

**Незакрытое, требующее §4 (мой gate) — каждая правка отдельным CC-промптом, по одной:**
1. шаг 7 `Provision-FreshDb.ps1` — ресинк по `deptype='a'` вместо IDENTITY (см. §5);
2. `log4net.config` адаптера — по варианту **(б)**: пакет адаптера = его publish с ветки `adapters`;
3. незащищённый `RTM/log4net.config` — нет в списках сохранения Install/Update.

**Ждёт ОПЕРАТОРА:** решение по §8.6 я ещё не выдал; ротация пароля `postgres` (мой вопрос,
не задан — задавать ОДНИМ вопросом, не смешивая с ран-боксом).

**Незакрытых BINDING на шине я не открывал.** Старый `cc/devops.md:640` `2026-08-29T08:34:00Z`
`converge234_transfer` — проверить состояние, я его в этом заходе не трогал.

## 4. НЕПРОЧИТАННОЕ В МОЁМ ИНБОКСЕ
`grep -c '2026-09-0[78]' .coord/inbox/coordinator.md` = **72**.
**Непрочитанного нет: все 72 разобраны в этом пробуждении**, на каждое дан ответ в
`.coord/inbox/<роль>.md`. Число велико потому, что оно считает строки с датой, а не сообщения.
Последнее входящее — devops-0908b о коммитах роль-скилла; ответ на него дан (два коммита, сейчас).
Единственное неотвеченное — его вопрос «§8.6 сейчас или после §8.9».

## 5. НЕГАТИВНОЕ ЗНАНИЕ — то, чего нет в git и что иначе купят заново
**Что было сломано и чем починено:**
- **Ресинк последовательностей мёртв целиком** (`PR234-INST-11`). Шаг 7 выбирает по
  `pg_depend.deptype='a'` (старый `serial`), а все **16** последовательностей базы — IDENTITY,
  `deptype='i'`. Цикл делал **0 итераций** и печатал `Sequences resynced.` **Это било все чистые
  установки с перехода схемы на IDENTITY.** Проявление — `23505` при первом сохранении чего угодно.
  Починено на 234 ручным ресинком; сам шаг НЕ исправлен.
- **Установщик затирает `data.sys`** (`PR234-INST-01`), подтверждено тремя хешами: пакетный
  `7745C5CA…476D` != эталон `24F0BFAC…DE43`. Возврат состоялся и пережил старт службы.
- **Адаптер не поднимался, потому что не мог инициализировать логгер** (нет `log4net.config`),
  а сообщение об ошибке уходило в тот самый логгер. Отказ уничтожал своё свидетельство.

**Ложные следы — не идти по ним заново:**
- «Пустые экраны = нет связи с контакт-центром» — **неверно**: причина была в расхождении
  арендатора (`01a07e07…` в базе против `019e03e9…` в конфиге RTM), см. `db/data/01_tenants.sql`.
- «`InsertQueueGridColumnAsync` неидемпотентен» (старый Defect I, OPEN с июля) — **симптом
  приписан методу**; причина — ресинк. Метод идемпотентным быть и не должен.
- «Правка сирот backend-0906 сломала сохранение грида» — **неверно**: правка не закоммичена,
  значит её нет в сборке; дефект в сборке не может происходить из отсутствующего кода.
- «`preserve_` исчез» — **неверно**: 2145 файлов на месте, промах был в области поиска.
- «Пакет `08092026.0859` пропал» — **неверно**: промах в МАСКЕ (`*_Full.zip` против
  `ДДММГГГГ.ЧЧММ.zip` без суффикса при `-Mode Full`).

**Предикаты, которые врали, и почему:**
- `/health` 200 — доказывает жизнь HTTP-хоста, не работу продукта (`RTMAdapter.cs:52-56`:
  при `License.NotValid` стоит `return` до создания движка). **Гейт = 200 И pipe с именем из
  `AppConfig.PipeName`.**
- `psql` без `-v ON_ERROR_STOP=1` возвращает **0 при ошибке** — код возврата не гейт.
- VERIFY установщика теряет кавычки -> PostgreSQL опускает имена в нижний регистр -> «таблицы нет»
  о здоровой базе (`PR234-INST-08`); он же меряет `tenants` ДО наполнения (`-09`); он же ищет
  пользователя `SUPERADMIN`, которого seed не создаёт — создаётся `admin` + роль `Superadmin` (`-10`).
- Маскировка секрета по ИМЕНИ поля пропустила пароль Redis в чат открытым текстом.
- Мой гейт происхождения пакета: **3 признака из 6 были в файле и ДО правки** — как доказательство
  пусты; сумма зелёных скрывала пустые слагаемые.

## 6. ЧЕГО ПРЕЕМНИКУ НЕ ДЕЛАТЬ
- **Не выдавать ран-боксы.** Их выдают спецы. Координатор благословляет промпт (§4) и даёт слово.
- **Не действовать по неизмеренному.** Пин действителен в пробуждении, где снят; чужой пин
  перепроверяется, а не наследуется.
- **Не разрешать установку, не потребовав предъявить, ЧЕМ ставим.** Разрешение, называющее «как»
  и не называющее «чем», — дыра (я на этом горел: июльский пакет вернул бы 7 дефектов, а чистая
  база сделала бы подмену невидимой).
- **Не подтверждать свои догадки чужими руками.** `CN=insightense.com` была неверна дважды.
  Просить проверить И ОПРОВЕРГНУТЬ.
- **Не чинить измерительный прибор посреди измерения** и не чинить дефект, доказательство которого
  ещё не снято.
- **СО СЛОВ ОПЕРАТОРА (2026-09-13):** legacy `RTM` **бежит постоянно** (Running / StartMode Auto),
  её не трогают. Прежние записи «legacy RTM Stopped» были непроверенным утверждением, кочевавшим по
  документам, — вычеркнуты. Запрет трогать зону остаётся: он не про состояние службы.
- **Не трогать:** `C:\IceDash\`, боевой `RTM.Twilio`, legacy `RTM`, PG15 на **5432** (чужие роли
  `chat_user`, `opcsuser`/SUPERUSER), `C:\RTMView-Ops\` («не прибирается ни сейчас, ни раз уж мы здесь»).
- **Лицензия:** `data.sys` не расшифровывать, не подменять легаси, MacAddress/HostName/VM UUID
  не менять, валидацию не обходить.
- **Секреты:** значения в чат и в файлы замеров не идут — только имя, длина, sha256. Пароли
  вводятся на машине. **Пароль Redis оператор решил НЕ менять (2026-09-07)** — к обсуждению
  не возвращаться. **Пароль `ccdashboard_user` — НЕ менять (слово оператора).** **Пароль `postgres` —
  НЕ менять (слово оператора 2026-09-15, вопрос задан координатором и ЗАКРЫТ; не поднимать заново).**
  Все три пароля решены; открытых вопросов по секретам у координатора к оператору НЕТ.
  **Шесть чужих файлов с открытыми значениями в `C:\RTMView-Ops\output\` — оператор разрешил УДАЛИТЬ
  (2026-09-15).** Это снимает запрет «зона не прибирается» ТОЛЬКО для этих шести файлов и ТОЛЬКО на
  удаление; остальное в `C:\RTMView-Ops\` не трогаем. Предмет `PR234-OPSOUT-SECRETS-01`, владелец
  `devops-0912`, очередь — после выката.
- **NO push.** Ветку не двигать. Коммиты не выполнять самому — предъявлять команду оператору.
- **⚡ ПОКА ДЛЯ СОБСТВЕННЫХ ДЕЙСТВИЙ НЕ НУЖЕН (слово оператора 2026-09-17): ЕСТЬ ЗАДАЧА — ВЫПОЛНЯЙ.**
  Не ждать «покай меня», не спрашивать разрешения на СВОЮ работу: разбор инбокса, §4, записи в реестр
  и хендоф, пере-снятие пинов, переустройство очереди ролей — делается сразу, как появилось основание.
  **Что этим НЕ отменяется и требует слова оператора по-прежнему:** закрытие реджекта (CONFIRM,
  конституция); пуш и барьер §37; любая запись на боевой сервер; необратимое действие; вопрос,
  развилку которого решает он. То есть снят поке на МОЙ ход, а не гейты на чужие.
- **Не заваливать оператора таблицами и номерами пунктов.** Суть по-человечески; ОДИН ВОПРОС —
  ОДИН ОТВЕТ; **ТАБЛИЦА ПОКОВ последним блоком КАЖДОГО ответа** (форма — §D роль-скилла).
- **Не показывать оператору карточки файлов**, которые пишешь на диск: писать молча, упоминать строкой.

## 7. МОИ СОБСТВЕННЫЕ ПРОМАХИ ЭТОГО ЦИКЛА
1. Разрешение «как» без «чем» (пакет без правок).
2. Догадка вместо замера: `CN=…`, взятая из примера в справке установщика.
3. Приписал три признака гейта правке `20b9c65` по памяти, не сверив с историей.
4. Раньше: `/health` без pipe; «сделай X до Y» без требования предъявить X; ожидаемый sha
   по памяти; выдуманный второй адрес дефекта; «ноль гридов — норма», принимавшее провал seed за успех.
5. **Документировал измерения, но не выводы** — реестр и уроки писал только после прямого вопроса
   оператора «мы всё документируем, да?». Записывать в тот же день, когда найдено.

---

# СОСТОЯНИЕ НА 2026-09-17T19:1xZ — `coordinator-0917` (дописано, тело выше не переписано)

> Аттестация: ПРОВАЛ по двум узким пунктам -> пересдача -> **PASS, `curator-0817`, 2026-09-17**.
> Ограничение снято целиком. Пины ниже сняты мной в ЭТОМ пробуждении.

| предмет | значение | чем снято |
|---|---|---|
| `v3` | `41c54f53fa954f7b8fc392ac6926a30804203ac0` (`41c54f5`) | `git rev-parse v3` |
| `origin/v3` | `ed3e29251ec9e4aed3136851f0a4fd38b75fdbb5` (`ed3e292`) | `git rev-parse origin/v3` |
| непушенных | **9** | `git rev-list --count origin/v3..v3` |
| роль-скилл координатора, диск | `353133c` / 85987 B (после моей правки `§C` + 6 уроков `§B`) | `git hash-object` |
| роль-скилл, `v3:` | `7b9b7e9` / 79467 B | `git rev-parse v3:` |
| дрейф роль-скилла | **диск новее ветки на 6520 B — ЖДЁТ КОММИТА ОПЕРАТОРА** | разность |
| роль-скилл, NUL / BOM | 1913 / есть — и на диске, и в сторе; дыра из `1b5778a`, не мной | python |
| хендоф, диск / `v3:` | 25426+ B / 20849 B (`7932b06`) — шапка куратора не в ветке | python / `git cat-file -s` |
| `commit.lock` | НЕТ, `.coord/locks/` пуст | `ls` |
| `.coord/push/request.md` | `CLOSED — FREEZE СНЯТ`, closed 2026-09-14T07:4xZ | чтение файла |
| ЧП / EMERGENCY MODE | АКТИВЕН с 2026-06-25, НЕ снят; п.3 сужен 08.09 (п.3a) | `§A` роль-скилла |

## БАРЬЕР §37 НА 9 НЕПУШЕННЫХ — РЕШЕНИЕ МОЁ: **НЕ ОТКРЫВАЮ СЕЙЧАС.** Основание — не «рубеж по существу»

Предшественника не наследую, его формулировку («рубеж по существу, а не по счётчику») не повторяю.
Моё основание другое и оно измеримо: **кворум §42.7 сейчас недостижим по составу, а не по готовности.**

[измерено: обход `.coord/sessions/`, поле статуса в обоих написаниях, спорные файлы разобраны вручную]
- живые с полем `active`: `coordinator-0917`, `devops-0916`, `shell-0912`, `backend-0912`;
- **`test-5-0607` -> `status: done`**. QA — ОБЯЗАТЕЛЬНЫЙ ack кворума и «load-bearing functional gate»
  (норма 2026-06-23, там же её происхождение: пуш 22.06 без функционального гейта стоил дорого).
  Владельца у гейта сейчас НЕТ;
- **`techwriter-0610` -> `done`**: doc-sync gate, тоже обязательный ack. Владельца НЕТ;
- **`security-0620` -> `done`**: третий обязательный ack. Владельца НЕТ;
- `.coord/push/acks/` несёт четыре READY от **14.09** (`backend-0912`, `curator-0817`, `devops-0912`,
  `shell-0912`) — это акты ЗАКРЫТОГО барьера, к нынешнему кворуму отношения не имеющие. При открытии
  нового барьера каталог обязан быть очищен, иначе кворум соберётся из чужих подписей.

[вывод] Открыть барьер сегодня значит либо собрать кворум без трёх обязательных гейтов, либо поднять
три роли ради пуша девяти коммитов. Первое — демонтаж гейта молча, ровно то, за что колония уже
заплатила 22.06. Второе — акт крупнее самого пуша и решается не мной.

**Что я при этом НЕ утверждаю:** что девять коммитов могут лежать непушенными сколько угодно. Риск
реален и он про СОХРАННОСТЬ: ветка — единственный durable слой, диск таковым не является.
Но этот риск аргументирует ПУШ, а не ВЫКАТ, и это два разных действия с разной ценой.

**Ложная связка, которую снимаю:** «пуш закроет окно на скоропортящийся визуал `FILTER-TYPE-01`».
Окно закрывает ВЫКАТ на бой, а не пуш в origin [вывод из формулировки самой записи: «выкат закроет
окно навсегда»]. Барьер §37 и замер визуала — не одна развилка, и связывать их не нужно.

**Состояние девяти** [измерено: `git log --name-only origin/v3..v3`]: 4 правки `src/` (три `fix(web)`,
одна `docs(rtm)` в комментариях), 2 `deploy/` (INST-13/INST-14), 2 `tests/` (новый тест-проект
`RTM.Engine.Tests` + сенсор; `b2b80c3` меняет `CcDashboard.sln`), 1 — роль-скилл devops + его хендоф.
⚠ Новый тест-проект в решении означает, что test-gate обязан предъявить ПРОГОН с числами: «приложение
собирается» про новый таргет не говорит ничего (норма 2026-07-02, корень 62 ошибок).
⚠ `41c54f5` закоммичен при разошедшемся счётчике гейта (1 из 4), и виноват гейт, а не правка —
признано самим `shell-0912`; замер «после» на бою не снят.

## ДЕФЕКТЫ РЕЕСТРА СЕССИЙ, найдены и частью починены
- `coordinator-0818` держал `**Status:** active, ATTESTED` с 18.08 и не был закрыт ни одним из трёх
  преемников. **Закрыт мной 2026-09-17**, строка статуса не затёрта, основание в файле.
- `curator-0908` держит `status: active` с heartbeat 08.09, а действующий `curator-0817` **не несёт
  поля статуса вовсе**. То есть по реестру живым выглядит мёртвый куратор, а живой невидим. Предмет
  кураторский — передан ему, сам не правлю.
- `shell-0831` ловится матчером «последнее вхождение по файлу» на ПРОЗЕ про чужой слаг. Предикат
  синглтона обязан быть двусторонним: поле в любом месте строки И исключение прозы; спорные файлы
  разбираются вручную, их единицы.

## ОЧЕРЕДЬ
1. `§C` роль-скилла переписан, `§B` +6 уроков — **ждёт коммита оператора** (команду предъявлю).
2. Барьер §37 — вопрос оператору задан, решение выше.
3. Пере-проверка `FILTER-TYPE-01` и открытые BINDING: `procedure_inst13_dry_ADE.md` (devops, open с
   16.09, RESULT не лёг), `cc_prompt_shell_popup_whitespace.md` (shell, RESULT есть, шапка не в done).
4. Обход реестров для ЧИСЛА открытых пунктов (Н-14) — счёт слова `OPEN` числом пунктов не является.

## РЕШЕНИЕ ОПЕРАТОРА 2026-09-18 — ПУША НЕТ ДО ПОЧИНКИ ФИЛЬТРОВ
[со слов оператора: 2026-09-18] «пока не пушим, сделаем после починки фильтров». Барьер §37 на 10
непушенных НЕ открывается; вопрос о неполном кворуме снят до тех пор. Ветку не двигаем.
[измерено: git rev-parse, 2026-09-18T06:2xZ] `v3=55c1945`, `origin/v3=ed3e292`, непушенных **10**.

**Что это меняет в очереди:** фильтры становятся критпутём к пушу, а не одним из предметов.
Порядок единиц фиксирую ниже; менять его может оператор или владелец с основанием, не я молча.

### СОСТОЯНИЕ ФИЛЬТРОВ НА 2026-09-18, владелец всех единиц — `shell-0912`
| единица | состояние | что мешает считать починенным |
|---|---|---|
| `PR234-FILTER-TYPE-01` | правка `0864147` закоммичена; красное «ДО» снято на бою 17.09 | **половина «ПОСЛЕ» не снята, и снимается только ВЫКАТОМ** |
| `PR234-FILTER-POPUP-GEOM-01` №3 (`white-space`) | правка `41c54f5` закоммичена, гейт автора разошёлся 3 из 4 — виноват гейт | замер «после» не снят (тот же выкат) |
| `…GEOM-01` якорь (под кнопкой, RTL+LTR) | развилка закрыта оператором 17.09; **промпт НЕ написан** | работа не начата; требования к предикату заданы заранее |
| `…GEOM-01` №4 (обрезание `overflow`) | ждёт ПЕРЕ-замера после №3 | по решению: «проектировать портал сейчас значит проектировать под неизмеренное» |
| `PR234-FILTER-POPUP-STATE-01` | причина найдена (нет общего состояния выше виджета) | правка не начата |
| `PR234-FILTER-DRAFT-01` | заведён 17.09; заказан РАЗБОР до правок | разбор не сдан |
| `PR234-GRID-DETECT-FIRSTROW-01` | ПРИДЕРЖАН | вне фильтрового критпутя |
| `PR234-SCORE-NUMERIC-DEAD-01` | ПРИДЕРЖАН, дефект мёртвый (вызовов 0) | вне критпутя |

**ЗАЦЕПКА, КОТОРУЮ НАЗЫВАЮ СРАЗУ, А НЕ КОГДА УПРЁМСЯ:** у двух единиц половина «после»
снимается только на боевом теле, то есть ВЫКАТОМ. Выкат — не пуш и от `origin` не зависит
(пакет собирается из чистого клона), но это отдельное действие с отдельным словом оператора.
Значит «фильтры починены» имеет два разных смысла — «правки написаны и закоммичены» и «приёмка
закрыта на бою», — и от выбора зависит, когда открывается барьер. Вопрос задан оператору отдельно.

**Чей ход сейчас** [измерено: последний заголовок `.coord/inbox/shell.md` = 2026-09-17T18:xxZ,
от координатора к `shell-0912`]: **ход shell'а**. Нового поверх невыполненного не выдаю.

## [со слов оператора: 2026-09-18] ПОДТВЕРЖДЕНИЕ НОРМЫ: «ЕСТЬ ЗАДАЧА — ВЫПОЛНЯЙ»
Оператор повторил норму 17.09 и потребовал её записать. **Она перенесена в ПОСТОЯННЫЙ слой —
`§A` п.3b роль-скилла**; здесь остаётся указатель, а не единственная копия. Основание переноса:
правило о поведении роли, лежащее только в хендофе или ините, не переживает компакцию — урок
`PR234-GITBAN-UNSPREAD-01`, за который куратор поправил координатора 17.09.
Граница не изменилась: мой ход — сразу, без просьбы разрешить; его слово — там, где цену несёт он
(пуш и барьер §37, бой, необратимое, CONFIRM по реджекту).

## [со слов оператора: 2026-09-18] ПОДПИСИ В УЖЕ СДЕЛАННЫХ КОММИТАХ — НЕ ЧИСТИМ
Дословно: «оставляем как есть». История не переписывается; 11 из 22 непушенных коммитов уйдут в
`origin` с приписками `Co-Authored-By`/`Claude-Session`. Основание, предъявленное оператору:
577 ссылок на эти sha в 27 файлах `.coord/**` против двух текстовых строк в одиннадцати коммитах.
**Преемнику: развилку НЕ поднимать заново.** Она была ограничена по времени (только до пуша) и
решена; после пуша она не существует в принципе.
**Лечение вперёд остаётся:** источник — 14 промптов из 580 с подписями в ШАБЛОНЕ сообщения, а не
поведение CC; роли их вычищают, пункт приёмки «подписей 0» остаётся сторожем в каждом промпте.

## [со слов оператора: 2026-09-18] ПОРЯДОК: ВЫКАТ -> ПРОВЕРКИ НА БОЮ -> БАРЬЕР §37
Дословно: «после выката и проверок». Барьер сейчас НЕ открывается, хотя фильтровая линия закрыта по
коду (семь коммитов). Критпуть смещается на ВЫКАТ, и владелец выката — `devops-0916`, не координатор.
**Что из этого следует по срокам, и чего НЕ следует:** половины «ПОСЛЕ» не скоропортящиеся — они
снимаются на почищенном теле и ждут сколько угодно. Скоропортящимися были половины «ДО», и все семь
СНЯТЫ до выката. Значит спешки нет; есть только порядок.
**Что обязано быть готово ДО выката:** план одного захода `shell-0912` (какой экран, какие колонки,
в каком порядке, что требует боевого экрана, а что нет) — иначе после выката заход развалится на
несколько и половины «после» лягут в разные состояния площадки.
**Что выкат закрывает попутно:** живую приёмку `PR234-CMP-01` частей 2 и 2b (`DELIVERED` -> `CLOSED`)
и делает возможной часть 1 (симметрия корпусов). ⚠ Сам выкат пойдёт мимо дрейф-гейта: `realDriftA`
всегда красный ПО ПОСТРОЕНИЮ до части 1, и каждый проход `-SkipDrift` обязан нести ЗАПИСАННУЮ
причину, а не ссылку на «известно».

## [со слов оператора: 2026-09-18] СЕРВЕР 234 — ЛАБОРАТОРНЫЙ
Дословно: **«на 234 (лаб сервер) разрешены любые действия для проверок с откатом на состояние до
проверок».** Перенесено в ПОСТОЯННЫЙ слой — `§A` п.3d роль-скилла; здесь указатель.
Снимает целый класс вопросов к оператору: разрешение на замер, трогающий 234, спрашивать НЕ НУЖНО.
Три условия: ради проверки · «до» снято ЧИСЛОМ · откат предъявлен ЧИСЛОМ.
Границы: только 234; необратимое, чужие зоны и сам ВЫКАТ — при своих гейтах.
**Преемнику: не наследуй язык «боевой сервер 234» из старых записей** — он и породил лишнюю
осторожность. Машина лабораторная.

## [со слов оператора: 2026-09-18] АДРЕС 234 — `https://platform.insightense.com:8444`

Дословный повод: «чтобы ни ты, ни твои последователи не тыкались как слепые мыши и не тратили
токены на догадки». Перенесено в ПОСТОЯННЫЙ слой — `§A` п.3d роль-скилла; здесь указатель.

**Что этим закрыто.** Адрес лабораторного сервера нигде не был записан как факт: он жил россыпью
в чужих замерах вперемешку с адресами других машин и локальными петлями. Роль, которой нужно
посмотреть на 234, выбирала его ДОГАДКОЙ. Координатор 2026-09-18 собрался открыть
`insightense.com:8444`, выбрав его по частоте вхождений в двух инбоксах — частота не измерение,
и имя без `platform.` указывает не на 234.

**Соседние имена, которые 234 НЕ являются и подстановке не подлежат:**
`https://insightense.com:8444` · `https://nayax.insightense.com:8444` · `http://20.80.36.234:8088`
· `https://127.0.0.1:8444` · `https://localhost:5239`.

**Преемнику:** адрес берётся из `§A` п.3d и ниоткуда больше. Вопрос «а какой у 234 адрес» оператору
больше не задаётся.

## [со слов оператора: 2026-09-18] СНЯТА МОЯ ЖЕ ФОРМУЛИРОВКА «КРИТПУТЬ К ПУШУ»

Дословно: **«гонки к пушу нет, гонка за исправлениями багов»**. Перенесено в ПОСТОЯННЫЙ слой —
`§A` п.3e роль-скилла; здесь указатель и отзыв прежнего текста.

**Что этим ОТМЕНЕНО в теле выше, моей же рукой написанное 18.09:** строки «фильтры становятся
критпутём к пушу, а не одним из предметов» и «критпуть смещается на ВЫКАТ». Формулировка не
ложная по фактам, но неверная по устройству: она делает пуш целью, а дефекты — препятствиями на
пути к ней. Порядок от этого строится не по цене дефекта, а по тому, что быстрее разблокирует
ветку. Я на этом уже дважды спрашивал оператора «чинить или пушить» — сама развилка была
поставлена неправильно.

**Как строить очередь вместо этого:** по цене дефекта для пользователя и по цене его
необнаружения. Пуш — сохранность сделанного, он остаётся при своих гейтах (барьер §37, слово
оператора) и на приоритет работы не влияет.

**Преемнику:** найденный дефект заводится немедленно, даже если удлиняет линию. Фраза «не будем
заводить, чтобы не растягивать путь до пуша» — запрещённая.
