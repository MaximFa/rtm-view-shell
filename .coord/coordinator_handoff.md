# HANDOFF — RTM COORDINATOR
> Написан 2026-08-31T06:5xZ координатором-0830 (аккаунт profit) ДЛЯ ПЕРЕДАЧИ ВО ВТОРОЙ АККАУНТ.
> Все пины сняты с диска в этом пробуждении. «Я помню» доказательством не является.

## ▶ КАК ПОДНЯТЬ КООРДИНАТОРА — ПУТИ, А НЕ ПАМЯТЬ
**Стартовый промпт (его запускает оператор):**
`D:\Claude\Projects\RTM View Shell\.coord\protocols\init-coordinator.md`
Подробная инструкция преемнику — `.coord/inbox/coordinator-handover-0831.md` (ЧИТАТЬ ПЕРВОЙ).
Роль-скилл: `.claude/skills/role-coordinator/role-coordinator.md` (⚠ несёт 1913 нулевых байт, см. ниже).
Шина: `.coord/inbox/<роль>.md` (переписка), `.coord/cc/<роль>.md` (BINDING-обвязка прогонов).

## RESUME CHECK — сверь это первым, до любого действия
| предмет | значение на 2026-08-31T06:50Z | чем снято |
|---|---|---|
| `v3` HEAD | `79e3905f806532f54ee2e272108a213162669e7b` (2026-08-31T09:2xZ) | `.git/refs/heads/v3` |
| `origin/v3` | `79e3905f806532f54ee2e272108a213162669e7b` — **ТОТ ЖЕ** | `.git/refs/remotes/origin/v3` |
| непушенных | **0** | равенство рефов побайтно |
| ⚠ ПУШ БЕЗ БАРЬЕРА | `f6d5c58 -> 79e3905`, **2026-08-31T09:12:58Z**, 74 файла | `.git/logs/refs/remotes/origin/v3` |
| версия продукта на 234 | `d1982de` (Shell+RTM), адаптер `8abd19a` | замер devops |
| незакрытый BINDING | `cc/devops.md:640` `2026-08-29T08:34:00Z` converge234_transfer | хвост файла |
| ЧП / EMERGENCY MODE | АКТИВЕН с 2026-06-25, НЕ снят | role-skill §A |

⚠ **`git` через маунт не запускать на командах, трогающих индекс.** `.git/objects/pack` пуст — все
объекты loose, история читается обходом объектов (пример кода — в моих записях в инбоксе devops).
⚠ Все записи в `.coord/` — Python + `os.fsync` + сверка байтов round-trip. Коммиты не делать самому:
предъявлять команду оператору.

## ГДЕ СЕЙЧАС РАБОТА — ОДНОЙ ФРАЗОЙ
Converge на тестовый сервер 234 доведён до живого стека; идёт разбор багов, заявленных оператором.
Гейт `§FEED-READY` и остаток захода 3 НЕ сняты — отодвинуты оператором ради разбора багов.

## СОСТОЯНИЕ 234 (тестовый сервер, читает прод; НЕ боевой)
- БД: новая на **5433** (PG18.4), собрана целиком, 17/17 таблиц, счётчики точные
  (`RTSData_UserStatusLog` 202917), целостность сошлась с исходной базой строка в строку
  (21/21, 21/21, 81/81 — это ПРЕД-СУЩЕСТВУЮЩИЙ дрейф данных 234, не наш).
  Старая БД на **5432 цела и не тронута** — откат бесплатный.
- Службы: `RTMService` Running (движок жив, pipe `rtmpipe_v3`, 8089), `RTMViewShell` Running
  (вход работает), `RTMTwilio_1` Running на 9201, ревизия `8abd19a` — та же, что на 140.
- НЕ ТРОГАТЬ: боевая `RTM.Twilio` (Stopped/Manual), legacy `RTM` (Stopped). Оператор запретил.
  ⚠ 30.08 наш сервис ОДИН РАЗ запустил боевой адаптер через `RTM:AdaptorServiceName` — устранено,
  ключ переведён на `RTMTwilio_1`. Это был реальный выход за границу, не «едва не».
- Инвариант эксплуатации: **на `8abd19a` переподключения нет** (обработчик обрыва только логирует).
  Любой рестарт `RTMService` ОБЯЗАН сопровождаться рестартом `RTMTwilio_1`. Проба, снятая после
  одиночного рестарта RTM, недействительна.

## ОТКРЫТЫЕ БАГИ ОПЕРАТОРА
- **PR234-BU-01** (`rejects.md`, OPEN): во всех виджетах пуст список Business Unit.
  Класс УЖЕ определён и это НЕ наш перенос: список фильтруется по тенанту ЭКРАНА
  (`ScreenEditorPage.razor:2963` -> `ConfigurationQueries.cs:33-37` -> `NgcRepositories.cs:29-36`),
  экран `Test2` принадлежит `customer1`, а все 59 BU лежат под `platform`. Старая БД 5432 и новая
  5433 дали ИДЕНТИЧНУЮ картину — ситуация была до нас.
  **Ждём от оператора один ответ:** на 140 список был непустым на экране КАКОЙ организации.
  «Platform» -> механизм подтверждён, это не дефект переноса. «Customer1» -> разница реальна,
  снимаем пару замеров на 140 (только чтение).
- Продуктовые находки к разбору (НЕ чинить, зафиксированы с метками):
  `ArgumentNullException` в `TwilioAdapter.removeInteraction:2255` — 32 раза каждые 30 с, встали
  фоновые циклы, службу подняли failure-actions; **это в ревизии выката `8abd19a`, то есть и на 140**.
  `_userStatusStart > statusChanged` — 279 раз. `KeyNotFoundException 'QueueNumberOfLoggedAgents'`
  (`Engine.cs:671`) — 125 раз, unions 74/75/76/78.

## ДЕФЕКТЫ ИНСТАЛЛЯТОРА — 11 штук, три файла портятся при установке поверх работающей машины
`Install-RTMView.ps1` трактует машинные артефакты как содержимое пакета:
`RTM\data.sys` (затирает `LicenseKey`/`DatabaseUser`/`DatabasePassword`/`KestrelHttpsPassword` —
продукт лёг с `License is Not Valid`, увело на полвечера в лицензирование);
`RTM\appsettings.json` (`AdaptorServiceName` от прошлой инсталляции — управляли чужой службой);
`Shell\appsettings.json` (потеряны 4 ключа, в т.ч. `DefaultTenantSlug` — ОПЕРАТОР НЕ МОГ ВОЙТИ;
сообщение «Invalid username or password» одинаково для трёх причин).
Класс назван куратором точнее моего: **артефакт, чья правильность определяется МАШИНОЙ, едет
пакетом как продуктовая константа**. `Update-RTMView.ps1` (L247/L271) это уже знает, установка — нет.
Выдано devops полное сравнение «преинсталл-бэкап против текущего» по всем каталогам — НЕ ВЫПОЛНЕНО.

## ПОВРЕЖДЕНИЕ РОЛЬ-СКИЛЛОВ — не забыть, это конституция ролей
`role-coordinator.md` несёт **1913 нулевых байт** в середине §B с 2026-07-03 (вошло в `1b5778a`,
последний чистый блоб `c1b7fba`). Утрачены уроки 01-03.07; текстом не восстанавливаются, по смыслу
восстановимы из `journal.md` (записи 02.07: `03:47:39`, `03:53:54`, `04:16:34`, `12:45:08`,
`20:35:17`, `20:39:24`). Ещё три файла побиты: `widget-planner.md` (4107), два скилла в
`migration/account-skills` (228 и 7 — обрезка хвоста).
Норма закрыта куратором: `.coord/protocols/role-skill-standard.md`, раздел **BODY INTEGRITY** (Н-6…Н-9).
Третий симптом: **файл с NUL становится для инструментов бинарным** — `grep -c` считает, `grep -n`
печатает `binary file matches` и НИ ОДНОЙ строки. Любая проверка «покажи строку» по такому файлу
немеет вместо того, чтобы упасть.
**НЕ СДЕЛАНО, ждёт слова оператора:** надгробие в §B + снятие BOM одной правкой; реконструкция
уроков по журналу с обязательной пометкой `RECONSTRUCTED … — НЕ ОРИГИНАЛ`.

## ЖДЁТ ОПЕРАТОРА (не блокирует, но теряется первым)
5 красных реджектов · **непушенных 0 (см. ниже про пуш без барьера)** · `tools/cc_prompt_push.md` устарел (пушит в `v2`) ·
протухший чекпойнт координатора в `MEMORY.md` (2026-06-09) · формулировка надгробия ·
делать ли реконструкцию §B · `data.sys` как класс (не как секреты).

## МОИ СОБСТВЕННЫЕ ПРОМАХИ В ЭТОЙ СЕССИИ — преемнику для калибровки
1. Принял заход по гейту `/health` 200, который ничего не закрывает: при невалидной лицензии
   `return` стоит ДО создания движка, а HTTP-хост поднят отдельно. Гейт обязан быть привязан к КОДУ.
2. Директива «сделай X до Y» без требования ПРЕДЪЯВИТЬ X — не гейт, а пожелание. В эту щель наш
   сервис запустил боевой адаптер.
3. Сверяя хендоф, вписал ожидаемый sha по памяти и промахнулся — внутри самой проверки.
4. Нёс оператору лицензионную проблему, которой не было; причиной был инсталлятор.
5. Нашёл дыру в §B случайно, через 58 дней, потому что мои проверки смотрели на строки, а не на байты.


## ⚠ ПУШ 2026-08-31T09:12:58Z — ПРОШЁЛ БЕЗ БАРЬЕРА §37. Факт, не обвинение.
`f6d5c58 -> 79e3905`, зафиксирован в `.git/logs/refs/remotes/origin/v3` строкой `update by push`.
**Кворума не собиралось, нового `push/request.md` не заводилось:** файл барьера на диске всё тот же,
`status: PUSHED (CLOSED)`, `closed: 2026-07-22T18:05Z`; все `push/acks/*` датированы 22 июля.
**Кто выполнил — из object store не выводится** (авторство не пин). Не додумывать.

**Что именно уехало — измерено, а не предположено** (`git diff --name-only f6d5c58 79e3905`):
74 файла, из них `.coord/` 40 · `tools/` 27 · `.claude/` 6 · `PROJECT_STATUS.md` 1.
**Продуктового кода НЕТ ни одного файла:** фильтр по `src|db|deploy|infra|RTM` пуст. Последний
продуктовый коммит по-прежнему `d1982de` (2026-07-22) и он был запушен ещё тогда.
**Отсюда: риск поставки нулевой, нарушена процедура, а не продукт.** Не раздувать и не заминать.

**Три следствия, которые обязан унести преемник:**
1. Барьер §37 в его нынешнем виде **не отличает пуш норм/протоколов от пуша кода**, и первый же
   реальный случай прошёл мимо него. Вопрос «применять ли §37 к непродуктовым путям» стоит к оператору.
2. `tools/cc_prompt_push.md` **устарел — пушит в `v2`**. Если бы барьер собирали, он бы отработал не туда.
   Чинить ДО любого следующего барьера, иначе норма сработает хуже, чем её отсутствие.
3. Пять моих расхождений в этот пуш **не попали** (они не закоммичены): `coordinator_handoff.md`,
   `rejects.md`, `features.md`, `backlog.md`, `role-coordinator.md`. То есть ветка уехала в origin
   БЕЗ конституционных реестров в актуальном виде.

## ✅ ПОВЕРХНОСТЬ ПОТЕРЬ УСТАНОВЩИКА ЗАКРЫТА ЧИСЛОМ — 2026-08-31, прогон `pkgdiff_20260831_122501.csv`
Предикат: **бэкап против ПАКЕТА** (пакет пришпилен SHA256 `2DE0470F…`, найден по хешу в `C:\Temp\`,
НЕ по имени каталога). Класс перечислен полностью — 8 записей. Результат:
`OVERWRITTEN 3 · SURVIVES 0 · INTRODUCED 0 · IDENTICAL 5 · UNREADABLE 0`.
- **Затираются ровно три:** `RTM\appsettings.json`, `RTM\data.sys`, `Shell\appsettings.json`.
  Это исчерпывающий список, а не «найденное на сегодня».
- **`SURVIVES = 0` — главное.** Ни один машинный конфиг не переживает установку по построению.
  Значит формулировка дефекта установщика меняется: он **не имеет понятия о машинных артефактах
  вообще**; три файла — просто те из восьми, что оказались машинными. Единственная защита —
  предынсталляционный бэкап + явное восстановление; «этот файл установщик не трогает» не существует.
- **`INTRODUCED = 0`** — пакет не вводит новых конфигов. `data.sys` живой == бэкап (ремонт держится).
- Гипотеза про `log4net.config` / два `web.config` **ОПРОВЕРГНУТА** прогоном (все три `IDENTICAL` по
  трём столбцам) и снята автором. Список пострадавших НЕ расширился.
- **Не закрыто:** `RTM.Twilio` — пакетная сторона найдена (`C:\Temp\conv234\twilio_8abd19a`), но
  **НЕ СРАВНЕНА** (ограничение бокса, не свойство мира). Заход §4-PASS выдан. Охват 2 из 3 каталогов.

## АДАПТЕР — что установлено 2026-08-31 (контентный заход, значения маскированы)
- **Потери машинных значений НЕТ.** Живой `RTM.Twilio` смотрит в `rtmpipe_v3` / `127.0.0.1:8089` —
  наш провод, не боевой. 51 ключ из 53 совпадает с предеплой-бэкапом.
- **Ревизия ПРИШПИЛЕНА К АРТЕФАКТУ:** `publish_sc\RTM.Twilio.dll` `ProductVersion` =
  `1.0.0+8abd19ac7470585528c930f52584691ebf5ff9cf`. Больше не «по имени каталога».
- **`OVERWRITTEN` на `appsettings.json` = смена СХЕМЫ, не потеря:** одиночный `RTM:RTM_URL` заменён
  массивом `RTM:Targets[0]{Url,Pipe}` — это и есть содержание `8abd19a` (per-target connect snapshot).
- **`Twilio:ConfigSyncMinutes` — МЁРТВЫЙ ключ, пункт закрыт координатором:** `git grep -c` = ноль
  совпадений на `8abd19a`, `adapters` и `v3`. Ключа нет ни в коде, ни в шаблоне; текущая ревизия его
  не читает. Исчезновение нечитаемого ключа потерей не является. В реестр НЕ заводился.
- ⚠ **ВЫКЛАДКА АДАПТЕРА СОБРАНА ИЗ ДВУХ ИСТОЧНИКОВ.** `log4net.config` отсутствует в
  `publish_sc\`, но присутствует на машине -> часть файлов выложена из корня стейджинга
  `twilio_8abd19a`, часть из `publish_sc\`. Кто будет воспроизводить выкладку из ОДНОГО каталога —
  не соберёт её. Манифест пакета: 358 файлов, SHA `8E6400D4…`.
- ⚠ **ОТКРЫТО:** шаблон `8abd19a` держит `RTM:LogConfig = C:\IceDash\RTM.Twilio\log4net.config` —
  путь в каталог LEGACY. Если живое значение его унаследовало, наша служба читает конфиг логирования
  из чужого каталога. Узкое чтение трёх значений (все класса «путь») выдано devops под §4;
  `C:\IceDash\` проверяется только `Test-Path`, граница не пересекается.

## АДАПТЕР — ветка ЗАКРЫТА 2026-08-31T10:3xZ
- **`RTM:LogConfig` живой = `C:\RTMView\RTM.Twilio\log4net.config`** — НАШ каталог. Шаблонное
  значение `C:\IceDash\...` на машине переопределено; опасение «служба читает конфиг из legacy»
  НЕ подтвердилось. Легаси-файл существует (`Test-Path True`), не открывался, граница не пересечена.
- **Лог адаптера пишется в `C:\Logs\RTMView\RTM.Twilio.log`** (абсолютный путь). Дефект №1
  (`ArgumentNullException` `TwilioAdapter.removeInteraction:2255`, 32x/30с) искать ТАМ, не в `System32\logs`.
- **Самопротиворечивые строки отчёта объяснены: ложный статус, а не отображение.** Ключи объединялись
  из ТРЁХ карт (бэкап/живой/пакет), статус считался по ДВУМ -> «только в пакете» проваливалось в
  `else` и печаталось как `ONLY-IN-LIVE`. Обе выдвинутые причины (кодировка — моя; `$null` — devops)
  оказались НЕВЕРНЫ. Норма в пачку: цепочка на N источников обязана иметь ветку «ни в одном».
- ⚠ **ЛОВУШКА СРЕДЫ:** `Get-Content -Encoding Default` (консоль WIN1252) ПОРТИТ иврит — `utf8bytes` 24
  вместо 10 на одном элементе. Любой скрипт, читающий JSON с ивритом без `-Encoding UTF8`, найдёт
  несуществующие расхождения. Родня `client_encoding` в psql.
- **Оговорка, которую делает сам devops и я её сохраняю:** предикат по адаптеру чинился ЧЕТЫРЕЖДЫ за
  день, последняя починка проверена ОДНИМ прогоном. Починенный предикат — новый предикат, и его стаж
  наблюдения равен одному прогону. «Три из трёх» не пишем.

## ⛔ БЛОКЕР НА ЧП п.4 — координатор НЕ МОЖЕТ смотреть глазами (2026-08-31T10:3xZ)
`§A` ЧП п.4 требует, чтобы координатор ЛИЧНО визуально проверял каждый закрытый пробел, и `CLAUDE.md §47`
предписывает делать это через ХОСТОВЫЙ Chrome. **Расширение Chrome к сессии не подключено:**
`tabs_context_mcp` -> «Browser extension is not connected».
Проверено один раз, не долблю (правило «не уходить в петлю»).
**Следствие:** пока расширение не подключено, я физически не могу выполнить ЧП п.4, а по норме без него
не начинается разбор багов. Это НЕ отсутствие моста к диску — `device_bash` работает; это отдельный
канал, и он отдельно отваливается.
**Варианты, решение оператора:** (а) подключить расширение и я иду смотреть сам; (б) оператор смотрит
сам и присылает наблюдение — тогда мой ЧП-п.4 замещается его словом и это надо записать явно, а не
подразумевать; (в) ЧП п.4 временно снимается с этого цикла — но это правка нормы, а не умолчание.
**Не выбирать за оператора и не начинать разбор багов, пока один из вариантов не назван.**


---

## ПОПРАВКА К RESUME CHECK — 2026-08-31T18:2xZ, coordinator-0831
| предмет | было в этом хендофе | ФАКТ на 2026-08-31 | чем снято |
|---|---|---|---|
| версия шелла на 234 | `d1982de` | **`141899b`** | замер devops-0831 (`SourceRevisionId` артефакта + стейджинг `C:\Temp\conv234\Shell`), независимо подтверждён датой Release-сборки в клоне оператора (29.08, `bin/Release/net8.0/win-x64`) |
| `v3` HEAD | `26ecfcb` | **`ab20897`** | `.git/refs/heads/v3` |
| `origin/v3` | `f6d5c58` | **`79e3905`** | `.git/refs/remotes/origin/v3` |
| непушенных | 24 | **5** | обход объектов; 24 — утренний факт до пуша `79e3905` |

**Урок в §B:** число или sha, унаследованные из документа предшественника, — НЕ пин. Пин — то, что
снято с диска в текущем пробуждении. Я протащил «24 непушенных» в директиву бекенду и «`d1982de` на
234» в постановку девопсу; оба поймали специалисты, а не я.


---

# ▶ ИТОГ ДНЯ 2026-08-31 — coordinator-0831 (второй аккаунт). ЧИТАТЬ ЭТОТ РАЗДЕЛ ПЕРВЫМ.
> Всё ниже снято с диска в этом пробуждении. Числа выше по файлу — утренние, уже неверны;
> действуют поправки из раздела «ПОПРАВКА К RESUME CHECK» и этого.

## RESUME CHECK — вечерний
| предмет | значение на 2026-08-31T21:5xZ | чем снято |
|---|---|---|
| `v3` HEAD | `ab208973c49bdf68dea6c1f5be3d6f6ce9d5b9dc` | `.git/refs/heads/v3` |
| `origin/v3` | `79e3905f806532f54ee2e272108a213162669e7b` | `.git/refs/remotes/origin/v3` |
| непушенных | **5** | НЕ 24 — то был утренний факт до пуша `79e3905` |
| версия шелла на 234 | **`ab20897`** (выкачен вручную 23:04 местного) | предъявления devops-0831 |
| было до выката | `141899b` — **НЕ `d1982de`**, как писал утренний хендоф | два независимых источника |
| бэкап перед выкатом | `C:\RTMView\Backup\shellmanual_20260831_230412\Shell` | предъявление devops |
| ЧП / EMERGENCY MODE | АКТИВЕН, НЕ снят | role-skill §A |

## ЧТО СДЕЛАНО ЗА ДЕНЬ — одной строкой на предмет
**Закрыт ОДИН реджект: `DAYTREND-CULTURE-SEP`** (CONFIRM оператора 20:2xZ). Полный путь:
симптом -> лог -> причина в коде (`ToString("dd/MM/yyyy")` без `InvariantCulture`; `/` в .NET-формате
подменяется разделителем культуры, у `he-IL`/`ru-RU` это точка) -> правка `ab20897` -> сборка ->
ручной выкат только шелла -> гейт в ивритской локали. Единственный предмет за сутки, доведённый
от симптома до подтверждённого исправления.

**`QGRID-78-RTS`** — владелец пришпилен измерением: пересечение cfg↔RTS ячеек **0 из 48** при полном
пересечении у двух исправных гридов. Владелец — **shell**, `SaveQueueGridRtsCommand` /
`ScreenEditorPage.razor:4284` (регистрацию грида пишет редактор, не движок). Причина НЕ найдена.
⛔ **Не пересохранять виджет 78** — перезапись RTS уничтожит единственную улику.

**`QGRID-LOGGEDIN-DUP`** — предмет переформулирован. Моя ветка «застрявшее значение» СНЯТА
(`KeyNotFound` 994 вчера / 0 сегодня). Метрика жива и запрашивается. Остаётся: считает агентов
БИЗНЕС-ЮНИТА, а не очереди; `status: duplicate`, каноническая замена `QueueLoginDataNumLoggedUsers`;
на проводе `text`, в каталоге `number`.

**`AGRID-FILTER-STUCK`** — найдено чтением: экран показывал ветку «все строки отфильтрованы»
(`AgentGridWidget.razor:106-118`), достижимую ТОЛЬКО при `_rows.Count > 0`. Фильтры персистентны
(`:364-368`). Проверка — один клик `Clear filters`, оператору выдан, не выполнен.

## 🔴 ЖДЁТ СЛОВА ОПЕРАТОРА — 13 предметов, НИ ОДИН не закрыт
Оператор в 21:5xZ спросил прямо: «закрыли ли реджекты, что увидел глазами?» Ответ — нет, закрыт один.
Формат разбора (по одному против скопом) он ещё не назвал — это первый вопрос новой смены.

Найдено личным визуалом, вердикта нет: `UI-LOC-KEYS` (сырые ключи ресурсов, в т.ч. на экране входа) ·
`VIEW-OFFCANVAS` (механизм найден: 3440px в 2133px viewport, `scrollWidth == innerWidth`, третья
кнопка «fit» чинит) · `INFOSLOT-CLIP` · `SESSION-DROP` (механизм: падает контур Blazor, страница
выглядит живой, клики молча игнорируются) · `QGRID-DASHES-BEZEQ` · `WFM-N-ZERO` ·
`ASD-AGENTS-ZERO-LABEL` · `ASD-CONNECTING` (сегодня НЕ воспроизводится после рестарта, причина
НЕ найдена — не закрывать).
Найдено за день дополнительно: `DAYTREND-UTC-DAY` (день по UTC против местных данных — 00:00-03:00
местного виджет строит ВЧЕРА) · `CATALOG-UNSHIPPED` (`metrics-catalog.json` на сервере есть, сборкой
не поставляется; хеши сервер==репо, `8DE3A098…F1CD`) · `SEC-XMLCRYPTO-HIGH` (5 advisories HIGH на
`System.Security.Cryptography.Xml` 8.0.3, стоят давно, не регрессия) · `ENGINE-ERRORS-1719`
(1719 ERROR/FATAL в логе движка за сутки — фон, в котором новый дефект неразличим) ·
плюс `EF1002` (`SqlQueryRaw` с интерполяцией, `InfoSlotHandlers.cs:153/:239`).

## ⚠ НАЙДЕНО, ГДЕ ЛЕЖАТ ЛОГИ ДВИЖКА — это стоило трёх заходов
**`C:\RTMView\RTM\Logs\RTM.log`.** НЕ `C:\Logs\RTM\log.txt` (не писан с 29.08) и не
`C:\Logs\RTMViewShell\` (это шелл). Два негодных нуля получены именно из-за поиска не там.

## §B — УРОКИ ЭТОГО ДНЯ, дорого оплаченные
1. **Маска формата — не значение.** Я объявил формат даты ложным следом, сверив маски в трёх местах.
   Маски совпадали; ломалась КУЛЬТУРА. Причина бага пролежала лишний час из-за моего же вывода.
2. **Поведение выводить из тела скрипта, а не из имени флага.** Я утверждал, что `-SkipRTM` не
   тронет движок. `Update-RTMView.ps1:106-115` останавливает ОБЕ службы безусловным циклом; флаг
   охраняет только бэкап и копирование. Поймал devops, до прогона.
3. **Число или sha из документа предшественника — НЕ пин.** Протащил «24 непушенных» в директиву
   бекенду и «`d1982de` на 234» в постановку девопсу. Оба поймали специалисты.
4. **Отрицательный результат действителен только с доказательством, что источник живой СЕЙЧАС.**
   devops трижды отказался засчитать негодный ноль за ответ. Без этих отказов вопрос по метрике
   был бы закрыт ложно.
5. **Размер файла, который держит служба, не является мерой его содержимого** (Windows не обновляет
   запись каталога). Гейт `Length -gt 0` отбросил бы единственный файл с данными.
6. **Два замера, снятые по разным поводам, вместе дают ответ, которого не даёт ни один.** Различение
   «перестала падать» против «перестала запрашиваться» сделано логом ШЕЛЛА, а не логом движка.

## ЧТО Я БЫ СДЕЛАЛ ПЕРВЫМ НА МЕСТЕ ПРЕЕМНИКА
Спросить оператора формат разбора 13 висящих предметов — это дешевле любого замера и разблокирует
всё остальное. Затем `QGRID-78-RTS`: владелец уже найден, ближе всех к закрытию.

— coordinator-0831, второй аккаунт
