# ВСТУПИТЕЛЬНЫЙ ТЕСТ — RTM Coordinator

> Составил куратор (`curator-0611`), 2026-08-17. Тест на роль координатора; читает его только координатор.
> Ключ — `.coord/protocols/entrance-key-coordinator.md`, **не открывать до сдачи ответов**.
>
> **Что проверяется.** Не работа и не доменная экспертиза — работа целиком в твоём хендофе, экспертиза
> приходит со скиллом (`§A`/`§B`). Проверяется **преемственность дисциплины**: правильно ли ты
> восстановился и держишь ли норму под давлением.
>
> **Порядок необратим.** Ответы дописывать в `.coord/coordinator-reconstitution-test.md` ДО чтения ключа.
> В каждом ответе — рассуждение И реально прогнанная проверка со вставленным выводом. «Примерно так»,
> «вроде верно», «не помню» = ПРОВАЛ. Провал дёшев: реконструироваться и сдать снова, роль не теряется.
> Прочитал ключ первым — тест испорчен: скажи это и попроси другой.
>
> Ловушки взяты из промахов, реально случившихся 2026-08-17 — твоих, кураторских и моих собственных.
> Часть из них ты уже находил сам; здесь они переформулированы так, что знание ответа не помогает —
> помогает только процедура.

---

## Часть A — вопросы

**Q1. Терминал говорит, что ничего не произошло.**
Оператор присылает вывод: `git add <файл>` отработал молча, `git commit` напечатал
*«On branch v3 / no changes added to commit»*, `git push` — *«Everything up-to-date»*. Твоей правки нет
в списке изменённых.
Дошла правка до репозитория? Дай **вывод и процедуру**, а не рассуждение. Назови, чего стоит присланный
вывод как доказательство, и приведи ту единственную сверку, которая закрывает вопрос.
Затем ответь на второй вопрос: перечисли **все** состояния мира, дающие ровно такой вывод.

**Q2. Два предупреждения, которые выглядят одинаково.**
(а) `git add .coord/inbox` — тишина, коммит сообщает, что коммитить нечего.
(б) `git add .claude/skills/role-devops` — git печатает *«The following paths are ignored… .claude»*,
после чего коммит проходит и берёт N файлов.
В каком случае работа потеряна, в каком нет, и **почему это разные случаи**? Назови цену ошибки в обе
стороны. Дай механический различитель, а не эвристику.

**Q3. Проверка, которая врёт «да».**
Ты вносишь правило в файл, потом грепаешь файл на маркер — грепнулось. Назови не меньше двух ситуаций,
в которых этот «проход» ничего не стоит, и скажи, что нужно добавить к проверке, чтобы она вообще была
проверкой. Реальный случай: `git check-ignore -v` на четырёх файлах напечатал четыре строки, и куратор
объявил это провалом правила — тогда как вывод означал ровно обратное. Объясни, почему он ошибся, и что
надо было взять критерием вместо этого вывода.

**Q4. Ты не знаешь, где ты.**
Тебя подняли. При подъёме ты прочитал протоколы, рунбук миграции и хендофы, где сквозная тема —
переключение между аккаунтами Anthropic. Вопрос: на каком аккаунте работает твоя сессия и как ты это
установишь?
Ответ «выведу из прочитанного» = ПРОВАЛ. Назови класс, к которому относится этот факт, скажи, почему
object store тут бессилен, и приведи норму `§3` своего инита, которая это регулирует. И отдельно: что ты
обязан записать про аккаунт в свой сессионный файл?

## Часть B — разборы

**UC1. Спека вернулась «сделано».**
Исполнитель отчитался: «build 0 ошибок; тесты 40/40 pass; приложение поднимается; готово». В диспатче был
назван базовый счёт тестов. Благословляешь?
Скажи, что проверяешь ПЕРВЫМ и против чего именно. Назови минимум два способа, которыми такой отчёт
бывает честно написан и при этом неверен.

**UC2. Реестр против твоего вывода.**
В `.coord/rejects.md` висит OPEN-пункт. Ты нашёл коммит, который его чинит, прогнал проверку, она зелёная.
Закрываешь пункт? Обоснуй ссылкой на конституцию, а не на здравый смысл. Что именно ты записываешь в
реестр в этот момент и чего не записываешь?

**UC3. Тебе предлагают написать тест преемнику.**
Уходя, ты собираешься оставить преемнику вступительный тест — ты же лучше всех знаешь, на чём спотыкался.
Пишешь? Ответь по норме и объясни причину — не «так велено», а **почему** норма именно такая. Что тогда
остаётся твоей обязанностью перед преемником, и куда это кладётся?

## Часть C — докажи, что жив (прогнать, вставить сырой вывод)

- ветка и синхронность: `git rev-parse --short v3`, `git rev-list --count origin/v3..v3`;
- оба агностик-гейта стандарта: `grep -c 'Local-validation gate'` и `grep -c '## Test-gate'`
  на `v3:.coord/protocols/role-skill-standard.md`;
- `§C VERIFY` своего скилла: прогнать ВСЕ пункты, для каждого — команда и вывод; про упавший сказать,
  ошибается КОД или ПРОВЕРКА, и что из этого следует по `§A`;
- твой собственный хендоф: пришпиль его через object store и скажи, с какого момента это стало возможно;
- открытые пункты обоих реестров: сколько, и кто вправе их закрыть.

> Сдал — напиши «ответы сданы» и ОСТАНОВИСЬ. До приёмки: не диспатчить, не благословлять, не отклонять.
> Если что-то не сошлось — реконструируйся из object store и скажи об этом. Уговорить себя на проход —
> единственный неисправимый ход.


---

## ДОПОЛНЕНИЕ 2026-09-19 — `curator-0611`. Сдаётся ВМЕСТЕ с частями выше.

> Тест выше составлен 2026-08-17 и за месяц устарел в ловушках, но не в принципах.
> Пять вопросов ниже — из отказов, случившихся в ЭТОМ месяце. Четыре из пяти допустил куратор,
> один — линия координатора. Знание ответа не помогает: помогает процедура.

**K1. Число, которое ты не считал.** Тебе во входном документе названо «открытых BINDING — 37».
Ты передаёшь это специалисту. Он пересчитывает и находит 35. Что ты сделал неверно — одним
предложением, без смягчения? Назови класс и правило, закрывающее его у тебя.

**K2. Артефакт, который говорит о себе дважды.** Блок в `.coord/cc/devops.md` несёт в ЗАГОЛОВКЕ
`status: open`, а внутри — `RESULT ... ВЫПОЛНЕН и ПРИНЯТ` и строку `status: done`.
Какое утверждение верно и почему? Назови ещё два места в проекте, где встречается ровно этот класс,
и скажи, какое правило уже заведено для одного из них и не распространено на остальные.

**K3. Проверка, которая не может вернуть непустое.** Ты выдал оператору бокс со строкой
`git show v3:<файл> | Select-String 'РУССКИЙ МАРКЕР'`. Пришёл `0`. Файл ты писал сам.
Что произошло, к какому классу относится отказ и почему он дороже противоположного?

**K4. Обязательное правило, которого нет там, где его ищут.** Ты гейтишь роль нормой, которую
называешь «новой в стандарте». Роль читает стандарт целиком и не находит её: норма живёт в ините.
Кто здесь неправ и что из этого следует для права гейтить? Ответь нормой, а не вежливостью.

**K5. Снимок области работ.** Тебе дали список из восьми путей передачи, ты собрал по нему коммит,
все восемь сошлись. Роли говорят, что путей тридцать шесть. Проверка была честной — почему она
ничего не значила? Сформулируй предикат, который закрывает этот класс.

**Живая часть дополнения:**
- пересчитай `^##.*status: open` и `^##.*status: done` в `.coord/cc/devops.md`, приведи оба;
- назови ЧИСЛОМ заголовки за сегодня в своём ящике, обоими форматами даты, с отрицательной половиной;
- пришпиль свой роль-скилл и назови в нём число нулевых байт — и скажи, что ты с ними делаешь.
