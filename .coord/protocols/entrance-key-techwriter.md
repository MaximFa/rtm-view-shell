# КЛЮЧ — RTM Tech Writer (роль-специфичная часть)

> **Открывать только после сдачи ответов.** Парный к `entrance-key-COMMON.md`.
> Проверки, а не выводы; числа пере-снимать теми же командами на текущем `v3`.
> Презумпция провала: неоднозначность = провал.

**KR (разбор).**
Генераторы имеют **захардкоженное тело и не читают `editing/*.md`** — правка в одном месте расходится
молча. Вносить надо в ОБА. Поймать было обязано на верификации: **извлечь текст из готового .docx**
(`zipfile` → `document.xml`) и убедиться, что токены правки там есть. Доверие к тому, что генератор
отработал, — это `tool success ≠ delivery` в чистом виде.
ПРОВАЛ: «пересоберу ещё раз» без объяснения механизма.
Проверка: `git show v3:.coord/migration/project-memory/docx-gen-divergence.md | grep -ci 'zipfile'` → **≥1**.

**Живая часть.**
- `§C` прогнан ПОЛНОСТЬЮ, с сырым выводом по каждому пункту. Пересказ = провал пункта.
  Упавший пункт сам по себе не провал; провал — не назвать сторону ошибки и не сослаться на «Reality wins».
- Роль-скилл пришпилен: `git rev-parse v3:.claude/skills/role-techwriter/role-techwriter.md` даёт sha.
- **Вопрос о проверках — ключевой.** Для `role-metrics` и `role-security` ответ «мои идут через object
  store» ВЕРЕН — их `§C` написаны на `git show`/`ls-tree`/`grep`. Для остальных ответ обратный: их `§C`
  опираются на рабочее дерево (`Test-Path`, `Select-String`, `grep` по файлам), то есть подтверждают
  истины по маунту — поверхности, которой NORM-CUR-13 не верит.
  Засчитывается: кандидат назвал своё положение сам, с примерами пунктов, и объяснил опасность
  (проверка подтверждает истину по недоверенному источнику → ложная зелёная).
  ПРОВАЛ: «мои проверки в порядке» без разбора.
