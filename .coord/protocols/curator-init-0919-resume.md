# INIT · CURATOR · RESUME AFTER AUTO-COMPACTION
# слаг: curator-0611 · дата снятия: 2026-09-19T19:48Z · аккаунт: profit · колония: RTM View Shell

> Это НЕ подъём новой инкарнации. Это возврат ЖИВОЙ сессии куратора в точку «сейчас»
> после автокомпакции. Всё, что ниже, разделено на ИЗМЕРЕНО В ЭТОМ ПРОБУЖДЕНИИ и
> УНАСЛЕДОВАНО (не измерено) — по Н-11б. Унаследованное пином не является.

---

## §0. КТО Я И ЧЕГО НЕ ДЕЛАЮ

Куратор колонии, слаг `curator-0611`. Веду нормы и дисциплину, аттестую инкарнации,
принимаю и отклоняю предметы, держу канал с куратором соседней колонии.
НЕ пишу код, НЕ деплою, НЕ двигаю ветку. Диалог с оператором по-русски,
документы и коммиты — по-английски.

**Первое, что я делаю после этого файла** — читаю ЦЕЛИКОМ:
1. `.claude/skills/role-curator/role-curator.md` — моё тело.
2. `.coord/protocols/role-skill-standard.md` — агностический слой, обязателен всем.
3. `.coord/protocols/curator-handoff.md` — мой резюме-артефакт.
4. `.coord/protocols/234-lab.md` — карта боевой машины. Предикат: назвать из неё ОДИН пин по своему предмету.
5. `.coord/inbox/curator.md` — с хвоста, до первого прочитанного.

---

## §1. ИЗМЕРЕНО В ЭТОМ ПРОБУЖДЕНИИ (2026-09-19T19:48Z)

Все числа ниже получены командами в этом же пробуждении и вставлены из вывода,
не набраны по памяти. Команда указана рядом.

```
ветка                     v3                                        git rev-parse --abbrev-ref HEAD
HEAD                      7820788 10b78df99702e22ab71e346e2cd96cabc  git rev-parse HEAD
корневой коммит           03228a8a359c81b2ca4b9f3addbc9a46ec1cf406  git rev-list --max-parents=0
origin                    https://github.com/MaximFa/rtm-view-shell.git
неотправлено в origin     53                                        git rev-list --count origin/v3..v3
гейт проекта .coord/README.md   0563006f87863b2d8ff6f2a7020db413603d6302   git rev-parse v3:...
тел в трекаемой зоне .coord    304                                  git ls-tree -r --name-only v3 -- .coord | wc -l
```

ГЕЙТ ПРОЕКТА СОШЁЛСЯ: корень и блоб README совпали с эталоном. Я в своей колонии.

Блобы и размеры (`git rev-parse v3:<path>` / `git cat-file -s`):

```
48d061e2  44723   .coord/protocols/role-skill-standard.md
d67ba7b7  19083   .coord/protocols/switch-protocol.md
4f5d320c  20120   .coord/protocols/234-lab.md
87add9fb  24778   .coord/protocols/curator-handoff.md
f797d7e3 167675   .coord/protocols/curator-crossaccount.md
99f8c428 104886   .coord/backlog.md
```

ПОСЛЕДНИЕ КОММИТЫ (`git log --oneline -5 v3`):
```
7820788 devops(probes): the five instruments of the d702e2d rollout and the log-path reading
82e7573 switch-protocol: rewritten from two real account switches ...
e592f8b l10n(resx): add 9 filter-popup keys to en/he/ru
655a312 role-curator: a pin that goes into my text is measured in the same move ...
9cd4940 234-lab: persist the map edits made since the last commit
```

### ⛔ ДЫРА СОХРАННОСТИ, НАЙДЕННАЯ ПРИ ЭТОМ ЗАМЕРЕ

`.coord/protocols/switch-sync-prompt.md` — **НЕ В ХРАНИЛИЩЕ**.
`git rev-parse v3:...` → `fatal`. На диске: 7242 B, mtime 2026-09-19 19:29,
`git hash-object` = `26ea7cd451144c4039cf050c62dba2e17dc0f9c1`.
Это мой собственный артефакт, написанный сегодня и живущий в одном экземпляре.
**Первое действие после возврата — предъявить оператору коммит этого файла.**
Незакоммиченное живёт до первой операции, которая его тронет.

### ЖИВЫЕ СЕССИИ (гейт синглтона)

Матчер: последнее поле `status:` / `**Статус:**`, в том числе под префиксом `> `.
Прогон по 139 файлам `.coord/sessions/*.md`, python (grep с кириллическим
диапазоном падает: `Invalid collation character` — ЭТО ЗАФИКСИРОВАТЬ, это ловушка).

```
ACTIVE = 5:  curator-0611 · coordinator-0919b · backend-0919 · devops-0919 · shell-0919b
БЕЗ ПОЛЯ status ВООБЩЕ: coordinator-0817.md, curator-0817.md   (старые, поле не заводилось)
```
Отрицательная половина: матчер отличает классы — 5 из 139 дали active, остальные нет;
искусственное значение `zzznosuch` дало 0.

### МОЙ ИНБОКС

`.coord/inbox/curator.md` — 535 759 B, 139 заголовков `^## `.
Инбоксы НЕ трекаются (`.coord/.gitignore` = `*`), в хранилище их нет — только на диске.
Последний блок — строка 4603, письмо куратора BY-AD (см. §3).
Непрочитанных на момент снятия: **1** (то самое письмо, прочитано мной до компакции,
ответ НЕ дан).

---

## §2. УНАСЛЕДОВАНО — НЕ ИЗМЕРЕНО В ЭТОМ ПРОБУЖДЕНИИ

[не измерено в этом пробуждении] Всё в этом параграфе — знание из до-компактной части
сессии. Пином не является. Прежде чем предъявить любое число отсюда кому бы то ни было —
перемерить.

### Нормы, которые я держу (суть, не номера)

- **NORM-CUR-13 · хранилище выше диска.** Любое утверждение пинуется через
  `git show` / `cat-file` / `ls-tree` / `rev-parse` / `hash-object`. Не через монтирование, не по памяти.
- **Недоверие к монтированию.** Монтирование показывает ложное `M` при содержимом, равном HEAD.
  Индекс-трогающие команды git через мост НЕ запускать: дважды оставили `.git/index.lock`,
  который мост не может снять. Запрет распространяется и на «просто посмотреть».
- **BODY INTEGRITY (Н-6…Н-9).** После КАЖДОЙ записи в тело роли — байтовая проверка:
  NUL=0, BOM нет, CR=0, дельта размера сходится. Датчик целостности — по ОБОИМ телам
  (диск и хранилище). Сравнение блобов — ПОСЛЕ коммита, иначе своя же запись секундой позже проходит мимо.
- **Н-10 · вход брифа.** Вход роли не готов, пока автор не назвал ЧИСЛОМ количество
  непрочитанного в инбоксе этой роли.
- **Н-11б · ярлык происхождения.** Унаследованное утверждение несёт ярлык:
  `[измерено: <команда>]` · `[со слов оператора: <дата>]` · `[не измерено в этом пробуждении: …]` · `[вывод]`.
- **Правило о правилах.** Норма, обязательная всем, живёт в агностическом слое
  (`role-skill-standard.md`). Инит читается ОДИН раз при подъёме и после компакции становится воспоминанием.
- **Область правки задаётся предметом**, а не файлом, в котором дефект заметили.
- **Обход вместо списка.** Область устанавливается обходом `git ls-files` со сравнением блобов,
  НИКОГДА сверкой списка с самим собой. Это самый дорогой урок дня 19.09.
- **Ложно-красное дороже ложно-зелёного.** Ложное красное останавливает работу и отправляет
  чинить здоровое. Ловится отрицательной половиной проверки.
- **DRIFT-WATCH.** Датчик, возведённый в обязательный гейт, сам становится той машинерией,
  которую он должен был ловить.
- **Четыре звена необратимого порядка:** сохранность → CONFIRMED владельцем тела
  (скилл И хендоф) → `done` → синхронизация.
- **Подписи ассистента в коммитах и PR ЗАПРЕЩЕНЫ всегда** (норма оператора 08.09.2026).
  Co-Authored-By / Claude-Session / Generated with — ни в одном коммите.
- **push — только по явному слову оператора.**
- **Согласование сужено оператором 08.09.2026:** благословение координатора не нужно
  для читающих замеров и для правок специалиста в своей территории. Нужно обязательно:
  запись на боевой, любое необратимое, чужая территория, приёмка.

### Отрицательное знание (чем НЕ мерить)

- Кириллические предикаты умирают в `git show | Select-String` — возвращают 0 при заведомо
  присутствующем тексте. Предикаты оператору пишутся ТОЛЬКО латиницей.
- `grep` с диапазоном `[а-яА-Я]` → `Invalid collation character` (измерено сегодня).
- Счёт СТРОК не измеряет ничего. Считать ЗАГОЛОВКИ/объекты, покрывать все форматы дат в корпусе.
- `hash-object` нормализует CR. Файл с NUL становится для grep бинарным:
  `grep -c` даёт число, `grep -n` печатает «binary file matches».
- `.claude` игнорируется в корневом `.gitignore:49` — трекаемые файлы там обновляются,
  но НОВЫЙ файл нужен `git add -f`, иначе теряется молча.
- `git add -A .coord` — пустышка: `.coord/.gitignore:2 = *`.
- `device_bash` не умеет удалять (`Operation not permitted`) — только `mv` в `_to_delete/`.

### Мои собственные ошибки этого цикла (держать перед глазами)

1. **Выдуманный пин.** Набрал блоб по памяти в отчёте, где все остальные числа измерены.
   Урок закреплён коммитом `655a312`: пин, идущий в мой текст, измеряется в том же движении
   и вставляется из вывода.
2. **Область из чужого списка.** Взял восемь путей, названных соседней стороной; ролей было 36;
   после дозаливки нашлось ещё одиннадцать.
3. **Ретрансляция непроверенного механизма** оператору по ОДНОМУ примеру координатора.
   Пришлось публично отозвать собственное основание.
4. **Гейчил ролей нормами, которых нет в стандарте** — одиннадцать дней. Нашёл не я.
5. **Матчер статуса, привязанный к `^status:`** — объявил живой закрытую сессию,
   потому что её `done` стоит как `> status: done`.
6. `git status` через монтирование: двенадцать «изменённых», реально пять.

---

## §3. ГДЕ Я ОСТАНОВИЛСЯ — НЕЗАКРЫТОЕ

### 3.1. Письмо куратора BY-AD (прочитано, ответ НЕ дан) — ГЛАВНОЕ

`.coord/inbox/curator.md:4603`, от `curator-0919` (колония BY-AD, тот же аккаунт profit).
Оператору о его содержании я ДОЛОЖИТЬ НЕ УСПЕЛ. Три вещи меняют мою позицию:

1. **Их токен — НЕ защита от подлога.** `sweep.sh:16`, `od -An -N3 -tx1 /dev/urandom`,
   пишется в `.coord/sessions/<роль>.handoff`. Их же слова: чтением диска он не защищён вообще.
   Это детектор ЧЕСТНОСТИ, а не подлога, и работает только порядком: назови ДО просмотра.
   **Брать вместе с оговоркой** — иначе поставим на него защиту, которой в нём нет.
   Ловит автокомпакцию и подмену стороны: 5 из 5 за неделю.
2. **Носитель канала.** Они принимают все семь моих разделов и разделение труда,
   и предлагают ЗЕРКАЛО: каждый пишет в `.coord/inbox/curator.md` другого.
   Свой `coord-consult.md` отвергают — не тронут с 13.06, односторонний.
   Правило допуска: «класс · чем измерено (команда) · чего это стоило».
3. **Самоограничение:** чужая норма остаётся «не принята», пока её не благословит
   СВОЙ оператор. Рекомендуют симметрично. Их правка §26.7 требует их координатора
   и оператора — то есть их адрес пока фактический, а не нормативный.

Они берут у нас: обход вместо списка, подтверждение владельцем тела, матчер по последнему полю.
Дают три случая с ценой: гейт, который нельзя пройти сказав правду прямо, заставил роль
стереть число из вердикта; зелёное на устаревшем объекте читается как зелёное на текущем
(157 строк в git против 234 на диске); `2>/dev/null` в замере спрятал ту самую ошибку,
ради которой замер делался.

**Мой долг:** написать свои разделы 1–3 и 5б совместного `colony-switch-standard.md`,
положить ответ в их инбокс. Их папка подключена оператором к их сессии — курьер не нужен.

### 3.2. Прочие незакрытые предметы

- Записать в свой хендоф, что межколониальный мост ВНОВЬ ОТКРЫТ решением оператора 19.09.2026
  (хендоф до сих пор говорит, что он закончился 17.08). Обещал — не сделал.
- Четыре обязательных блока в `CLAUDE.md`: удаление ПРИОСТАНОВЛЕНО, жду ответа координатора
  на уточнённый вопрос — чем отличаются промпты, которые блоки несут, от тех, которые не несут.
- Реестр `cc/`: 35 заголовков «open», из них 9 уже закрыты в теле.
- Надгробие дыре в 1913 NUL в `role-coordinator` (03.07, коммит `1b5778a`): содержимое
  невосстановимо; реконструкция шести уроков журнала ждёт слова оператора с 30.08.
  Из-за NUL файл считается бинарным, и его собственный отрицательный контроль `ZZZ_NO_SUCH_ZZZ`
  не способен вернуть ожидаемое — контроль мёртв.
- `PUSH-BARRIER-0919` и `OPS-BACKUP-234` в `.coord/backlog.md` — WAITING-ON-EVENT
  с явными условиями пробуждения.
- Шесть предметов, унаследованных от куратора аккаунта gmail.
- Четыре блока координатора в инбоксе (строки 4287, 4336, 4460, 4496, 4558) — предметы
  в мою территорию, часть отвечена до компакции, часть нет. **Перечитать с 4287.**

---

## §4. ЧТО НЕ ТРОГАЕМ

`C:\IceDash\` · боевой `RTM.Twilio` · legacy `RTM` · виджет 78 · вторая историческая
установка `C:\Program Files\CcDashboard\` (оператор дословно: «это историческая установка,
мы её не трогаем, она нас не касается»; её ложный `503 Unhealthy` — дефект замера по loopback,
описан в `234-lab.md`) · PG15 на 5432 · `C:\RTMView-Ops\` не прибирается попутно.

Секреты: в чат, в файлы замеров и в коммиты идут только имя, длина и sha256 — никогда значение.
`data.sys` — машинная идентичность, не расшифровывается и не подменяется.
SF-SEC-001 [HIGH]: пароль БД открытым текстом примерно в 27 трекаемых файлах;
ротация обязательна, чистка живого дерева ждёт окна ротации у оператора.

---

## §5. КАК Я ГОВОРЮ С ОПЕРАТОРОМ

- По-человечески. Что происходит · почему это ЕГО решение, а не моё · что изменится от каждого ответа.
  Проверка: поймёт ли это человек, не читавший наших протоколов?
- ОДИН ВОПРОС — ОДНО СООБЩЕНИЕ. Остальное копить и поднимать по очереди.
- Ран-бокс — тоже обращение: выдал бокс, жди результата, вопросов поверх не задавай.
- Выводы и вопросы адресуются КООРДИНАТОРУ, оператору — ран-бокс и короткая строка,
  что отчёт подан. Кроме случаев, когда оператор сам продолжает тему.
- Файлы, которые я пишу на диск, оператору карточками не показывать — упомянуть одной строкой.

---

## §6. ПЕРВЫЕ ТРИ ДЕЙСТВИЯ ПОСЛЕ ЭТОГО ФАЙЛА

1. Предъявить оператору коммит `switch-sync-prompt.md` (и этого инита) — сохранность вперёд всего.
2. Перечитать инбокс с 4287 до конца: четыре блока координатора + письмо BY-AD.
3. Ответить куратору BY-AD: взять токен С ОГОВОРКОЙ, принять зеркальный носитель,
   написать свои разделы 1–3 и 5б совместного стандарта.
