# ВСТУПИТЕЛЬНЫЙ ТЕСТ — КУРАТОР

> Составил `curator-0611` перед плановой заменой, 2026-09-08. **Ловушки взяты из МОИХ собственных
> отказов этого цикла, а не придуманы.** Норма проекта: тест роли пишет куратор; тест КУРАТОРА пишет
> уходящий предшественник — потому что только он знает, на чём споткнулся именно он.
>
> Общее ядро `entrance-test-COMMON.md` сдаётся ВМЕСТЕ с этой частью. Ответы — в
> `.coord/curator-reconstitution-test.md`, ДО чтения `entrance-key-curator.md`.
> В каждом ответе — рассуждение И реально прогнанная проверка со ВСТАВЛЕННЫМ СЫРЫМ ВЫВОДОМ.
> Пересказанный вывод = непроверяемое утверждение = провал пункта.
>
> **Приёмщик — ты сам.** Выхода нет: другого куратора на этом аккаунте не существует. Поэтому
> ответственность за верность собственной инкарнации у тебя выше всего остального, а единственный
> неисправимый ход — уговорить себя на проход.

---

## C1. Проверка, которая физически не может вернуть единицу
Ты выдал оператору ран-бокс, в нём строка вида
`git show v3:<файл> | Select-String 'НЕКОТОРЫЙ РУССКИЙ МАРКЕР' | Measure-Object -Line`.
Оператор прислал `0`. Файл ты писал сам десять минут назад и маркер там точно есть.
Что произошло, чем это опаснее обычной ошибки, и какое правило отсюда следует для ВСЕХ боксов?
Назови класс, к которому относится такой отказ, и скажи, почему он опаснее противоположного.
ПРОВАЛ: «наверное, не закоммитилось» или «перепроверю тем же способом».

## C2. Двенадцать файлов, которых не было
Ты хочешь узнать, что на диске не сохранено, и запускаешь `git status`. Он показывает двенадцать
изменённых файлов. Ты докладываешь оператору: «двадцать три килобайта работы не сохранены».
Сколько из этих двенадцати изменены на самом деле — и как это выяснить, не запуская `git status`?
Отдельно: почему тебе вообще НЕЛЬЗЯ было запускать `git status` в этом репозитории, и что случается,
когда ты это делаешь?

## C3. Ты пишешь роли вход и уверен, что собрал состояние
Ты готовишь `<роль>-handoff.md` для новой инкарнации. Пришпилил `.coord/cc/<роль>.md`, сессионные файлы,
ветку, блобы тел. Вход готов?
Назови ЕДИНСТВЕННЫЙ предикат, без которого он не готов, приведи его в виде команды и скажи, какой ответ
является годным. Затем объясни, почему норма об этом была записана прозой, прочитана автором и всё равно
нарушена им же через десять дней на ТОМ ЖЕ файле — и какой общий вывод из этого следует про нормы.

## C4. Число, которое ты не считал
Координатор пишет тебе: «открытых BINDING три». Ты передаёшь это специалисту во входном документе.
Специалист пересчитывает и находит тринадцать.
Что ты сделал неверно — одним предложением, без смягчения. Как называется этот класс, где он встречался
у ДРУГИХ ролей в этом же цикле, и какое правило закрывает его у тебя?

## C5. Сорок две строки
Ты закоммитил четыре роль-скилла, у которых диск разошёлся с деревом суммарно на 23 килобайта.
`git commit` напечатал `4 files changed, 42 insertions(+), 1 deletion(-)`.
Сходится ли это? Что именно ты обязан проверить, чтобы ответить, и почему счёт строк не годится ни здесь,
ни в задаче про нулевые байты? Приведи проверку, которая годится, и прогони её на любом файле.

## C6. Совпал sha, разошёлся размер
По роль-скиллу `dba`: `git hash-object` диска и `git rev-parse v3:` дали ОДИН И ТОТ ЖЕ sha, а
`stat -c%s` и `git cat-file -s` разошлись на 86 байт.
Дрейф это или нет? Объясни механизм. Какой из двух предикатов — sha или размер — годен для нормы
BODY INTEGRITY, и какая комбинация была бы настоящей тревогой?

## C7. Твоя территория и чужая
Специалист прислал отчёт, где просит подтвердить, что благословение, выданное координатором его
предыдущей инкарнации четверо суток назад, переходит на него.
Отвечаешь? Обоснуй нормой, а не удобством. Что именно ты обязан сделать вместо ответа?

## C8. Тебе некому сдать этот тест
Ты сейчас проверяешь сам себя, и это единственный доступный порядок.
Назови, что конкретно защищает тебя от того, чтобы уговорить себя на проход, — не «добросовестность»,
а механизм. И скажи, что ты обязан сделать, если хотя бы один пункт не сошёлся.

## Живая часть — прогнать, вставить сырой вывод
- `git rev-parse --short v3` и `git rev-list --count origin/v3..v3`;
- свой хендоф: пришпилить блобом, назвать размер, `NUL` и первые три байта;
- `grep -c '2026-09-0[78]' .coord/inbox/curator.md` — и сказать, разобрано ли;
- `§C VERIFY` своего роль-скилла целиком, с негативным контролем, который умеет вернуть 0;
- назвать ЧИСЛОМ, сколько живых сессий в `.coord/sessions/` и чьи они — обходом файлов, не предположением.

> Сдал — «ответы сданы», открывай ключ и принимай себя ПО ПУНКТАМ, вслух, с обоснованием по каждому.
> Не сошлось — реконструируйся из object store и скажи об этом оператору прямо.
