> # ⚡ ПРОБУЖДЕНИЕ 2026-09-12 — читать ПЕРВЫМ, до всего остального
>
> **Что случилось.** Переустановка приложения Claude оборвала все сессии на аккаунте `profit`.
> Артефакты не пострадали: это разрыв СВЯЗИ, а не потеря данных. Клон, ветка и история целы.
>
> **Сессии 08.09 мертвы.** В `.coord/sessions/` у `coordinator-0908`, `curator-0908`, `devops-0908c`,
> `backend-0908`, `shell-0908` стоит `status: active` — это НЕСНЯТЫЙ статус ушедших сессий, а не живые
> инкарнации. Запись реестра не есть факт о жизни; свою синглтон-проверку строй на ДАТАХ, не на слове
> `active`.
>
> **Пины на 2026-09-12T13:2xZ, сняты куратором в этом пробуждении:**
> ```
> ветка v3 = 84bd7ce   origin/v3 = 79e3905   НЕПУШЕНО 24
> гейты стандарта: 'Local-validation gate' = 1, '## Test-gate' = 1
> ```
> Пины ниже по файлу — от 08.09 и старше. **Пере-снимай сам**, чужие пины не наследуются.
>
> **С 08.09 в колонии не двинулось НИЧЕГО.** Последний коммит `84bd7ce` от 08.09; последние записи во
> всех пяти живых инбоксах — 08.09; из файлов после 09.09 изменились только артефакты сборки
> `tools/Soma/bin|obj`. Значит **всё, что написано ниже, по состоянию действительно** — устарели только
> пины и тот факт, что сессии больше нет.
>
> **Непрочитано в `.coord/inbox/backend.md` (норма Н-10): 15 записей** после последней отметки
> `handled` (она от 31.08), из них 14 — сентябрьские.
> Последнее, что тебя ждёт: `coordinator-0908` — «НОРМА ИЗМЕНЕНА СЛОВОМ ОПЕРАТОРА: §4…».
>
> **Хендоф и инит пересоздавать НЕ НАДО.** Этот хендоф написан 08.09 уходящей сессией твоей роли,
> лежит в `v3` и байт-равен диску; инит по устройству без состояния и не протухает. Требовалась
> шапка пробуждения — она перед тобой.
>
> **Первое действие:** §1 своего инита целиком (пины, `§C VERIFY`, хендоф, инбокс, синглтон),
> затем разбор инбокса. До отчёта §5 куратору — не работай.
>
> — куратор `gmail` (`curator-0817`), 2026-09-12

# BACKEND HANDOFF / БРИФИНГ — RTM Server / Shell Backend

> ⚠ **Это НЕ хендоф предшественника.** Живая линия backend оборвалась 2026-07-22 и своего преемника не
> написала: файла `backend-handoff.md` не существовало вовсе до этой записи. Брифинг собран **куратором
> из артефактов** — инбокс роли, `role-backend §A/§B/§C`, реестры, дерево `v3`, переписка координатора.
> Каждый факт ниже либо пришпилен, либо помечен как непришпиленный.
> Читать ВМЕСТЕ с `.coord/protocols/init-backend.md` — там подъём, здесь состояние.
> Составлено 2026-08-31T06:20Z (часы устройства, `date -u`), куратор `curator-0817`.

## ▶ BOOT BLOCK (норма Н-1)
- start prompt: `.coord/protocols/init-backend.md` · этот хендоф: `.coord/protocols/backend-handoff.md`
- вступительный тест: `.coord/protocols/entrance-test-COMMON.md` + `entrance-test-backend.md`
- ответы: `.coord/backend-reconstitution-test.md` · инбокс: `.coord/inbox/backend.md`
- роль-скилл: `.claude/skills/role-backend/role-backend.md` · клон: `D:\Claude\Projects\RTM View Shell`, ветка `v3`
- **Тот же инит — промпт ВОЗОБНОВЛЕНИЯ после автокомпакции**, не только холодного старта.

## Пины, снятые при составлении

```
v3 = 26ecfcb · origin/v3 = f6d5c58 · origin/v3..v3 = 24 (непушенных)
role-backend: v3 blob 5981015 == hash-object диска 5981015   (версия 0.1, last_verified 2026-06-16)
db/functions/01_ngc_functions.sql: v3 blob 9e19e59 == диск 9e19e59
```

**Инбокс пришпилен ПО ДАТЕ, а не по наличию** (норма 2026-08-29, введена после того, как я же ошибся на
брифинге девопса): последняя запись в `.coord/inbox/backend.md` — **2026-07-22T13:40Z**, файл не менялся
с 2026-07-22. Сессионного файла backend в `.coord/sessions/` нет ни одного. `.coord/cc/backend.md` —
2026-07-22. Три независимых источника сходятся: **линии backend нет 40 дней.** Singleton проверь сам —
это твоя обязанность по иниту, а не мой вывод.

## Где линия оборвалась

2026-07-22, на закрытии WFM Phase 1. Последнее, что сделал `backend-0626`:

- Диагностировал C2 LIVE #2 — агрегатные BU («US All», «DE All») не давали WFM-снимка. Root оказался
  **KEY MISMATCH**: конфиг виджета хранит BU **id**, виджет `Get`-ит id, а цикл `Set`-ил **name**.
  Своё же предыдущее утверждение («виджет шлёт имя BU») он **отозвал** — координатор это отдельно
  отметил как правильное поведение.
- Написал и провёл фикс: `WfmRealtimeLoop` `buKey = BusinessUnitId.ToString()`, коллизия-чек на `buKey`.
  Приземлилось в **`d1982de`**, проверено им по object store.
- Дал ACK=READY на пуш-барьер (9 коммитов, WFM Phase 1). Барьер закрылся, `d1982de` на `origin/v3`.

**`d1982de` (2026-07-22) — последний ПРОДУКТОВЫЙ коммит проекта.** Всё, что легло после, — протокольное.

## Что произошло без тебя (короткий контур)

Продукт стоял 38 дней; 29–30 августа работа возобновилась вокруг **переноса на 234** (devops + координатор)
и живой продуктовой диагностики. Сейчас на ходу: `PR234-BU-01` (во всех виджетах пуст список Business Unit
на 234, при 59 строках в `NGC_BusinessUnit`; на **140 список работает** — ищут разницу между машинами,
140 только на чтение) и `§FEED-READY`. Твоего участия там пока не запрашивали, но см. ниже: один из
исходов диагностики адресует именно тебя.

## ⛔ IN-FLIGHT и открытое — твоё

1. **Дефект №1 адаптера — координатор поднял его выше остальных и несёт оператору первым пунктом.**
   `ArgumentNullException (Parameter 'key')` в `TwilioAdapter.removeInteraction:2255` ← `setInteractionsAsync:746`,
   **32 раза каждые 30 секунд**; встали `RunMaintenanceLoopAsync` и `ProcessRTDataQueueAsync`, службу
   подняли failure-actions. Это **в ревизии `8abd19a`**, то есть в том, что стоит и на 140.
   Пин: `.coord/inbox/devops.md`, блок `2026-08-30 ~06:2xZ`, п.5.
   **Факт, который иначе выведут неверно:** `8abd19a` — `fix(adapter): per-target connect snapshot`,
   **2026-07-12**, и он **НЕ на `v3`**: живёт на ветке `adapters`.
   ```
   git merge-base --is-ancestor 8abd19a v3   -> ложь
   git branch -a --contains 8abd19a          -> adapters, remotes/origin/adapters
   ```
   При норме «коммитим только в `v3`» это стоячее расхождение. **Вердикт по нему — координатора**, не
   твой и не мой; но не считай `8abd19a` частью `v3` и не ищи его в её истории.
2. **Реджект `6ebd39f` — STATUS: OPEN, «auto-reconnect not delivered».** Тёплый свап адаптера 14 июля
   на 140 провалился: новый адаптер не переподключился НИ к legacy `rtmpipe`, НИ к нашему `rtmpipe_v3`;
   откатились. **`6ebd39f` НЕ передеплоивать, пока backend не починит И не пройдёт живой warm-swap
   connect-тест.** Жёсткая директива оператора: **legacy RTM никогда не перезапускается нашей
   процедурой** — адаптер обязан сам переподключаться к живому неперезапущенному pipe-серверу; максимум,
   что можно бортануть, — наш `RTMService`. Урок оттуда: деплою адаптера нужен живой connect-lifecycle
   тест, а не только формат-раундтрип `[WIRE-01..05]`.
3. **Max Wait на F5** — `OWNER PINNED = RTM/backend`. Виджет очищен как добросовестный. По твоему же
   `§B` 2026-07-20 корень оказался **в шелловском виджете** (локальный тик и якорь), а не в RTM —
   прочитай оба урока 2026-07-20 подряд, они друг друга поправляют. Номер FIX-пуша `<pending>`.
4. **Defect I** (`23505 PK_RTSGrid_Column`, `RtsRepository.InsertQueueGridColumnAsync` неидемпотентен) —
   OPEN, ждёт операторского CONFIRM.
5. **`LoadData` startup-only** — класс, из которого выросло уже два реджекта (US Queue Grid; live pickup
   конфигурации). В `features.md` висит EPIC «LIVE (targeted) pickup всех UI-конфиг-изменений вместо
   рестарта RTM» — QUEUED/HELD, не одобрено к сборке.
6. **`PR234-BU-01` может приземлиться тебе.** Метод задан координатором: попарно 234 против 140, первым —
   ответ источника списка BU. Если источник на 234 отдаёт пусто, а на 140 непусто — причина ниже
   источника (данные/тенант) и это перенос, а не продуктовый баг; если оба отдают одинаково — причина
   выше, и тогда разговор к коду. **Не влезай раньше этой развилки** — диагностику ведёт devops.

## Расхождения, найденные куратором при составлении (разбирайся сам, чужое я не правлю)

**Твой `§C VERIFY` не сходится по двум пунктам из пяти. Прогнал через object store, оба тела совпадают
(диск == дерево, `9e19e59`), то есть это НЕ ложь маунта:**

```
§C#1  git show v3:db/functions/01_ngc_functions.sql | grep -c 'CREATE PROCEDURE'   -> 2      (ожидание >10)
§C#2  git grep -h 'p_tenant_id uuid' v3 -- 'db/functions/*.sql' | grep -v 'uuid)' | wc -l -> 25 (ожидание 0)
§C#3  git show v3:RTM/RTM/appsettings.json | grep -c 'TenantId'                    -> 1      PASS
§C#4  ...RtmRelayService.cs | grep -cE 'SubscribeUnionAsync|SubscribeGridAsync'    -> 2      PASS
§C#5  ...RtmRelayService.cs | grep -c 'JsonElement'                                -> 3      PASS
негативный контроль: тот же файл, grep -c 'ThisMarkerMustNotExist'                 -> 0      (предикат рабочий)
```

**Вердикта по ним я не выношу — это твой `§A` «Reality wins» и твой разбор.** Скажу только, что вопрос
стоит ровно так: **ошибается КОД или ПРОВЕРКА?** Подсказку не даю, но обрати внимание, что §C#2 ищет
`p_tenant_id uuid` в строке, не оканчивающейся на `)`, — а параметры в этих файлах записаны в несколько
строк. Отсюда до ответа один шаг, и шаг этот твой.

**Третье, общее:** весь твой `§C` написан против **рабочего дерева** (`grep` по файлам) — поверхности,
которой NORM-CUR-13 не верит. Из двенадцати ролей object-store-форму имеют `security`, `metrics` и —
с сегодняшнего дня — `devops` (он переписал свой `§C` сам, это было условием его аттестации). Тот же
вопрос заложен в твой вступительный тест: **засчитывается разбор, а не «у меня всё в порядке».**

**Четвёртое:** режим ЧП в твоём `§A` формально снят, но сохраняется **намеренно** решением оператора —
он дисциплинирует. **Не прибираться здесь.** Вопрос стоит открытым в `.coord/backlog.md`.

## Нормы, принятые пока роли не было (читать обязательно)

- **ПОСТАВКА ≠ ЗАКРЫТИЕ.** Сдал — пишешь `DELIVERED <sha> <UTC-дата>` в реестр; **закрывает пункт ТОЛЬКО
  оператор.** Молчащий реестр неотличим от полного — на этом ошиблись две сессии.
- **Ран-бокс оператору выдаёшь ТЫ**, владелец работы, а не координатор — и только после его §4-бласса.
- **Пробуждение после паузы = инит.** Критерий не «сколько прошло», а «проверял ли я это в текущем
  пробуждении». Ветку, sha, непушенные, чужие статусы — перепроверять, не вспоминать.
- **Аккаунты — не твоё дело**, топологию ведёт куратор; **среду из читаемых документов не выводить**.
- **Тест преемнику ты не пишешь** — его пишет куратор. Твоё на выходе: обновить этот хендоф (обновить,
  не переписать: append-only, устаревшее помечается superseded) и записать промахи цикла в `§B LESSONS`.
  **И закоммитить** — иначе первых двух пунктов не существует.
- **BODY INTEGRITY** (`092ff2e`): после каждой записи сверять байты — размер, NUL (`tr -dc '\000' | wc -c`),
  BOM, и хеш диска против блоба. `.claude/**` под игнором, но игнор действует ТОЛЬКО на неотслеживаемые пути:
  НОВЫЙ файл туда — `git add -f`, уже отслеживаемый коммитится обычным `git add`, без `-f` и без
  предупреждения (исправлено куратором 2026-08-31: прежняя формулировка подавала частный случай как
  общий и стоила ошибок трём ролям). И **писать в `.claude` инструментом доставки запрещено — только
  через `device_bash`**.
- Оператору — по одному обращению за раз, и **код-бокс тоже обращение**.

## Первое действие

1. Подъём по `.coord/protocols/init-backend.md` целиком.
2. **Вступительный тест: `entrance-test-COMMON.md` + `entrance-test-backend.md`.** Ответы —
   в `.coord/backend-reconstitution-test.md`, **до** чтения ключей. Сдал — «ответы сданы» и СТОП.
3. До приёмки: читать, разбирать инбокс, готовить — можно. Выдавать ран-боксы, писать код, трогать
   реестры, 234 и 140 — нельзя.
4. Singleton: `.coord/sessions/` на другой активный backend-слаг **и дата последней записи в
   `.coord/inbox/backend.md`**. Молчание реестра не есть факт об отсутствии работы.

— куратор `gmail` (`curator-0817`), 2026-08-31T06:20Z. Пины перепроверю при приёмке.


---

## ВХОД ДЛЯ ИНКАРНАЦИИ 2026-09-06 — записано куратором (пробуждение после паузы = инит)

**Пины на момент записи (ПЕРЕ-СНИМИ САМ, чужие пины не наследуются):** `v3 = 69fc306`,
`origin/v3 = 79e3905`, непушенных 9. Ревизия выката `d1982de`, на 234 стоит она же.
Предыдущая инкарнация `backend-0831` зажата куратором 2026-09-05, клеймы свободны: ты не продолжаешь
её разговор, ты поднимаешься на её результатах.

### ПРЕДМЕТ: осиротевшие ячейки Queue Grid — диагноз готов, «разберись» не требуется

`src/CcDashboard.Application/Commands/Dashboards/SaveQueueGridRtsCommand.cs:147-152` удаляет строки
грида с комментарием `// Cascade will delete cells`. **Это утверждение о схеме, которого в схеме нет:**
между `RTSGrid_Row` и `RTSGrid_Cell` внешних ключей ноль. Ноль ИЗМЕРЕН, а не выведен из молчания —
в том же `db/schema.sql` найдено 5 других `FOREIGN KEY`, то есть предикат умеет находить и умеет
вернуть 0. Проверено координатором и независимо пере-снято куратором 2026-09-06.

`RtsRepository.cs:245-249` — один сырой `DELETE` по строке, ячейки не трогаются.
Второе место с тем же дефектом: `RtsRepository.cs:155` — удаление грида целиком, тот же цикл.

**Подпись дефекта, из-за которой он и виден:** соседний блок ТОГО ЖЕ файла (`:71-77`, колонки)
написан в той же вере в каскад, но продублировал удаление ячеек явно «для ясности» — и случайно
спасся. Отсюда обе цифры за неделю: `Cell->Column` 21 -> 21, `Cell->Row` 21 -> 37.

**Практический смысл:** каждое сохранение экрана, убравшее строку Queue Grid, оставляет её ячейки
в базе навсегда.

### ЧТО НУЖНО И ЧЕГО ДЕЛАТЬ НЕЛЬЗЯ
**Нужно:** правка кода в ОБОИХ местах, и ответ на вопрос, почему автор считал, что каскад есть —
след намерения в истории или миграциях, либо прямо названное его отсутствие.
**ЗАПРЕЩЕНО:** чистить уже накопленные сироты. Это запись в живые данные, отдельная санкция оператора
и отдельный заход. Чинить механизм и подчищать последствия одним движением нельзя — иначе не отличить,
что подействовало.
**НЕ ТРОГАТЬ:** `1496` и `1504` — улика по параллельному предмету.

### ⛔ ГРАНИЦА, КОТОРУЮ НАЗВАЛ САМ КООРДИНАТОР
Диагноз принёс тебе он, а значит мог отравить вход собственной уверенностью. **Пере-сними пины сам:**
отсутствие FK по схеме, обе точки вызова, оба блока файла. Хоть один факт не сошёлся — **СТОП к нему**,
а не «наверное, он имел в виду другое».
Прецедент, ради которого это написано: `shell-0905` вчера получил от него подсказку про RTL — и она
оказалась ложным следом, умершим от измерения. Подсказка старшего не факт.

### КРИТЕРИЙ ПРИЁМКИ ФОРМУЛИРУЕТСЯ ДО ПРАВКИ
Как мы докажем, что сироты больше НЕ появляются — измеримо, а не «код выглядит правильно».
Сформулируй до того, как тронешь строку, и подай на §4 вместе с планом.

### ГРАНИЦЫ И ВЕДЕНИЕ
`NO push`, ветку не двигать. `§4` ведёт координатор: правка проходит у него ДО прогона; ран-бокс
авторства специалиста, оператору бокс несёшь ты сам. Вступительный тест не сдаёшь — роль аттестована
2026-08-31 после пересдачи одного пункта.
Параллельно и без пересечения: `shell-0905` — расхождение view/edit; `devops-0905` — ждёт санкции.


---

# ХЕНДОФ backend-0906 -> преемнику · 2026-09-08T12:06Z

> Append-only. Всё выше — брифинг куратора от 2026-08-31 и вход куратора от 2026-09-06; они
> ИСТОРИЯ, читать для контекста. **Действующее состояние роли — этот раздел.**

## §1 BOOT BLOCK
- инит-промпт: `.coord/protocols/init-backend.md` — выполнить ЦЕЛИКОМ, это не чтение, а действие
- этот хендоф: `.coord/protocols/backend-handoff.md`
- роль-скилл: `.claude/skills/role-backend/role-backend.md` (`§A` + `§B active` каждый инит; `§D` не грузить)
- инбокс: `.coord/inbox/backend.md` · исходящий: `.coord/inbox/coordinator.md`
- сессионный файл: `.coord/sessions/backend-<MMDD>.md`
- клон: `D:\Claude\Projects\RTM View Shell`, ветка `v3`. Клон в `C:\...\Documents\...` — СТЕЙЛ, не трогать.
- **Тот же инит — промпт возобновления после паузы и после сжатия контекста.**
- Вступительный тест не сдаёшь: роль аттестована 2026-08-31, PASS куратора подтверждён 2026-09-06.

## §2 ПИНЫ, СНЯТЫЕ В ЭТОМ ПРОБУЖДЕНИИ (2026-09-08T12:06Z) — ПЕРЕ-СНИМИ САМ
```
v3            = 3d3014688e6f554cba116fc78e09fca8e31aa169
origin/v3     = 79e3905f806532f54ee2e272108a213162669e7b
непушенных    = 17
backend-handoff.md            диск == v3:  4f00854275b4382e398cb6b74effaeb977faa9cc   (ДО этой дописи)
role-backend.md               диск == v3:  0884f63b0a9e29c38478846e10fe068fe3328eeb   (ДО дописи §B)
SaveQueueGridRtsCommand.cs    диск fb8bc0a08b9237856c6dbe1556562cc212a58b1d
                              v3:  75487a4aae89db6ecf532fb3f157c2251e9d5758
NUL по всем трём телам = 0
```
**Оба моих тела я только что изменил** (этот раздел + `§B LESSONS`), поэтому их хеши уже разошлись
с деревом — это ожидаемо и есть незакоммиченная работа, см. §3.4. Ветка за смену ушла
`9146aee -> 23cdbd6 -> 3d30146` чужими руками; за содержимым чужих коммитов я не следил.

## §3 ЧТО В РАБОТЕ ПРЯМО СЕЙЧАС И ЧЕЙ ХОД

### 3.1 Правка сирот Queue Grid — БЛАГОСЛОВЛЕНА, НЕ ЗАКОММИЧЕНА, ПРИЁМКА ЗАМОРОЖЕНА
Файл `src/CcDashboard.Application/Commands/Dashboards/SaveQueueGridRtsCommand.cs`, строки 149-153:
перед `DeleteQueueGridRowAsync` добавлен `DeleteQueueGridCellsByRowIdAsync` и заменён ложный
комментарий про каскад. §4 координатора пройден 2026-09-06, дифф принят целиком.
**Правка лежит только на диске.** Коммит я НЕ делал намеренно: `git add/commit` через маунт
оставляет `index.lock`, который мост снять не может (`init §3`), поэтому коммит был вынесен
в ран-бокс оператору — до которого мы не дошли. **Это не забывчивость, и не «потеряно».**
**Ход: не мой.** Приёмка заморожена координатором (§3.2), а не отменена.

### 3.2 ⛔ НЕЗАКРЫТЫЙ BINDING: противоречие о базе `rtmviewdb` — МОЙ ДОЛГ, Я ЕГО НЕ ИСПОЛНИЛ
`.coord/inbox/backend.md`, блок `2026-09-06T~11:1xZ`, адресован devops-0905 И мне.
Координатор потребовал от каждого НЕЗАВИСИМО прогнать идентификацию подключения:
`current_database()`, `inet_server_port()`, `inet_server_addr()`, `current_user`, `version()`,
`current_schemas(true)`; четыре `to_regclass` (`RTSGrid_Cell`, `RTSGrid_Row`, `NGC_BusinessUnit`,
`RTSData_Interaction`) с печатью null'ов; счётчики; `SELECT datname FROM pg_database`;
негативный контроль `to_regclass` по заведомо несуществующему имени; и дословную строку подключения.
**Я этот замер НЕ прогнал** — между постановкой и моей заменой пришёл срочный `PR234-QGRID-79`,
и я ушёл в него. Долг остался. **Пока он не снят, приёмка правки сирот заморожена.**
Суть противоречия: devops доложил, что в `rtmviewdb` НЕТ ни одной таблицы `RTSGrid_*`; я в той же
базе насчитал `RTSGrid_Cell = 338`. Оба утверждения не могут быть верны. Кто прав — решает замер,
не старшинство. **Это первое, что преемник должен закрыть.**

### 3.3 PR234-QGRID-79 — ДИАГНОЗ СДАН И ПРИНЯТ, ХОД У DEVOPS
`23505 PK_RTSGrid_Column` при сохранении Queue Grid на чистой установке 234 от 2026-09-08.
Мой вывод (принят координатором 2026-09-08): **дефект НЕ в коде.** `InsertQueueGridColumnAsync`
не выбирает `ColumnId` — его выдаёт база; сид `RESTART IDENTITY` + `COPY` с явными id 1..5 без
`setval` оставляет генератор на 1. Штатное лекарство уже написано: `Restore-All.ps1:138` шаг E3.
Предикат назван ДО замера: **`last_value` < `max(ColumnId)`** подтверждает, обратное — опровергает,
и тогда разбор заново, а не подгонка. Замер ушёл devops. Правку не делаем — «правка в коде
замаскировала бы кривую установку», формулировка принята координатором.
Координатор отдельно проверяет, не тот же ли это старый `Defect I` («метод неидемпотентен»).
**Ход: devops (числа) и координатор (Defect I). От меня — ничего до чисел.**

### 3.4 Незакоммиченное — три файла, коммит НЕ мой
`SaveQueueGridRtsCommand.cs` (правка сирот), `backend-handoff.md` (этот раздел),
`role-backend.md` (§B LESSONS). Коммитить через маунт нельзя — `index.lock`. Отдать оператору
одной строкой, когда у преемника будет свой ход к оператору.

### 3.5 Долг, записанный за ролью, НЕ гейт
- Транзакционность `SaveQueueGridRtsCommand` целиком (окно «строка без ячеек» при краше между двумя
  вызовами) — записана координатором в бэклог 2026-09-06, отдельный предмет.
- Перевод `§C VERIFY` на object-store-форму и закрытие двух несошедшихся пунктов (`§C#1`, `§C#2`).
  Куратор требует как долг и прямо сказал: **барьером перед продуктовой правкой не ставить.**
  Я его не исполнил и не выдавал исполненным.

## §4 ИНБОКС — ЧИСЛО И СОСТОЯНИЕ
`grep -c '2026-09-0[78]' .coord/inbox/backend.md` -> **4**.
Все четыре относятся к 2026-09-08 (за 07-е записей нет) и **разобраны**:
два блока `PR234-QGRID-79` (постановка и принятие диагноза) плюс два вхождения даты внутри них.
**Непрочитанного на момент выхода нет.** Но см. §3.2: блок от 2026-09-06 прочитан и НЕ ИСПОЛНЕН —
это другое, и оно хуже непрочитанного, потому что выглядит закрытым.

## §5 НЕГАТИВНОЕ ЗНАНИЕ — то, чего нет в git и что иначе будет куплено заново
1. **Диагноз старшего может содержать выдуманное место.** Координатор дал два дефектных места;
   второго не существует — он заключил по имени вызываемого метода, не дочитав охватывающий.
   Он сам это признал и забрал ошибку себе. Читай охватывающий метод целиком.
2. **Негативный контроль спас ложную приёмку.** Сироты не выросли ни разу на непочиненном
   (21 -> 21 -> 21). При обратном порядке мы записали бы «дельта 0» после правки в победу.
3. **Ложный след, умерший от измерения: «смотрим не ту базу».** Опровергнут тем, что экран нашёлся
   в этой же базе — **но опровержение негодное**: экран живёт в слое Shell, который есть в обеих
   картинах, то есть признак базы не различает. Различающий признак — наличие `RTSGrid_*`.
   Не повтори: опровергать среду нужно РАЗЛИЧАЮЩИМ признаком.
4. **Врал мой собственный предикат:** «миграций в проекте нет». Искал каталог по имени в корне,
   не нашёл, принял за отсутствие. Миграции есть: `src/CcDashboard.Infrastructure/Migrations/App/`.
   Отрицательное утверждение о существовании требует предиката, доказавшего умение НАХОДИТЬ.
5. **`dashboard_widgets.GridId` — НЕ ссылка на `RTSGrid_Grid`.** Это собственный identity таблицы
   виджетов, и конфигуратор показывает именно его («מזהה רשת»). Настоящая ссылка —
   `ConfigJson->>'gridId'`. Я потратил ход, ища грид 81, которого не существует.
6. **Канва редактора показывает состояние, которого в базе нет.** После F5 виджет с холста исчез,
   а в базе у грида 55 пять строк при двух на экране. Это граница shell, не backend; параллельно
   этим занимается `shell-0905` (расхождение view/edit). Не лезь туда backend-ом.
7. **`23505` на identity-ключе, который код не выбирает, — это сид, а не код.** См. §3.3 и `§B`.
8. **Сироты на локальной базе — один старый пучок** (`CellId 857..886` при вершине 1470), а не
   постоянная течь. Формулировка входа «каждое сохранение оставляет ячейки» измерением НЕ
   подтвердилась; координатор её снял как свою достройку. Цифры `21 -> 37` — с ДРУГОЙ базы,
   он это признал. Какой именно — на момент моего выхода не названо.

## §6 ЧЕГО ПРЕЕМНИКУ НЕ ДЕЛАТЬ И ПОЧЕМУ
- **НЕ чистить накопленные сироты.** Запись в живые данные, отдельная санкция оператора, отдельный
  заход. Чинить механизм и подчищать последствия одним движением нельзя — иначе не отличить,
  что подействовало.
- **НЕ трогать ячейки `1496` и `1504`** — улика по параллельному предмету.
- **НЕ править код под `PR234-QGRID-79`.** Это замаскирует кривую установку; отказ чинить у себя
  здесь есть работа, а не уклонение. Формулировка принята координатором.
- **НЕ запускать через маунт git-команды, трогающие индекс** (`status`, `add`, `diff`, `commit`):
  остаётся `index.lock`, мост снять его не может, репозиторий у оператора встаёт. Только читающие.
- **НЕ push, ветку не двигать.**
- **НЕ достраивать за старшего.** Прецедентов уже три за неделю: RTL у `shell-0905`, второе место
  у меня, «каждое сохранение». Не сошёлся факт — СТОП к нему, а не догадка о том, что он имел в виду.
- **НЕ считать приёмку сирот размороженной**, пока не снято противоречие §3.2.
- **НЕ ходить на 234.** Замеры там формулируются как читающие запросы и уходят через devops.
- **НЕ считать `§C VERIFY` прогнанным** — я его в свою смену не прогонял и сказал об этом прямо.
  «Не сделано и названо» лучше зелёной галочки на недоверенной поверхности; не превращай моё
  честное «не делал» в чужое «сделано».

— backend-0906, profit, 2026-09-08T12:06Z
