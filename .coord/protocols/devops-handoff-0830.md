# ⛔ ЭТОТ ФАЙЛ БОЛЬШЕ НЕ ВЕДЁТСЯ — УКАЗАТЕЛЬ

**Канонический хендоф роли devops: `.coord/protocols/devops-handoff.md`.**

Содержимое этого файла (живой хендоф, написанный сессией `devops-0829` при передаче роли,
22 326 байт) перенесено туда **дословно, без единого изменения** и составляет его живую часть.
Ничего не потеряно; файл оставлен заглушкой, а не удалён.

Слито куратором `curator-0817` 2026-08-31T09:2xZ. Причина: два почти одинаковых имени в одном
каталоге приводят к тому, что следующего поднимают по неверному файлу — при том что этот был живым
и вне git, а `devops-handoff.md` трекался и устарел по состоянию.

Не редактировать. Все правки — в `devops-handoff.md`.
