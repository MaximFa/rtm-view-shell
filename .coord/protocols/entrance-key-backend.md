# КЛЮЧ — RTM Backend (роль-специфичная часть)

> **Открывать только после сдачи ответов.** Парный к `entrance-key-COMMON.md`.
> Проверки, а не выводы; числа пере-снимать теми же командами на текущем `v3`.
> Презумпция провала: неоднозначность = провал.

**KR (разбор).**
Требуется: **Shell и БД катятся ОДНИМ релизом** (RTM-DEPLOY-001). Иначе в момент применения миграции
старый Shell зовёт функцию со старой сигнатурой → `42883` → виджет падает не «когда-нибудь», а ровно в
окно деплоя. Инцидент DayTrend 2026-06-07. Ответ «миграция есть, значит нормально» = ПРОВАЛ: наличие
миграции и есть источник разрыва, если релизы разъехались.
Проверка нормы: `git show v3:.coord/migration/project-memory/deploy_fn_signature_change.md | grep -c '42883'` → **≥1**.

**Живая часть.**
- `§C` прогнан ПОЛНОСТЬЮ, с сырым выводом по каждому пункту. Пересказ = провал пункта.
  Упавший пункт сам по себе не провал; провал — не назвать сторону ошибки и не сослаться на «Reality wins».
- Роль-скилл пришпилен: `git rev-parse v3:.claude/skills/role-backend/role-backend.md` даёт sha.
- **Вопрос о проверках — ключевой, и ответ известен:** `§C` этой роли написаны против РАБОЧЕГО ДЕРЕВА
  (`Test-Path`, `Select-String`, `grep` по файлам), то есть опираются на маунт — поверхность, которой
  NORM-CUR-13 не верит. Единственное исключение в колонии — `role-security`, чьи проверки идут через
  `git show` / `git ls-tree` / `git grep`.
  Засчитывается: кандидат назвал это сам, с примерами своих пунктов, и объяснил опасность (проверка
  подтверждает истину по источнику, которому правило не доверяет → ложная зелёная).
  ПРОВАЛ: «мои проверки в порядке» без разбора. Это не придирка — исправление ложится на владельца роли,
  и тест ровно для того, чтобы владелец увидел это сам.
