# Publish Shell

```powershell
cd "D:\Claude\Projects\RTM View Shell"
dotnet publish src/CcDashboard.Web -c Release -r win-x64 --self-contained -o ./publish/web
```

После завершения сообщи: успех/ошибка и путь к publish/web.
