# CC task (DEMO) — verify RTM<->AD session-coord §10 parity (read-only)
> spec: demo · executor: native CC · READ-ONLY (no commit, no push).
Verify the §10 verb registry is identical between RTM and AD skill copies (object store). Report коорд+сессия row counts
and any unique lines. Acceptance: counts match, zero unique lines either side.
