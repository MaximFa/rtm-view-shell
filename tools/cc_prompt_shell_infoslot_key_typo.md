# CC task — PR234-L10N-KEYLEAK-01 / шаг 1a: fix the one-letter key typo in InfoSlotWidget

**Author:** shell-0919b · 2026-09-20 · **Branch:** `v3` (= `bc2b714`) · Code/comments EN.
**Authority:** coordinator-0919b 19.09 23:12Z — правильное имя `InfoSlots_`, опечатка в разметке.

## INIT / служебные части (§0.6a, §0.6b, §0.6, §0.5 — в силе; амендмент 19.09 их не трогал)
`cd "D:\Claude\Projects\RTM View Shell"` · **§0.6a** проверка целостности до первой правки:
`git status --short`, по каждому `M`-файлу сверить число строк с `git show HEAD:"$f"`, усечённый —
восстановить из HEAD и **не продолжать**. Инструмент `Edit` ЗАПРЕЩЁН, после записи `fsync`.
**§0.6b** биндинг: преамбула в `.coord/cc/shell.md` ДО первой правки, постамбула `RESULT` после.
**`commit.lock`** обязателен (§42.4, раздел «Коммит»). **§0.6** пост-коммитная сверка.
**§0.5/PD-007** последним действием `git show HEAD:"$f" > "$f"` по закоммиченному файлу, затем `sync`.
`journal` не ведём; авторство роли и сессии — строкой в сообщении коммита. `NO push`.

## Дефект
Разметка зовёт ключ, которого нет в двух словарях из трёх. Ключ существует под ПОЛНЫМ именем
`InfoSlots_ExpiresAt` во всех трёх `.resx` и зовётся из двух других мест; в виджете он написан
без `s`. На иврите и английском пользователь увидит на экране имя ключа вместо текста; на русском
дефект не виден, потому что там случайно живёт осиротевший ключ с опечаточным именем.

## Задача — ОДНА БУКВА, один файл
`src/CcDashboard.Web/Components/Widgets/InfoSlotWidget.razor:57`
```
было:  @L["InfoSlot_ExpiresAt"]
стало: @L["InfoSlots_ExpiresAt"]
```
Больше в этом файле не менять НИЧЕГО. `.resx` не трогать ни одним символом — осиротевший ключ из
`ru-RU` снимает backend отдельной единицей, это НЕ твоя работа и не в этом коммите.

## Входной пин — СТОП при расхождении
```
git hash-object src/CcDashboard.Web/Components/Widgets/InfoSlotWidget.razor -> 14828780302405b7c28e014874dde2674d38363d
```

## ИГЛЫ — ожидание печатается ДО измеренного
Прибор один и тот же по всему корпусу (`src/`, каталоги `obj/` и `bin/` исключены):
```bash
grep -rn '\["<ИМЯ>"\]' --include=*.razor --include=*.cs src/ | grep -v '/obj/' | wc -l
```
| имя ключа | ДО | ПОСЛЕ |
|---|---|---|
| `InfoSlot_ExpiresAt` (опечатка) | 1 | **0** |
| `InfoSlots_ExpiresAt` (верное) | 2 | **3** |
| сумма двух | 3 | **3** — не удалили и не удвоили, а переименовали |
| `.resx` в коммите | — | **0 файлов** |
| изменённых файлов в коммите | — | **ровно 1**, удалений 0 |
| `dotnet build` Release | — | **0 errors** |

**Отрицательная половина (обязательна, иначе результат не засчитан):** тот же прибор на выдуманном
имени `ZZZ_Not_A_Key` обязан вернуть **0** и ДО, и ПОСЛЕ. Вернул не ноль — прибор ловит не то,
что заявлено, СТОП. (Имя предъявлено живым прибором, а не файлом: файла под него нет по построению.)

**Положительный контроль:** прибор на `InfoSlots_ExpiresAt` ДО правки обязан вернуть **2**, а не 0 —
это доказывает, что он вообще способен находить верное имя, а ноль опечатки — не его немота.

## Не в этой задаче
Правка `.resx` (backend), разметка `ScreenEditorPage` (ждёт 19 ключей), любые другие ключи.

## БИНДИНГ — преамбула (ОТКРЫТЬ в `.coord/cc/shell.md` ДО первой правки, Python + os.fsync, не затирая)
```
## 2026-09-20 | binding: shell <-> CC | directive: tools/cc_prompt_shell_infoslot_key_typo.md | status: open
Автор `shell-0919b`. Предмет `PR234-L10N-KEYLEAK-01` шаг 1a. Основание — coordinator-0919b 19.09 23:12Z.
### ЗАМЕР ДО
    v3 = bc2b714
    InfoSlotWidget.razor  14828780…  InfoSlot_ExpiresAt 1 · InfoSlots_ExpiresAt 2 (по всему src/)
    словари: InfoSlots_ExpiresAt 1/1/1 · InfoSlot_ExpiresAt 0/0/1 (осиротевший в ru — не наш)
### СТАТУС: §4 PASS (coordinator-0919b). Прогон запущен оператором.
```

## БИНДИНГ — постамбула (ДОПИСАТЬ после прогона, в том числе если он сорвался)
```
### RESULT (CC, <дата UTC>)
- коммит: <sha> | файлы: <перечислить>
- предикаты факт/ожидание: <все строки таблицы выше>
- отрицательная половина: ZZZ_Not_A_Key = <0 | иначе STOP>
- положительный контроль: InfoSlots_ExpiresAt ДО = <2 | иначе STOP>
- сборка: <errors> errors
- отклонения от промпта: <нет | перечислить>
### СТАТУС: <delivered | stopped: причина>
```

## Коммит
Один коммит, узким явным путём, без `-A`. **NO push**, на боевую не выкатывать.
Сообщение: `fix(shell): call the InfoSlots_ExpiresAt key by its real name so the label is not a key name [shell]`

**`commit.lock` (§42.4) — обязателен, им гейтим.** Порядок:
1. Взять замок атомарно: Python `open(".coord/locks/commit.lock", "x")`, внутрь — роль, sha сессии, `acquired` (UTC).
   Файл существует — ретрай 5 x 60 c; не взял — **коммит не делать**, напечатать владельца замка и СТОП.
2. `acquired` старше 15 минут — напечатать содержимое и ЖДАТЬ решения оператора. **Не удалять самому.**
3. Под замком: `git add` (только заявленный путь) -> `git commit` -> §0.6 пост-коммит-сверка.
4. Отпустить: удалить `commit.lock`, `sync`. Коммит сорвался — замок всё равно отпустить.
Замок покрывает и plumbing-путь (§0.4). Обойти замок нельзя ни при каком объёме правки.
