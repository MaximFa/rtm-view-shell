# Skill diff for operator — session-coord §10/§8 updates

> Generated by CC task cc_prompt_codify_protocol.md (2026-06-12).
> These changes go into `.claude/skills/session-coord/session-coord.md`.
> The skill is a read-only cache; only the OPERATOR applies changes via Settings.

---

## §10 — add note about inbox-hook default

Add after the command table (around line 214, after "Default duty regardless of commands"):

```markdown
**Inbox-check lifecycle hook (SAFE default, per CLAUDE.md §42.8):**
Sessions read the PERMANENT role mailbox `inbox/<role>.md`. On each turn:
- Turn-start: peek for new content after last `> handled` marker ("N new" check).
- Idle between tasks: auto-process (`коорд: входящие` semantics).
- Mid-task: hold "N pending" — do NOT interrupt.
- Completion hook: end reply with "разобрать входящие? (N новых)" if pending.
STRICT variant (auto-process mid-task) = operator opt-in.
```

---

## §8 / Lessons table — add two items

Add to the lessons table (around line 140):

| ID | Lesson | Source |
|---|---|---|
| L-SC-22 | **Permanent role mailbox** — each role (coordinator, devops, metrics, etc.) has a permanent inbox at `inbox/<role>.md` (NOT per-session-slug). Sessions inherit and append to it. On takeover, the successor continues reading the same role inbox. | 2026-06-12, CLAUDE.md §42.8 |
| L-SC-23 | **Tech Writer = mandatory doc-sync gate** — the push-barrier quorum is NOT complete without Tech Writer''s `READY` or `HOLD`. Impact triage: no-impact = instant READY; minor = READY + doc-debt; doc-blocking = HOLD until doc in `approved/`. | 2026-06-12, CLAUDE.md §42.7 |

---

## Operator action

1. Open `.claude/skills/session-coord/session-coord.md` in Settings
2. Add the §10 note after the command table
3. Add the two lessons (L-SC-22, L-SC-23) to the §8 table
4. Save

The CLAUDE.md §42.7 and §42.8 are already committed — this skill update makes sessions aware of the norms.