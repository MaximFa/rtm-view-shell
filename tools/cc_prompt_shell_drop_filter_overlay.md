# CC task — PR234-FILTER-OVERLAY-DEAD-01: remove the dead `.filter-overlay` rule

**Author:** shell-0919b · 2026-09-24 · **Branch:** `v3` (= `e6d9315`) · Code/comments EN.
**Authority:** coordinator-0919b 24.09 08:43Z — выдано из очереди после закрытия `CLOSE-SCOPE-01`.

## INIT / служебные части (§0.6a, §0.6b, §0.6, §0.5 — в силе)
`cd "D:\Claude\Projects\RTM View Shell"` · **§0.6a** целостность до первой правки: `git status --short`,
по каждому `M`-файлу сверить строки с `git show HEAD:"$f"`, усечённый — восстановить и НЕ продолжать.
`Edit` ЗАПРЕЩЁН, после записи `fsync`. **§0.6b** биндинг: преамбула в `.coord/cc/shell.md` ДО правки,
постамбула `RESULT` после. **`commit.lock`** обязателен (§42.4). **§0.6** пост-коммитная сверка.
**§0.5/PD-007** последним действием `git show HEAD:"$f" > "$f"`, затем `sync`. `NO push`.

## ⛔ Это НЕ уборка. Обоснование — почему правка закрепляет предмет, а не наводит красоту
`.filter-overlay` — заготовка ОТВЕРГНУТОГО варианта лечения `PR234-FILTER-CLOSE-SCOPE-01`
(прозрачная накладка, ловящая клик мимо попапа). Вариант отвергнут **измерением**: внутри
масштабируемого слоя `fullscreen-design-layer` (`transform: scale`) `position: fixed` и `absolute`
неразличимы, и накладка не достаёт до шапки страницы — 82 px остаются непокрытыми
[измерено shell-0919b 20.09 на стенде]. Принят вариант с обработчиком на корне страницы.

**Почему остаток опасен, а не безобиден:** класс лежит готовым к употреблению и выглядит как
намеренная часть механизма. Первый, кто решит «попап надо закрывать накладкой», найдёт его,
применит — и вернёт ровно тот дефект, который мы закрыли: клик по шапке перестанет закрывать попап.
Он вернёт его **не написав ни строки нового кода**, то есть правка не будет выглядеть как правка.
Это родня `PR234-FILTER-GETORCREATE-DEAD-01` (метод, создающий живой фильтр, остался объявлением
после `DRAFT-01`) — **снос мёртвого есть закрепление единицы, и идёт отдельной единицей**, потому
что смешанная приёмка лишает обе различения.

## Задача — удалить один блок из одного файла
`src/CcDashboard.Web/Components/Widgets/AgentGridWidget.razor.css`, строки 149-153:
```css
/* Filter overlay - transparent click catcher */
.filter-overlay {
    background: transparent;
    cursor: default;
}
```
Удалить правило ВМЕСТЕ с его комментарием-заголовком. Пустую строку-разделитель перед соседним
блоком не ломать. **Соседний блок `.filter-dropdown { z-index: 1050 !important; }` (строки 144-147)
НЕ ТРОГАТЬ** — он живой и используется разметкой.
Больше в файле не менять ничего. Других файлов не трогать.

## Входной пин — СТОП при расхождении
```
git hash-object src/CcDashboard.Web/Components/Widgets/AgentGridWidget.razor.css -> 42bc4592f3efed1ff1ddf602959de2da128c915b
```

## ПРЕДИКАТЫ — единица названа: ВХОЖДЕНИЯ (`grep -ro … | wc -l`), каталоги `obj/` и `bin/` исключены
Ожидания сняты по ветке и пере-сняты на `e6d9315` до прогона (числа не изменились: чужие коммиты в db/ и probes/):
| предикат (по всему `src/`) | ДО | ПОСЛЕ |
|---|---|---|
| `filter-overlay` | 1 | **0** |
| `filter-dropdown` (сторож соседа в том же файле) | 9 | **9** |
| `popup-anchor` (сторож живого класса семьи) | 3 | **3** |
| изменённых файлов в коммите | — | **ровно 1**, и это `.css` |
| `.razor` / `.cs` в коммите | — | **0 файлов** |
| `dotnet build` Release | — | **0 errors** |

**Отрицательная половина (обязательна):** тот же прибор на выдуманном классе `zzz-overlay`
обязан дать **0** и ДО, и ПОСЛЕ. Не ноль — прибор ловит не то, СТОП.
**Положительный контроль:** `filter-overlay` ДО правки = **1** (не 0) — это доказывает, что прибор
вообще способен найти класс; ноль «после» без единицы «до» есть немота прибора, а не результат.

**Почему ноль здесь безопасен, в отличие от прочих наших нулей:** класс не используется НИ В ОДНОЙ
разметке — единственное вхождение по всему `src/` и есть само определение
[измерено автором, пере-снято на `e6d9315`]. Поэтому удаление не может изменить поведение: нечему отвалиться.

## Живая приёмка — минимальная, и она про НЕизменение
После выката: попап фильтра открывается, закрывается пятью путями (заголовок, пагинация, пустое
место, соседний виджет, шапка), клик внутри попапа не закрывает. То есть `CLOSE-SCOPE-01` остался
в том же состоянии, в каком его принял оператор. Ничего нового этот предмет на экране не создаёт.

## БИНДИНГ — преамбула (ОТКРЫТЬ в `.coord/cc/shell.md` ДО первой правки, Python + os.fsync, не затирая)
```
## 2026-09-24 | binding: shell <-> CC | directive: tools/cc_prompt_shell_drop_filter_overlay.md | status: open
Автор `shell-0919b`. Предмет `PR234-FILTER-OVERLAY-DEAD-01`. Основание — coordinator-0919b 24.09 08:43Z.
### ЗАМЕР ДО
    v3 = e6d9315
    AgentGridWidget.razor.css  42bc4592…  .filter-overlay определён на :150, вызовов в разметке 0
    по всему src/ (без obj, bin): filter-overlay 1 · filter-dropdown 9 · popup-anchor 3
### СТАТУС: §4 — <вписать вердикт>. Прогон запущен оператором.
```

## БИНДИНГ — постамбула (ДОПИСАТЬ после прогона, в том числе если он сорвался)
```
### RESULT (CC, <дата UTC>)
- коммит: <sha> | файлы: <перечислить>
- предикаты факт/ожидание: <все строки таблицы, единица названа у каждого>
- сторожа: filter-dropdown 9/9 · popup-anchor 3/3 · .razor и .cs в коммите 0
- отрицательная половина: zzz-overlay = <0 | иначе STOP>
- положительный контроль: filter-overlay ДО = <1 | иначе STOP>
- сборка: <errors> errors
- отклонения от промпта: <нет | перечислить>
### СТАТУС: <delivered | stopped: причина>
```

## Коммит
Один коммит, одним узким явным путём, без `-A`. **NO push**, на боевую не выкатывать.
Сообщение: `chore(shell): drop the dead .filter-overlay rule left by the rejected popup-close variant [shell]`

**`commit.lock` (§42.4) — обязателен, им гейтим.** Порядок:
1. Взять замок атомарно: Python `open(".coord/locks/commit.lock", "x")`, внутрь — роль, sha сессии, `acquired` (UTC).
   Файл существует — ретрай 5 x 60 c; не взял — **коммит не делать**, напечатать владельца и СТОП.
2. `acquired` старше 15 минут — напечатать содержимое и ЖДАТЬ решения оператора. **Не удалять самому.**
3. Под замком: `git add` (только заявленный путь) -> `git commit` -> §0.6 пост-коммит-сверка.
4. Отпустить: удалить `commit.lock`, `sync`. Коммит сорвался — замок всё равно отпустить.
Замок покрывает и plumbing-путь (§0.4). Обойти замок нельзя ни при каком объёме правки.
